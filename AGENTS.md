# TimeFit 에이전트 작업 규칙

## 목적

TimeFit은 부산에서 약속 전 남은 시간을 활용할 장소와 코스를 제안하는 iOS 우선 Expo 앱이다. 추천의 신뢰성, 처음 쓰는 사람도 이해할 수 있는 UIUX, 재현 가능한 검증을 같은 우선순위로 다룬다.

## 비판적 협업

- 사용자의 제안은 요구사항 후보이지 자동 확정 사항이 아니다. 일정, 데이터 근거, 보안·개인정보, 비용·호출량, UX, 유지보수성, 기술 부채 관점에서 반례와 부작용을 먼저 검토한다.
- 단순히 현재 버그를 가리는 예외 처리보다, 여러 입력에서 적용되는 정책·모듈·테스트 경계를 우선한다.
- 공모전 MVP라는 이유로 임시 구조를 정당화하지 않는다. 앱스토어 배포와 이후 기능 확장을 전제로 API 경계, 데이터 모델, 테스트 가능성, 관찰 가능성을 설계한다.
- 반대할 때는 구현을 멈추기 위한 추상이 아니라, 문제가 되는 가정·영향 범위·더 나은 대안·결정에 필요한 근거를 구체적으로 제시한다.

## 기준 문서

- 추천 규칙의 단일 기준: `docs/03_product/추천로직.md`
- 사용자 요구사항·검증 상태: `docs/테스트.md`
- 화면 공통 규칙: `docs/03_product/UIUX_공통규칙.md`
- UIUX 수동/E2E 시나리오: `docs/03_product/UIUX_테스트명세.md`
- DB 구조: `docs/04_backend/데이터베이스설계.md`

문서끼리 충돌하거나 코드와 맞지 않으면 임의로 한 문서를 덮어쓰지 않는다. 현재 동작, 기존 결정일, 사용자에게 확인할 선택지를 함께 제시하고 확정된 기준만 갱신한다.

### 변경 이력 기록 원칙

- 추천·데이터·UIUX·DB 정책 문서를 수정할 때는 최종 규칙만 남기지 않는다. `이전 방식 → 발생한 문제/관찰 → 교체한 방식 → 교체 이유 → 상태(현행/구현 전/철회)`를 같은 문서에 기록한다.
- 철회한 방식은 삭제하지 않고 `철회` 상태로 남긴다. 단, 실행 기준이 되는 현행 규칙과 시각적으로 구분해 새 작업자가 철회안을 다시 구현하지 않게 한다.
- 단편적 예외 처리로 바뀐 규칙은 해당 사례뿐 아니라 적용 범위와 반례를 기록한다. 이력이 없는 정책 변경은 완료로 간주하지 않는다.

## 요구사항 관리

1. 새 요구사항은 `docs/테스트.md`의 기존 ID와 먼저 비교한다.
2. 관계를 `동일`, `보완`, `충돌`, `신규` 중 하나로 판정한다.
3. 충돌이면 기존 규칙을 조용히 대체하지 않는다. 사용자에게 영향을 설명하고 결정받는다.
4. 요구사항은 관찰 가능한 기대 결과, 입력 조건, 예외, 검증 방법을 포함한다.
5. 추천·데이터·UIUX 규칙을 바꾸면 `추천로직.md`, `테스트.md`, 해당 UIUX 문서를 같은 변경에서 갱신한다.
6. 인터뷰와 결정은 트리로 관리한다. 자식 질문에는 부모 결정 ID, 이 질문이 필요한 이유, 부모 결정에 미치는 영향을 함께 적는다.
7. 부모 결정이 미확정이면 자식 답변을 최종 구현 규칙으로 승격하지 않는다. 자식 답변은 `조건부`로 기록하고, 부모 확정 뒤 충돌을 다시 검사한다.

## 하네스 엔지니어링

실시간 시각, GPS, 외부 API, DB 상태에 따라 결과가 달라지는 기능은 반복 가능한 하네스로 먼저 검증한다.

- **고정 입력**: 현재 시각, 출발/약속 좌표, 남은 시간, 로그인 사용자, 장바구니 상태를 코드로 주입할 수 있어야 한다.
- **외부 API 어댑터**: TMAP, ODsay, TourAPI, 카카오, Supabase 호출은 화면이나 추천 계산에 직접 섞지 않는다. 성공·지연·실패·빈 응답 fixture를 주입할 수 있는 경계를 둔다.
- **데이터 fixture**: 대표 장소와 운영시간, 동일 단지, 중복 장소, 운영 불가 장소를 최소 데이터로 구성한다. 운영 API를 반복 호출하는 회귀 테스트는 금지한다.
- **시간/GPS fixture**: 새벽, 짧은 자투리, 가까운/먼 약속, 약속 없음, GPS 실패를 고정 시나리오로 보관한다.
- **결과 스냅샷**: 추천 결과는 장소 ID·전략·가능 수단·시간 상태를 비교한다. 제목이나 랜덤 순서만 비교하지 않는다.
- **정리**: 테스트가 만든 DB 사용자·코스·알림은 테스트 종료 시 삭제하거나 전용 테스트 데이터로 격리한다.

하네스는 실제 API 정확도를 대체하지 않는다. 실제 API 검증은 호출량을 기록한 소수의 수동/통합 시나리오로 분리한다.

## MCP와 자동화

MCP는 다음 목적으로만 사용한다.

- iOS 시뮬레이터/실기기 조작, 화면 캡처, 콘솔·크래시 로그 수집
- Supabase 테스트 데이터 생성·조회·정리와 RLS 검증
- 고정 fixture를 사용한 E2E 흐름 실행

외부 API 키, 실제 사용자 데이터, 운영 Supabase 데이터를 MCP 테스트 입력으로 쓰지 않는다. MCP 실행 결과에는 시나리오 ID, 입력 fixture, 성공/실패, 캡처 또는 로그 위치를 남긴다.

## 테스트 계층과 완료 기준

1. **단위 테스트**: 순수 추천 계산, 공간 범위, 시간·운영시간 게이트, 장바구니 조립. 네트워크·React·DB 없이 실행한다.
2. **통합 계약 테스트**: 화면 컨테이너와 표시 컴포넌트, 저장 payload, API 어댑터 요청/응답 변환을 고정 fixture로 검증한다.
3. **E2E 테스트**: 시간 입력 → 후보 지도 → 장소 상세 → 장바구니 → 저장 → 내 코스 → 진행/변경 흐름을 실행한다.
4. **실기기 확인**: 지도 WebView, 외부 카카오맵 전환, 위치/알림 권한, 네트워크 실패는 iOS 실기기에서 확인한다.

코드 변경 전 실패하는 테스트 또는 재현 시나리오를 먼저 추가한다. 구현 후에는 최소 `npm run test:typecheck`, `npm run test:ui`, `npm test`를 실행한다. UI·네이티브 의존 변경은 iOS 번들 또는 E2E를 추가로 검증한다.

## UIUX와 구조

- 화면 컨테이너는 상태·API·저장·네비게이션 연결만 담당하고, 표시 컴포넌트와 순수 계산을 분리한다.
- 특정 오류를 막기 위한 조건을 기존 화면에 계속 누적하지 않는다. 공통 정책은 엔진 또는 전용 모듈로 이동한다.
- 디자인 토큰과 공통 컴포넌트는 실제로 두 곳 이상에서 같은 규격이 확인될 때만 만든다.
- 모든 상단 컨트롤은 safe area 아래에 둔다. 불필요한 설명문보다 상태·아이콘·행동으로 다음 동작을 전달한다.

## 보안과 기록

- `.env`, `.env.local`, API 키, 액세스 토큰, 실사용자 정보는 로그·문서·테스트 fixture·커밋에 넣지 않는다.
- DB 스키마 변경은 Supabase migration으로 관리한다.
- 커밋 전에는 관련 문서와 테스트 결과를 갱신하고, 사용자가 만든 변경을 되돌리지 않는다.
