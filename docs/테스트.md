# TimeFit 테스트 현황

## RELEASE-CONNECT-FINAL-PARALLEL-01 — 2026-09-10 TestFlight 내부 검증 수락

기존 출시 수동 검증의 **보완**. 사용자 보고 기준 TestFlight **1.0.0(1)**의 실행·개발 UI 미노출·위치 권한 미요청·CAPTCHA 로그인/재로그인·문서 링크·수동 검색/지도/주변·추천부터 길찾기 E2E·Live Activity 도착/출발/완료·앱 동기화·History 증가·재실행 보존 모두 통과. 상세: [최신 수락 기록](work/integration-decision/release-connect-final-parallel.md).

이전 실기기 대기 → 해당 빌드의 사용자 관찰 → 내부 체크리스트 수락(현행). 아래 과거 미검증 표기를 전체 항목 완료로 일괄 변경하지 않는다. 보고에 없는 예외·계정 삭제·다른 OS 시험, App Privacy 게시 및 심사 제출은 별도다. 이번 기록에서는 제품 테스트를 재실행하지 않았다.

## DEC-RELEASE-MANUAL-LOCATION-01 — 2026-09-09 승인·미검증

REC-13 및 UX-07/12/20/21/25의 GPS 부분을 **충돌·사용자 승인 교체**, 수동 선택·기록·학습은 유지한다. 이전 GPS 출발지/주변 조회 → 자동 측위 없는 출시 필요 → 검색·지도 핀 확정만 허용 → 출시 구조 단순화 → 구현 전. 과거 GPS 규칙은 철회 이력이며 최대180분/2곳은 불변이다.

- [ ] 신규/기존 권한 허용/거절 모두 GPS 조회·권한 요청0, legacy·WebView·딥링크 포함.
- [ ] 기준 장소 미선택은 선택 안내, 검색/지도 확정 후 정상 추천·주변 거리순. 기본 지도 중심 자동 확정0.
- [ ] 과거 GPS snapshot 자동 복원·재전송0, 일반 방문 이력·학습 표본 무단 삭제0.
- [ ] 수동 경로·앱/웹 클릭 시작·Live Activity·완료/개인화/guest 가져오기 회귀 PASS.
- [ ] 출시 네이티브 설정·문서·실기기 일치. 신고 제외 판단과 기술 PASS 구분.

역할별 명령·입력·예외·검증: [수동 장소 전환](work/integration-decision/release-manual-location.md). 아직 구현/실기기 완료가 아니다.

## DEC-KAKAO-ROUTE-START-02 — 2026-09-09 현행·수락

관계: COURSE-13/15 및 UXV-43/51의 **보완·시작 조건 충돌 교체**. 사용자 확정에 따라 코스 길찾기 클릭을 이동 의사로 저장한다. 앱/Live Activity 공유 이동 → 외부 열기, 웹 팝업 취소·닫기·복귀에도 유지한다. 전체 열기 실패만 이번 신규 의사를 안전하게 복구하며 재열기/중복/늦은 실패는 확정 상태를 덮어쓰지 않는다. 클릭은 도착·방문·체류 학습 증거가 아니며 주변 둘러보기에는 적용하지 않는다.

사용자 보고: 직전 확인 목록 1번 전체에 대해 ‘1번 확인 완료’. **사용자 수동 확인 수락**이며 기기별 상세·실행 빌드·캡처는 미제공이다. 자동 UI 731 PASS/기존 skip1, core421 PASS는 기존 로그 확인; 이번 문서 작업에서 재실행하지 않았다. 이 문서 아래 과거 handoff 성공 전 이동 미확정 문구는 시작 조건에 한해 철회 이력이다. 입력·예외·검증 및 인수인계: [현행 결정](work/integration-decision/kakao-route-start-decision.md).

## DEC-RELEASE-AGE-01 (2026-09-09, 확정·구현 전)

DOCS-04 미정 가입 연령 정책의 보완. 나이별 추천용 생년월일 수집은 복원하지 않는다. 신규 가입에 기본 미선택 `[필수] 만 14세 이상입니다` 자기확인을 추가하고 미확인 가입을 차단, 로그인 방식 유지, 약관/처리방침 가입 대상 일치. 부모 동의·본인인증·생년월일 수집 제외. 비로그인/기존 계정에 대한 전역 차단은 미확정이며 임의 추가 금지. 이전→문제→교체→이유와 입력·예외·검증은 [사용자 결정](work/integration-decision/release-user-decisions.md)에 기록. 구현/실기기 완료 아님.

## RELEASE-DEPLOY-PREP-01 / UX-RELEASE-EXIT-03 (2026-09-09)

관계: 출시 후속 보완·현행 메인/기록 탭 종료 정책 복구. 기록 없는 종료→과거 MyCourses 도달 발견→명시 기록없는 종료만 Home reset, 기록 성공은 ActivityRecord 유지→폐기한 정상 내 코스 흐름 재진입 방지→구현 전. 자동 저장 실패는 진행 유지·재시도 가능, 명시 종료 전에 강제 종료 금지. terminal/개인화/완료 멱등 의미 불변. 입력·실패·중복·뒤로가기·LA 성공 및 안전 로그 fixture 검증을 [실행 명령](work/integration-decision/release-deploy-preparation.md)에 지정. DB 함수 배포/서명 자격은 준비 단계이며 실제 원격 변경 별도 승인.

## RELEASE-MINIMAL-FOLLOWUP-01 (2026-09-08)

관계: RELEASE-EXECUTION-WAVE-01 보완. 5개 준비 완료→Extension privacy/Archive·운영 미확인 관찰→[네이티브 최소 보완·DB/API 읽기 전용 운영 확인](work/integration-decision/release-minimal-followup.md)으로 분리→UI/기능 동결 유지→명령 작성·실행 전. 기대결과/입력/예외/검증은 연결 문서에 명시. 기존 개인화 C와 사진 감사 재실행 없음. 계정 탈퇴는 승인된 전용 계정1회 후속으로 제한하며 현재 계정 삭제 금지. 제출/공개 미승인.

## RELEASE-EXECUTION-WAVE-01 — 출시 전환 (2026-09-08)

관계: RELEASE-SUBMISSION-FINAL-01 보완. UI 반복 수정 → 일정 지연 위험 → 사용자 승인으로 UI/기능 동결, 제출 설정·문서 연결·확인된 차단만 최소 보완 → 출시 집중 → 현행/실행 전. 기대결과: 동일 최종 후보에서 개발 도구 비노출·짜투리/iPhone 설정·공개 URL/가입 문서 일치·회귀 PASS. 위치 문의 답변 대기와 기술 PASS는 별도이며 공개 가능으로 자동 승격하지 않는다. 입력/예외/역할/검증은 [실행 명령](work/integration-decision/release-execution-wave.md). 제품 정책 불변, 과거120분·개인화 보류·UI 구현 전 표기를 복원하지 않는다.

## RELEASE-SUBMISSION-FINAL-01 착수 (2026-09-08)

U-LIVE-ACTIVITY-FINAL-02 최종 안내 실기기 항목은 사용자 ‘확인했다’ 보고로 확인 접수. 신규 도구 실행/별도 빌드 시험으로 기록하지 않는다. 기능 확장 후 출시 준비 요청에 따라 문서 정합성·읽기 전용 배포 코드 감사로 전환한다. 자동·실기기 기능 확인과 최종 제출 artifact/권리/공개 URL/스토어 준비 완료는 구분한다. docs/work/integration-decision/release-submission-final.md 참조. 게시·운영 쓰기·업로드 미승인.

## UX-FINAL-INTERACTION-WAVE-01 (2026-09-08, 추가 보완·구현 전)

U-MAIN-COURSE-POLISH-01 실기기4항목 사용자 확인 접수. 후속은 메인 두 줄/소제목·빈 결과 정확한 안내·추가 후보 pending·review 접기·앰버 현재 점·계산 그룹 중앙·지도 카메라 재중심, 메인 내부 스택, LA 좌우 배치·최종 완료 액션이다. 기존 밀도/행동 보완→추가 피드백→역할별 세 작업으로 교체/추가→탐색·완료 접근성 개선. 지도는 카메라만 변경, active back은 메인 이어가기, LA 완료는 앱과 같은 적격 단계/controller이며 중간 완료·중복 기록/학습 금지. 상세 입력/예외/검증은 docs/work/uiux/final-interaction-wave.md. 기존 날짜·180/2곳·학습/소유권 정책 불변. 부모 작업 승인 아래 OS 완료 구현 경계는 조사·검증하며 자동 및 실기기 수락을 분리한다.

## UX-MAIN-COURSE-POLISH-01 / DATA-PLACE-PHOTO-RECHECK-01 (2026-09-08, 승인·구현 전)

관계: 기존 메인·설정·추천·코스 UI 보완, 사진 권리 재조사는 기존 fail-closed 유지 보완. 큰 카드/촘촘한 배치/분산 행동 → 파악·접근 불편 → 여백 계층·작은 썸네일 확인 타임라인·진행 하단 고정 행동/현재 점 강조·요약 우측 취소 → 정보와 행동 가시성 개선. 상세 명령 docs/work/uiux/main-course-final-polish.md의 A/B 및 검증을 따른다. 메인 TIMEFIT 장식 제거, 명확한 약속 전 문구, 출발/도착 세로 점 연결, 영업시간 흰색 강조를 포함한다. 작은 화면/큰 글씨/사진 없음/1·2곳/LA 공유 상태/중복 탭·취소 확인 fixture 필수. 시간·추천·날짜·개인화 정책은 불변이다. 사진은 docs/work/data-curation/place-photo-recheck.md에 따라 공식 근거와 카드/마커 가공 조건을 구분하며 미확인은 기본 이미지 유지. 자동·실기기 수락은 별도다.

## DEC-RELEASE-180-01 (2026-09-08, 확정·구현/검증 전)

기존120분 입력 상한 요구 보완:180 허용·181 거절, 최대2곳 유지. 긴 입력 요청→상한180으로 교체→선택 가능 범위 확대,240/3곳 미채택. 기존120이하·체류/개인화/운영시간/호출 예산 불변. 경계119/120/121/179/180/181·자정/익일·1/2곳·기존 진행 복구·알림 fixture 필수. 상세 docs/work/integration-decision/release-three-hour-two-stop.md. 과거 완료 테스트를 새180분 검증 완료로 해석하지 않는다.

## UX-PROFILE-AUTH-POLISH-02 (2026-09-08, 구현 전)

기존 프로필 관리/기록 표시/인증 UX 보완. 내정보에서 관리 버튼으로 별도 화면 진입→닉네임 변경·계정 삭제·로그아웃 순서. 비로그인 상태 설명 대신 로그인+별도 체류 동의 기반 혜택 안내. 유효한 현재 계정 닉네임만 `{이름}님의 자투리 기록`, 나머지 `나의 자투리 기록`. 로그인/가입 폼 입력과 오류 분리, 이탈 모드 비밀번호 정리. inline 관리·중복 문구·공유 input→사용자 불편 관찰→역할별 화면/폼 분리→구현 전. 계정 변경/late 응답·폼 교차0·기존 재인증/동의/삭제 계약 fixture 필수. docs/work/uiux/profile-management-auth-polish.md 참조.

## UX-HISTORY-FINAL-03 (2026-09-08, 확정·구현 전)

UX-HISTORY-POLISH-02 보완. 현재 Animated 수정 후에도 부자연스러운 drag가 관찰돼 최신 코드에서 재현/수정한 뒤 상단4박스를 지도·큰 방문 횟수(places 합계, 반복 포함)·짧은 카테고리 정보로 교체한다. 날짜 옆 ⋯/long press 삭제 메뉴와 swipe/접근성은 같은 기존 삭제 controller를 쓴다. 상시 swipe 설명·자동 swipe 시연은 미사용. 상세 이력·A 선행 게이트·gesture/메뉴 충돌·빈/중복/import/pending/좌표 누락 검증은 docs/work/uiux/history-motion-summary-menu.md. 저장/개인화·completionId 삭제 단위·전체 삭제 범위는 불변이다.

## UX-HISTORY-POLISH-02 (2026-09-08)

관계: UX-HISTORY-SWIPE-01 보완·카테고리 목록 필터 신규. 구현/검증 전. 기록 행과 휴지통은 상단 통계 카드와 같은 radius14, 전체 삭제는 섹션 제목 우측, 필터는 목록만 적용한다. 기존 completionId 코스 행/계정 전체 삭제 범위는 유지한다. 복합2곳은 선택 카테고리 포함 기록을 한 번 표시한다. 느린 드래그/반전/정착 중 재잡기/세로 경쟁/필터·계정 변경 및 필터 중 전체 삭제 범위 fixture 필수. 코드의 move setState·release 즉시 스냅은 확인됐지만 실기기 끊김 원인은 재현 후 확정한다. 구분선 리스트/하단 전체 삭제/전체 목록 → 사용자의 식별·탐색·끊김 관찰 → 둥근 카드/헤더 행동/필터/연속 gesture로 교체 예정. 상세: docs/work/uiux/history-card-filter-gesture.md.

## 2026-09-08 — UX-NEARBY-HANDOFF-02 / UX-HISTORY-SWIPE-01

관계: U-NEARBY-SIMPLE-01·UX-COLOR-01 및 기록 탭 개별 삭제 요구 보완. 사용자 확정·구현/검증 전. 상세 실행/반례는 docs/work/uiux/nearby-handoff-and-history-polish.md.

- 주변: 우측 검색은 흰색 `검색` 텍스트로 표시하고 폭/간격을 조정한다. 카카오 길찾기가 열렸는데 실패 문구가 뜨는 사용자 관찰을 재현한다. browser cancel을 실패로 처리하는 코드가 원인 후보이나 기기 원인은 미확정이다. 수락/취소/실패를 구분하고 실제 실패 안내는 유지한다. 외부 연결의 코스/기록/체류 부작용0.
- 기록: 통계와 `내 계정의 방문 기록` 간격 확보, 반복 설명 축소. 행 왼쪽 스와이프→휴지통→기존 삭제 확인/서비스. full swipe 삭제0, completionId 한 행 단위, 성공/실패/기기 정리 대기·계정 전환·통계 갱신 fixture와 접근성 경로 필수.
- 이력: 아이콘 검색/실패 오표시·중복 삭제 텍스트 → 사용자 가독성/오류 관찰 → 텍스트 검색/외부 결과 구분/행 삭제로 교체 → 단순화와 대상 명확화 → 구현 전. DB·추천·개인화 정책은 변경하지 않는다.

## UX-COLOR-01 — 앱 전체 텍스트 가독성 (2026-09-08)

- 관계: UX-31 보완. 사용자 확정·구현/검증 전.
- 기대 결과: 어두운 배경의 활성 버튼/텍스트 행동/탭 라벨과 파란 주요 버튼의 텍스트는 흰색 계열, 파란 강조는 아이콘/선택 배경/선/테두리로 분리한다. 보조 회색·비활성·오류 등 의미 구분은 유지한다.
- 입력/예외: 주변 상세·목록·현위치 및 다른 탭/화면의 기본·누름·선택·비활성·실패 상태. 파란색 일괄 치환으로 지도/상태 표시를 변경하지 않는다.
- 검증: 화면별 색상 역할 계약 fixture와 실제 화면 가독성 확인. 주변부터 적용하고 앱 전체 잔여 화면을 별도 체크하여 부분 적용을 전체 완료로 기록하지 않는다.
- 이력: 파란 행동 텍스트 → 짙은 배경에서 낮은 선명도 관찰 → 흰색 행동 텍스트/비텍스트 파란 강조 → 읽기 쉬운 다크 UI 유지. 이전 방식은 해당 범위 철회. 상세는 UIUX_공통규칙.md의 UX-COLOR-01을 따른다.

## 최신 확정 — DEC-LIVE-LEARNING-EVIDENCE-01

REC-34·DB-08/09/11·COURSE-13의 **보완**, 정책 확정·구현/검증 전. 회원+별도 동의의 새 적격 완료만 앱/Live Activity 확인 모두 학습한다. 비로그인/guest 이관 과거 학습0. 당시 증거 불명확·구형 및 일반 알림 액션은 제외하되 진행/완료/길찾기 유지·재탭 요구0. 최소 표본 저장 후 활성 증거를 완료/취소/만료/교체 경계에서 정리하며 기존32건7일/서버180일은 유지한다. 중복/계정전환/철회/삭제 late 재생0과 native3건→다음 추천 fixture 필수. 이전 app_action 한정→native 누락 관찰→증거 기반 공통 소비로 교체 예정. 상세 결정은 docs/work/integration-decision/release-personalization-account.md 맨 위 최신 절을 따른다.

2026-09-07 계정 후속 확정(구현 전): 닉네임은 선택·중복 허용·trim 후1~20자·줄바꿈/제어문자 금지. 회원 탈퇴는 최근10분 내 비밀번호 재인증을 서버 검증하며 일반 로그인/로그아웃에 추가 강제하지 않는다. 추가 제안 미확정→사용자 동의→확정의 이력과 기술/배포 게이트는 release-personalization-account.md 및 DB-RELEASE-IDENTITY-01 B 최신 절을 따른다. 닉네임 경계/재인증 만료·위조·token refresh 비대체 fixture를 포함한다.

DEC-RELEASE-DATA-LIFECYCLE-01 (2026-09-07, 확정·구현 전): 학습 표본180일/적격 최신5·3개 미만 기본 복귀, 체류 outbox32건7일, legacy 소유 추정 금지·승인 방문이관만 허용, 회원 방문기록 삭제/탈퇴까지 보유, reset은 표본/파생 삭제+off·일반 off는 보유기간 내 유지, 카페/문화시설 근거 기반 세부분류 우선. 보유/삭제 미확정→사용자 승인 최소 lifecycle로 교체한 이력과 남은 승인 항목은 docs/work/integration-decision/release-personalization-account.md의 최신 절을 따른다. fixture는 180일/7일/32건 경계·조회 만료·정리 실패/재시도·reset 후 pending 재생성0·계정 격리를 포함한다. 법정 수치/심사 승인 보장으로 표현하지 않는다.

DEC-GUEST-MEMBER-BENEFIT-01 (2026-09-07, 사용자 확정·구현 전): guest/anonymous/회원 미동의도 동일한 Live Activity·앱 진행 안내를 이용한다. 로그인+별도 동의의 새 적격 완료 체류만 계정 축적·다음 추천 개인화에 사용한다. guest 가져오기는 방문 이력만이며 과거 체류 학습0. 가입 강제 모달 대신 비차단 안내, 카테고리별 실제 유효 표본 수, 실제 적용된 결과만 맞춤 표시를 제공한다. 단순 로그인/3개 이력 존재를 개인화 적용으로 표시하지 않는다. 최신 결정 문서의 혜택 구분 절과 release-personalization-wave.md의 역할별 fixture를 따른다. 이전 혜택 구분 불명확→진행 안내와 학습 가치 분리→체험 보호·장기 이용 가치 명확화의 보완이며 Live Activity 로그인 제한은 채택하지 않는다.

## 2026-09-07 최신 결정 — 출시 개인화·계정 기록

[DEC-RELEASE-PERSONALIZATION-01](work/integration-decision/release-personalization-account.md)은 **사용자 확정·구현/검증 전**이다. 서버 개인화 출시 후 보류를 출시 전 제한적 적용으로 변경한다. 회원 기록은 계정 격리하고 로그아웃 시 표시/추천 적용을 중단한다. 비로그인 방문 기록은 로그인 후 명시 승인으로만 가져오며, 별도 개인화 동의와 분리하고 과거 비로그인 체류는 학습하지 않는다. 나이 신규 수집 제거·권리 미확인 사진 기본 이미지 대체도 확정됐다. 이전 방식→문제→교체 이유와 필수 반례는 연결 문서에 기록했다. 아래 과거 로컬 단계의 서버 쓰기0은 당시 구현 경계이며 새 출시 범위 보류 명령으로 재사용하지 않는다.

## DEC-LIVE-ACTION-HANDOFF-01 — 잠금화면 동작 확장 (2026-09-06)

상태: **사용자 확정·구현/검증 전**, COURSE-13·UX-19 보완. 이 절과 충돌하는 과거 출발 확인만 기록하는 흐름은 철회이며 나머지 로컬 우선 계약은 유지한다.

- 도착 확인은 잠금화면에서 한 번만 한다. native 저장과 Activity의 즉시 머무는 중 전환, 앱 복귀 시 다음 단계 복구를 연결한다. 예정 시각 전 확인도 허용한다.
- 이제 출발해요는 명시 출발과 다음 길찾기 요청을 함께 만든다. 필요한 잠금 해제 → TimeFit 활성화 → 기존 adapter로 다음 구간 카카오맵 자동 실행이며 앱에서 재탭을 요구하지 않는다. 확정 snapshot 순서의 다음 방문 장소 또는 최종 목적지를 사용한다.
- 이 명시 출발의 최초 확인 시각과 handoff 성공은 별개다. 외부 실행 실패/재시도는 departedAt을 바꾸지 않으며 성공 전 routeOpened로 표시하지 않는다. 앱의 일반 길찾기만 누른 경우는 기존대로 handoff 성공 때 출발로 기록한다.
- 잠금 해제 취소/활성화 실패/외부 전환 실패 때 성공을 합성하지 않는다. 요청 중복 소비·stale run/stop/revision·완료 코스 재실행을 차단한다. 외부 앱 실행과 로컬 저장은 원자적이지 않으므로 중단으로 성공 여부가 불명확하면 자동 반복 대신 앱에서 명시 재시도를 제공한다.
- 로컬 단계의 서버 표본/업로드 큐/개인화 적용0, GPS/원격 push 미사용, exact cleanup을 유지한다. 알림 발화 정책 변경은 이번 범위가 아니다.

이력: 출발 기록 후 앱 길찾기 재탭 → 사용자 중복 조작 및 잠금화면 도착 무반응 관찰 → 도착 동기화 복구와 명시 출발/단일 길찾기 요청 연결 → 잠금 해제 이후 추가 조작 감소. 테스트는 cold/warm start, 잠금 해제 취소, 중복 이벤트, 외부 실패/불명확 결과, 실제 1/2곳 순서와 cleanup을 포함한다. 상세 실행 기준: docs/work/uiux/live-activity-dwell-progress.md의 “잠금화면 도착 복구·출발 자동 길찾기” 명령.


> 기준일: 2026-09-04 KST
> 목적: 사용자가 확정한 동작 요구사항을 단일 목록으로 관리하고, 구현 여부가 아니라 **검증 근거** 기준으로 테스트 충족률을 갱신한다.

## 1. 갱신 규칙

- 사용자가 새 요구사항이나 이상 사례를 말하면 먼저 이 문서에 ID와 기대 결과를 추가한다.
- 새 요구사항을 등록하기 전, 기존 ID와 비교해 관계를 `동일`, `보완`, `충돌`로 판정한다.
  - `동일`: 새 항목을 만들지 않고 기존 ID의 근거·테스트 시나리오만 갱신한다.
  - `보완`: 기존 ID를 유지하고 세부 조건을 추가한다. 기존 결과에 영향을 주면 변경 내용을 함께 기록한다.
  - `충돌`: 기존 규칙을 임의로 덮어쓰지 않는다. 사용자에게 기존 유지·새 규칙으로 대체·두 규칙 병행 중 선택을 물은 뒤, 확정된 방향만 반영한다.
- 대체된 요구사항은 삭제하지 않고 `대체됨` 상태와 대체한 ID·결정일을 남긴다. 현재 적용되는 요구사항만 충족률에 포함한다.
- 구현 전에는 `미검증`, 테스트 코드가 실패를 재현하면 `부분`, 자동 테스트 또는 재현 가능한 수동 시나리오가 통과하면 상태를 갱신한다.
- 추천·데이터·DB 정책을 변경한 커밋에는 관련 ID, 실행 명령, 결과를 함께 기록한다.
- UIUX-35는 기존 UXV-18의 `시간 예산 우선 표시` 요구를 **보완**한다. 결과·상세·확인 화면의 시간 값은 별도 계산하지 않고 공통 시간 여정 모델에서 파생한다.
- DATA-18은 2026-08-23 중앙값 권장 정책을 **대체**한다. 권장 체류는 활동 대응 AI-Hub 카테고리의 최빈값 30분이며, 최소 20분·최대 60분(문화시설 120분)은 자투리 활동 제품 정책으로 둔다.
- 퍼센트는 코드 라인 수가 아니라 **요구사항 수** 기준이다. 수동 확인은 기능 확인이지 자동 회귀 방지는 아니므로 자동화율과 분리한다.

| 상태 | 의미 |
|---|---|
| `자동` | CI/로컬 명령으로 반복 검증 가능 |
| `수동확인` | 시뮬레이터 또는 실기기에서 사용자가 확인 |
| `부분` | 구현은 있으나 핵심 분기·실기기·회귀 검증이 부족 |
| `미검증` | 요구사항은 확정됐지만 검증 근거가 없음 |
| `대체됨` | 이후 확정된 요구사항으로 더 이상 적용하지 않음. 충족률 계산에서 제외 |

### 요구사항 등록 형식

```text
ID: REC-XX
관계: 신규 | 동일(REC-XX) | 보완(REC-XX) | 충돌(REC-XX)
기대 결과: 사용자가 확인 가능한 한 문장
검증 방식: 단위 | 통합 | E2E | 수동
결정: 유지 | 대체 | 병행 (충돌일 때 사용자 확정 후 기록)
```

## 2. 현재 요약

| 지표 | 요구사항 수 | 비율 |
|---|---:|---:|
| 자동 검증 완료 | 이전 집계 16 / 75 | **21%** |
| 수동 확인 완료 | 이전 집계 16 / 75 | **21%** |
| 확인 완료 합계 | 이전 집계 32 / 75 | **43%** |
| 부분 검증 | 이전 집계 26 / 75 | 35% |
| 미검증 | 이전 집계 17 / 75 | 23% |

위 수치는 이번에 추가한 `REC-24`, `UX-24` 및 대체 처리한 항목을 반영하기 전 **이전 집계**다. 현행 요구사항의 상태별 재집계는 다음 QA 세션에서 수행한다. 기능 구현률이 아니라 자동 또는 수동으로 기대 결과를 확인한 비율이며, 공모전 제출 전 목표는 `확인 완료 90% 이상`, 그중 핵심 흐름 자동화 `50% 이상`이다. 후보 지도와 상세 시트의 세부 UIUX 요구사항은 [UIUX 테스트 명세](03_product/UIUX_테스트명세.md)에서 별도로 관리한다.

## 3. 요구사항 목록

### A. 추천 엔진과 시간 안전성 (REC-01~34, 대체 ID 포함)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| REC-01 | 출시 입력 종료 시각은 현재 시각보다 뒤이며 최대 120분 이내여야 한다 | 구현 전환 필요 | 이전 180분은 자동 1~3곳 조립 범위였으나 one-stop 출시와 명시 pair-only 전환 뒤 과도한 잔여 시간을 만들 수 있어 `REC-30`의 120분으로 교체했다. 1분·120분·121분·자정 경계를 고정 시각으로 검증한다. 상태: **180분 철회·120분 현행**. |
| REC-02 | 지도 후보는 도보·대중교통·차량 중 하나 이상의 진입 수단으로 최소 체류·안전 여유를 충족할 때만 표시한다 | 대체됨 | 2026-08-21 `REC-17`, `REC-19`로 대체. 차량 OR·다중 장소 기준은 새 단일 경유지 정책과 충돌한다. |
| REC-03 | 후보는 출발지 근처·도착지 근처·실제 이동 경로 주변에서 함께 수집하고, 세 범위 밖 장소는 후보로 넣지 않는다 | 부분 | 철회 이력: 직선 생활권·중간 표본 방식은 서면→사상에서 부산역 후보가 섞일 수 있어 실제 경로 기반 방식으로 교체했다. 추천 실행 시 실제 경로 범위 안 최대 8km 공간 후보를 한 번 준비하고, 지도는 기본 1km만 표시하며 슬라이더로 이미 준비된 후보의 표시 범위만 확장한다. 실제 경로 polyline fixture 5건, 기준 경로 캐시·부분/전체 실패 fixture 6건, 지도 슬라이더 API 미호출 UI 계약을 추가했다. 실제 API E2E가 남아 `부분`이다. [전환 기록](archive/product-history/추천_실경로탐색전환계획.md) 참조. |
| REC-04 | 약속이 가까워도 시간 안의 후보를 우회율 때문에 제거하지 않는다 | 자동 | `planner-policy.test.mjs` 및 경성대 사례 호출 재현 완료 |
| REC-05 | 후보 상태는 최소 체류·수단별 안전 여유를 하드 조건으로 하고, 추가 여유 10분으로 여유·짧게 가능·빠듯을 구분한다 | 부분 | 관계: 보완. 최소 체류 통과 테스트는 있으나 10분 상태 경계 자동 테스트가 필요하다. |
| REC-06 | 시설형·유료 성격 장소는 추가 이동보다 체류가 짧으면 제외한다 | 부분 | 정책 구현. 카페·식당 경계 테스트 필요 |
| REC-07 | 자연·거리·광장 성격 장소는 짧은 접근 시 15~20분 체류를 허용한다 | 부분 | 정책 구현. 거리별 경계 테스트 필요 |
| REC-08 | 이동 효율은 하드 제외가 아니라 전체 자투리 예산 대비 추가 이동 비중으로 순위에만 반영한다 | 자동 | `routeMovementOverhead()` 및 정책 테스트 |
| REC-09 | 후보 목록은 근사 이동시간, 코스 확정은 TMAP·ODsay 실제 이동시간으로 재검증한다 | 대체됨 | 2026-08-23 `REC-21`로 대체. 대표 추천·새 추천·상세는 실제 경로값만 표시하며, 근사값은 내부 후보 선별·순위에만 사용한다. |
| REC-10 | 3곳 이상도 시간 안이면 장바구니에 담을 수 있다 | 대체됨 | 2026-08-23 `REC-17`로 대체. 자유 장바구니 대신 엔진이 최대 3곳 대표 코스를 조립한다. |
| REC-11 | 장소 순서를 자동 정렬하고 사용자가 바꾸면 시간 적합성을 다시 계산한다 | 대체됨 | 2026-08-23 `REC-17`로 대체. 1~3곳 순서는 엔진이 조립하며 사용자가 직접 편집하지 않는다. |
| REC-12 | 같은 `siteGroupId`의 포괄 장소와 내부 장소는 함께 담을 수 없다 | 자동 | 데이터 계약 테스트 및 엔진 조합 규칙 |
| REC-13 | GPS로 받은 부산 좌표도 장소 검색 좌표와 동일하게 출발지로 사용해 후보를 생성한다 | 수동확인 | 관계: 보완(REC-03). 시뮬레이터 GPS를 스타벅스 경성대점으로 설정한 뒤 180분 도보 추천에서 후보 31개·운영시간 통과 7개·코스 10개 확인. 자동 UI 테스트 필요 |
| REC-14 | 지도 기본 반경에서 보이는 장소는 공간·근사 시간뿐 아니라 현재 운영 가능 여부를 통과해야 하며, 코스 확인 단계는 실제 경로·운영시간으로 최종 재검증한다 | 부분 | 관계: 보완(REC-03, REC-09, DATA-06). 2026-08-17 발견: 지도 탐색 모드가 운영시간 게이트보다 먼저 반환되어, 지도 후보 수가 기존 코스 수보다 눈에 띄게 늘었다. 로컬 운영시간 게이트를 추가했다. 시설형 운영시간 경계, 미확인 카페·식당·시설 제외, 야외 자연관광 허용, 시간대 미확인 시장·거리 제외 단위 테스트 4건 통과. 서면 왕복 120분 기준 기본 1km 지도 후보는 36개에서 24개로 줄었다. 실제 API 확정 차단과의 일관성 E2E가 남아 `부분`이다. |
| REC-15 | 장소 상세의 이동·체류·남는 시간은 같은 전체 코스 시간 예산으로 표시되며 세 값의 합은 입력 시간과 일치한다 | 자동 | 관계: 보완(UIUX 상세 시트). 2026-08-17 발견: 기존 화면은 새 장소의 접근·다음 구간 이동만 보여주면서, 남는 시간은 이미 담긴 장소를 포함한 전체 코스로 계산해 기준이 섞였다. `총 이동`, `코스 체류`, `남는 시간`으로 통일했고 `candidate-evaluation.test.ts`가 다중 장소 시나리오의 합산 일치와 실제 이동 합계를 검증한다. |
| REC-17 | 추천의 기본 단위는 엔진이 조립한 1~3곳 코스이며, `출발 → 1~3개 장소 → 도착지/복귀` 전체 이동·**권장 체류**·도착 여유가 시간 예산 안이어야 한다. 결과는 상단 대표 1개와 검증 대안 목록으로 구성한다. | 대체됨 | 2026-08-30 `REC-27`로 대체. 코스 단위·대표/대안 구조는 유지하되, 권장 체류를 전 코스의 하드 조건으로 둔 규칙을 철회했다. |
| REC-18 | 사용자가 약속 전 남길 도착 여유를 슬라이더로 설정하며 기본값은 10분, 0분은 허용하지 않는다 | 부분 | 관계: 신규(REC-01, REC-05 보완). 출시 UI에 5~30분 슬라이더와 기본 10분을 적용했다. 고정 시각 UI/E2E 경계 검증이 남았다. |
| REC-19 | 차량은 추천 후보 통과·공간 탐색·순위에 사용하지 않고, 도보·대중교통 조합만 추천 판정에 사용한다 | 부분 | 관계: 대체(REC-02, REC-03). 단일 장소 추천 모듈은 차량을 타입 수준에서 받지 않으며 차량 전용 후보 제외 fixture가 통과한다. 실제 경로 범위 서비스의 차량 baseline 제거는 후속 정리다. |
| REC-20 | 대표·대안 코스는 권장 체류 합계를 충족해야 하며, 남는 시간은 시간 여정에 그대로 표시한다. 최소 체류만 가능한 코스를 `짧게 가능`으로 대표·대안에 노출하거나, 남는 시간을 채우려고 체류·장소 수를 늘리지 않는다. | 대체됨 | 2026-08-30 `REC-27`로 대체. 최소 체류만 가능한 검증 코스도 숨기지 않되, 같은 장소를 체류시간 변형으로 중복하거나 남는 시간을 채우려 체류를 늘리는 규칙은 계속 금지한다. |
| REC-21 | 대표 추천·검증 대안 목록·사용자가 상세로 연 장소는 후보 단계와 선택 단계에서 동일한 실제 도보·대중교통 구간 시간으로 이동·**엔진 선택 체류**·도착 여유·남는 시간을 표시한다. 같은 좌표쌍·수단의 재조회는 TTL 캐시를 재사용한다. | 구현 전환 필요 | 관계: 보완(REC-27), 대체(REC-09의 근사 후보 표시). 코스 V1은 사전선정 뒤 제한된 후보만 정확 경로로 검증한다. 선택 체류와 `추천/짧게 가능` 상태가 목록·상세·저장에 같은 스냅샷으로 전달되는 fixture가 필요하다. |
| REC-22 | 기본 지도는 실제 경로·출발/도착 생활권의 좁은 활동 범위 안에서 `추천` 또는 `짧게 가능`인 후보만 보인다. 먼 후보와 불가능 후보는 사용자가 명시적으로 범위를 넓히는 탐색에서만 보여 준다. | 부분 | 관계: 대체(REC-03의 8km 선조회·슬라이더 표시 확장). 초기 최대 8km 후보 풀은 유지하되 기본 표시는 1km, 사용자 슬라이더의 `2·3·5·8km`에서만 확장한다. `one-stop-search-scope.test.ts`가 기본/확장 후보 경계를 고정 fixture로 검증한다. 지도 실기기 상호작용과 가까운·왕복·먼 약속 E2E가 남았다. |
| REC-23 | 운영시간 미확인 조건부 후보는 대표 코스·자동 확정에서 제외하고 더보기에만 `운영시간 확인 필요`로 표시한다. 카카오맵 확인 뒤 사용자의 명시 확인이 있으면 조건부 수동 포함 코스에만 넣을 수 있으며, `추천/검증됨`으로 승격하지 않는다. | 대체됨 | 2026-08-31 `REC-28`, `DATA-25`로 결과 역할을 세분화했다. **접근 시간 근거가 없는** 조건부 후보의 대표 제외·조건부 상태 유지 원칙은 그대로다. |
| REC-24 | 시간·운영시간·실제 경로 하드 조건과 기본 적합성이 같은 후보 사이에서만, 앱의 완료 기록에 없는 지역·활동을 보조 순위에 쓸 수 있다. GPS 방문·소비 이력이나 근거 없는 첫 방문·인기도는 추정하지 않는다. | 미착수 | 관계: 신규(제품 발견성 보완). 완료 기록 있음/없음·동점 후보 fixture에서 완료 이력 기반 순위만 달라지고, 시간 적합성·운영시간 통과 여부는 바뀌지 않는지 검증한다. |
| REC-25 | 대표·대안은 표시 전 실제 경로를 검증한다. 서버 Route Proxy는 공개 장소 간 방향·수단별 구간 캐시와 Kakao 수단별 일일 호출량을 관리하며, 정밀 GPS·사용자 ID·IP를 장기 경로 캐시나 호출량 집계에 저장하지 않는다. | 구현 전환 필요 | 관계: 보완(REC-21). 2026-08-28 결정: 대중교통·도보는 Kakao REST 단일 제공사이며, 동일 구간 cache hit를 우선한다. Kakao 실패·한도 초과는 TMAP·ODsay·근사시간으로 우회하지 않고 검증 불가로 반환한다. ODsay Basic(30/일)과 TMAP은 자동 추천 런타임에서 제외한다. mode·방향·공개 POI identity·route version cache, 도보/대중교통의 독립 soft/hard 일일 한도, 개인정보 비영속, provider fallback 0회를 fixture로 검증해야 한다. 자동 대표 ID·좌표·분류 변경 때는 mobile snapshot과 Edge `ROUTE_PROXY_CATALOG_SNAPSHOT_JSON`을 같은 version으로 배포하고, stale public version은 receipt를 가진 안전 `rejected`로 구분하며 provider 호출 0임을 fixture로 검증한다. |
| REC-26 | 첫 추천은 새 Kakao provider attempt 최대 8회로 대표 포함 검증 코스 4개를 목표로 한다. 8회 뒤 4개 미만이면서 사전 선별한 미검증 코스가 남은 경우에만 전체 최대 16회까지 보충한다. `다른 검증 코스 더 보기` 한 번은 같은 세션의 다음 후보만 최대 8회 검증해 새 코스 최대 3개를 추가한다. cache hit·세션 동일 구간 재사용은 새 attempt로 세지 않으며, 후보 큐 소진·전역 한도·provider 실패 때는 목표를 채우려 호출하지 않는다. | 배포 후 보류 | 관계: 대체(2026-08-29의 고정 8회 REC-26), 보완(REC-21, REC-25, REC-28). 이 행의 1~3곳·첫 8→16회 규칙은 계속 보류한다. one-stop 첫 8회와 사용자 요청당 8회는 별도 `REC-31`이 현행 기준이다. |
| REC-27 | `출발 → 1~3개 장소 → 도착지/복귀`의 실제 도보·대중교통, 구조화 운영시간, 도착 여유를 통과하면서 각 장소에 최소 체류 이상을 배정할 수 있으면 대표·대안 후보가 된다. 권장 30분 충족은 우선순위·`추천` 상태이고, 최소 20분만 가능한 실제 경로 코스는 `짧게 가능` 상태로 함께 반환한다. 한 장소·같은 순서는 체류시간 변형으로 중복하지 않으며, 최대 60/120분이나 남는 시간만으로 체류를 자동 연장하지 않는다. | 배포 후 mixed 다장소 보류 | 관계: 보완(REC-17, REC-20, REC-21). 1곳의 최소/권장 체류·운영시간·실제 경로 규칙은 `REC-30` 출시 entry에 그대로 적용한다. mixed 2·3곳 자동 조립은 보류하며, 사용자가 검증 A를 명시 선택하는 pair-only 2곳은 별도 `REC-32`로만 재개한다. |
| REC-28 | 결과는 실제 경로·운영시간·최소 체류·도착 여유를 통과한 상단 대표 코스 1개와 첫 대안 최대 3개로 구성한다. `다른 검증 코스 더 보기`는 `continueLimitedRepresentativeCourseV1({ ...originalInput, continuation })`에 첫 계산과 같은 입력·provider·route ports를 다시 전달하고, continuation의 cursor·후보 ID/signature·검증/탈락 signature·opaque receipt key로 다음 후보만 로컬 재수화한다. 새로 검증된 코스만 기존 목록 뒤에 최대 3개 추가한다. continuation에 좌표·사용자 ID·함수/provider 객체를 넣거나 서버에 세션/큐를 저장하지 않는다. 재수화 불일치는 route 호출 없이 `continuation_unavailable`으로 종료한다. 이미 보인 동일 코스·순서만 다른 동일 집합·탈락 코스는 재검증하지 않으며, 화면의 총 대안 수에는 고정 상한을 두지 않는다. | 배포 후 다장소 보류 | 관계: 대체(2026-08-31의 무경로 탐색 목록 REC-28), 보완(REC-21, REC-26). 이 행의 `continueLimitedRepresentativeCourseV1`와 다장소 queue는 계속 보류한다. `REC-31`은 같은 안전 원칙을 별도 one-stop entry로 재사용하며 2·3곳을 재활성화하지 않는다. |
| REC-29 | 구조화 운영시간이 없는 시장·거리·골목은 검증 코스가 아니라 별도 `운영시간 확인 후 들러볼 곳`에만 표시한다. 데이터가 권역형 조건부 후보로 식별하고 시설·개별 점포·도매/새벽 전용·장기 이용 장소를 제외했을 때만 **기기의 실제 현재 시각** `10:00 ≤ t < 18:00`에 노출한다(09:59 숨김, 10:00·17:59 노출, 18:00 숨김). 18:00 경계를 넘거나 foreground 복귀 시 이미 보인 조건부 카드·더 보기도 숨기며, 개발용 추천 시작 시각은 이 창을 연장하지 않는다. 출발/도착 중 가까운 쪽과의 직선거리로 가까운 8개를 먼저 보이고 로컬 `더 보기`로 다음 8개를 기존 뒤에 추가하며, 이 탐색은 route/API 호출 0이다. 카카오맵 확인 뒤 사용자가 명시적으로 계산을 요청한 경우에는 실제 **확인 시각도** `10:00 ≤ t < 18:00`일 때만, 최초 추천 뒤 경과 시간을 차감한 입력으로 실제 경로를 계산한다. 이후 시각은 route 0·재추천 안내이며 성공해도 `운영시간 확인 필요(사용자 확인)` 상태를 유지한다. | 구현 전 | 관계: 보완(REC-23, DATA-25). 실제 표시 시각과 추천 세션 시각을 분리한 09:59/10:00/17:59/18:00·18:00 경계 경과·foreground 복귀, 경과 시간 차감, 8개 페이지·거리순·중복 없음·더 보기 0 route, 제외 분류, 외부 지도 열기 0 route call, 명시 확인 전 route call 0, 확인 뒤 조건부 상태 유지 fixture를 검증한다. |
| REC-30 | 공모전 출시 게이트에서 현재 시각부터 최대 120분 안의 자동 추천은 실제 경로·운영시간·최소 20분 체류·도착 여유를 통과한 **1곳**만 계산·표시하며 2·3곳 public segment 요청은 시작하지 않는다. 결과 카드는 사진·장소·활동 카테고리와 도착 여유를 제외한 `이동+선택 체류` 합계만 `약 N분 코스`로 표시한다. 코스 확인 상세는 실제 이동과 선택 체류를 세로 순서로 보여 주며 체류는 `둘러보기 약 N분`, short는 `가볍게 둘러보기 약 N분`으로만 표현한다. 최소·최대·범위·조절기와 진행 화면 체류 분은 노출하지 않는다. | 부분 | 관계: 2-U·U-RELEASE-ONESTOP-01·U-COURSE-CARD-DETAIL-01 구현과 카드 상세 fixture 7/7을 수락했다. 추천 선택 폭 확장은 별도 REC-31이며, 출시 후보의 실제 상세·길찾기 단일 smoke가 남아 부분 상태를 유지한다. |
| REC-31 | 출시 one-stop 첫 결과는 신규 provider attempt 최대 8회로 대표 1개와 첫 대안 최대 3개를 반환한다. 미검증 single 후보가 남을 때만 `다른 장소 더 보기`를 표시하며, 한 번 누를 때 다음 후보를 신규 attempt 최대 8회로 검증해 새 코스 최대 3개를 기존 목록 뒤에 누적한다. 대표·기존 순서와 카드는 유지하고 전체 표시 수에는 고정 상한을 두지 않는다. 첫 결과가 0건이어도 후보가 남으면 최종 실패로 닫지 않고 명시 더보기를 제공한다. cache/session reuse는 새 attempt가 아니며, 중복·기탈락 후보 재검증, 자동 연속 페이지, 이 one-stop entry 내부의 2·3곳 자동 조립, 조건부 자동 승격은 금지한다. | 자동 확인 | 관계: 보완(REC-30), one-stop 범위에서만 REC-26/28의 요청형 검증 원칙을 재사용한다. `2-V`, `U-ONE-MORE-01`, `QA-ONE-MORE-01`을 수락했다. 밀집 fixture에서 고유 장소 `4→7→10`, initial 0건 뒤 `0→3`, 호출 상한과 금지 경계를 확인했다. `REC-32`의 pair-only branch는 이 entry를 바꾸지 않고 검증 A를 받은 뒤 별도 시작한다. native 체감은 전체 UIUX 정리 뒤 출시 후보 수동 smoke 1회에 합친다. |
| REC-32 | Results의 실제 표시 one-stop 카드 tap은 선택이 아니라 전체 지도형 장소 상세를 연다. 상세의 명시 `선택하기`로 A를 확정한 경우에만 최대 2곳 제한 조립을 시작한다. 각 B는 `O→A→B→D`와 `O→B→A→D`의 세 exact leg·두 운영시간·각 최소 20분·도착 여유를 모두 비교하며, 유효한 최소 이동 순서(동률 A 우선)만 최종 방문 순서로 사용한다. 기본 성공 목표 3·부족 허용·누적 최대 6이고 session 신규 route attempt 최대 36을 유지한다. 상세 열기·닫기는 route 0, A 취소는 선택 전 one-stop 목록·순서·더보기·스크롤을 route 0회로 복원한다. | 엔진 자동 확인 / 상세·선택 UI 구현 전 | 관계: `2-Y`·`2-Z`·API 예산과 양방향 exact 최적화는 수락 상태를 유지한다. `DEC-PLACE-COURSE-FLOW-01`이 U-TWO-STOP-03의 카드 tap 즉시 선택만 철회하고 상세 후 명시 선택으로 교체한다. 기존 Results/ScrollView·seed·복원·36회 경계는 재사용한다. |
| REC-33 | 같은 recommendation session·입력에서 exact 검증된 두 장소 집합 `{A,B}`는 첫 선택 장소가 바뀌어도 재사용한다. A에서 B 확인 후 취소하고 B를 선택하면 A를 우선 후보로 신규 adapter/provider attempt 0회에 표시하며, 실제 방문 순서·legs·시간·운영 상태는 기존 exact snapshot을 유지한다. 현재 branch의 B 목표 3·누적 6에는 한 후보로 세고 나머지는 남은 예산으로만 확인한다. B 후보 시간은 pair 전체 합계가 아니라 A one-stop 대비 이동+체류 추가분을 표시하고, 비양수·snapshot 불일치에는 전체 시간임을 명시한다. 다른 session/input/signature, abort·stale·terminal·미검증 pair는 재사용하지 않는다. | 자동 통합 확인 / 실기기 체감 1회 남음 | 관계: REC-32 보완. `2-AA`·`U-TWO-STOP-04`·`QA-TWO-STOP-02`에서 동일 세션 역선택 0-call, 새 세션·입력/provider signature 변경 비재사용, terminal partial 비저장, attempt-limit exact partial 유지, A 51분·pair 88분의 `37분 추가`, 자동 예산 16 소진, initial 3·누적 6, 손상 seed·abort를 확인했다. 독립 결과는 전용 17/17·엔진 35/35·집중 UI 37/37·전체 UI 242 통과(기존 skip 1)·core 117/117·typecheck·diff check 통과다. 새 세션의 모든 미검증 pair를 첫 목록에 보장하지 않으며 작은 iPhone UI 체감만 출시 후보 smoke에 남긴다. |
| REC-34 | 일반 로그인 사용자가 별도 체류 기록 동의 후 같은 `(category, subCategory)`에서 명시 도착·출발 완료 표본을 3개 이상 만든 경우에만 개인화 체류를 적용한다. 새 표본마다 최근 최대 5개 중앙값을 5분 단위로 반올림하고, 기본 권장값 대비 ±10분과 장소 min/max로 제한한다. 개인화는 기존 체류 배정기의 soft recommended override와 동률 보조 순위에만 쓴다. 목표가 1곳/2곳의 운영시간·총예산에 맞지 않으면 feasible clamp 또는 기존 비개인화 선택 체류로 복귀하며, 실제 경로·운영시간·최소 체류·도착 여유를 완화하거나 원래 가능한 후보를 제거하거나 route를 추가 호출하지 않는다. 같은 subCategory 문자열이어도 상위 category가 다르면 분리하며, missing subCategory, anonymous Auth, 미동의, 미완료 표본은 계산에서 제외한다. | 구현 전 | 관계: 신규(DEC-LIVE-DWELL-01). 1·2·3·5·6개, 이상값, 복합 키 격리, invalid 표본, one-stop/pair 목표 초과 fallback·결과 집합 불변과 API/DB 호출 0 순수 fixture를 `2-AB`에서 검증한다. |

### B. 장소 데이터·운영시간 신뢰도 (DATA-01~24 및 대체 REC-16)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| DATA-01 | 추천 카테고리는 자연관광지·상업지구·레저/스포츠·문화시설·카페만 사용한다 | 자동 | 일반 식당은 자투리 활동 카탈로그에서 제외한다. `recommendation-data-contract.test.mjs` |
| DATA-02 | 숙박·교통·의료·주차 시설은 추천 데이터에서 제외한다 | 자동 | 데이터 계약 테스트 |
| DATA-03 | 이름 차이 중복 장소는 대표 장소로 병합하고, 병합 근거는 검토 원천에 남긴다 | 자동 | 데이터 계약 테스트 |
| DATA-04 | 포괄 장소는 대표 권역으로 사용하고 내부 개별 장소는 런타임에서 제외한다 | 자동 | 데이터 계약 테스트 |
| DATA-05 | 카카오에서 상세 확인되지 않은 장소도 활동 근거가 있으면 조건부 후보로 남기되, 카카오 검색 링크와 운영시간 상태를 제공한다 | 자동 | 데이터 계약 테스트 |
| DATA-06 | 시설형·권역형 장소는 신뢰할 수 있는 운영시간 근거가 없으면 자동 추천하지 않는다 | 대체됨 | 2026-08-31 `DATA-25`로 세분화했다. 시설형은 계속 구조화 운영시간이 필수이고, 권역형은 전체 점포 운영시간이 아닌 검토된 **권역 접근 근거**가 있을 때만 탐색 후보가 될 수 있다. |
| DATA-07 | 새벽에는 검토된 야외 상시 접근 장소 외 시설·상권을 추천하지 않는다 | 자동 | 자연관광 성격 게이트 테스트 |
| DATA-08 | 카카오 보조시설 결과는 `weak`으로 보존하되, 상세 링크 대신 안전한 장소 검색 링크를 제공한다 | 자동 | 데이터 계약 테스트 |
| DATA-09 | 자동 추천 장소는 일반 관광·업종 카테고리가 아니라 자투리 활동 유형과 활동 완결성 검토를 통과한 장소만 사용한다 | 자동 | 관계: 신규(REC-01, DATA-01 보완). 런타임은 `approved 49 + conditional 307 = 356개`의 `scenic_pause / quick_browse / compact_culture / quick_rest` 활동 카탈로그만 사용한다. 일반 식당·장기 활동·내부 개별 시설은 제외 또는 보류 기록으로 남긴다. `busan-poi-catalog.test.mjs`와 `short-stay-catalog.test.mjs`가 검증한다. |
| DATA-10 | 승인·조건부 장소는 최소·권장·최대 체류 범위와 그 산출 근거를 가지며 `min <= recommended <= max`를 만족한다 | 자동 | 관계: 대체(단일 `dwell`). 직접 AIHub 분포는 권장 범위 근거로, 활동 유형·수동 검토는 최소/최대 활동 상한 근거로 분리한다. 런타임 `shortStay` 정책과 단일 장소 추천 단위 테스트가 우선 사용을 검증한다. |
| DATA-11 | 상권 데이터는 검토 후보 발굴에만 사용하며, 상호·업종·좌표만으로 카페·베이커리·서점·소품샵을 자동 추천하지 않는다 | 부분 충족 | 관계: 신규(DATA-06 보완). 런타임에는 검토 카탈로그에서 활동 유형을 받은 장소만 들어간다. 카페·베이커리 등 `quick_rest`는 운영시간·활동 완결성 근거가 있는 승인 또는 조건부 후보이며, 운영시간 미확인 상태를 추천 통과로 표현하지 않는다. 개별 장소 수동 검토는 계속 필요하다. |
| DATA-12 | 명시된 시장·거리·골목·마을·공원·해변의 내부 장소는 대표 권역을 추천하고, 내부 후보는 대표 장소 ID와 함께 제외 또는 보류한다 | 자동 | 관계: 보완(DATA-04, DATA-09). 활성 런타임 원천에 `scope.type = 내부장소`가 남지 않는지 데이터 계약으로 검사한다. 좌표 근접만으로 관계를 추정하지 않으며, 대표 권역이 없는 내부 후보는 보류·제외 목록으로 추적한다. |
| DATA-13 | 카페·베이커리·서점·소품샵은 대기시간을 추정하지 않고, 파싱 가능한 공식 운영시간과 활동 유형을 통과할 때만 초기 체류 템플릿으로 승인한다 | 부분 충족 | 관계: DATA-11 보완. `행사별/프로그램별/매장별 상이`는 운영시간 근거가 아니며, 대기 없음이라는 표현도 금지한다. 활동 범위 카탈로그는 런타임에 적용했지만, 조건부 후보의 운영시간 미확인 UI·선택 뒤 실제 확인 E2E는 남았다. |
| DATA-14 | 도시공원·공공개방시설 표준데이터는 장소 수 확대만을 위해 런타임 후보에 넣지 않는다 | 충족 | 관계: DATA-09 보완. 큰 공원은 기존 TourAPI·부산 공식 원천을 사용하고, 도시공원은 운영시간·사진·활동 완결성 부족으로 추천 원천에서 제외한다. 공공개방시설은 대표 권역 확인 외 사용하지 않는다. |
| DATA-15 | 전통시장 표준데이터는 내부 점포가 아닌 대표 시장만 추가하며, 기존 장소와 정확한 이름·300m 이내 좌표가 중복될 때만 병합한다 | 부분 충족 | 관계: DATA-12 보완. 추가 시장은 대표 시장 단위와 활동 유형을 충족한 조건부 후보만 런타임에 넣고, 음식 중심 시장 권역은 제외한다. 운영시간 미확인 시장은 권역 시간대 게이트와 카카오맵 확인 안내를 따른다. |
| DATA-16 | 장소 부족 시 활동 유형·좌표·출처가 있는 보류 후보는 `conditional`로 지도 탐색에 포함하되, 운영시간 미확인은 명시와 카카오맵 확인 링크를 제공한다 | 부분 충족 | 관계: DATA-09, DATA-13 보완. 유형 없는 보류와 하드 제외는 승격하지 않는다. 307개 조건부 후보를 런타임에 연결했고, 데이터 계약은 카카오 URL과 상태를 검증한다. 후보 상세의 `운영시간 확인 필요` 표현과 확정 차단 E2E는 남았다. |
| DATA-17 | 운영시간 미확인 시설·카페·권역 후보는 삭제하지 않되 더보기에만 `운영시간 확인 필요`로 구분하고, 시간 내 이용을 보장하거나 대표 추천·자동 확정으로 표현하지 않는다 | 미검증 | 사용자 확인 뒤에도 공식 근거·추천 상태로 승격하지 않고 조건부 상태를 저장한 채 경로·시간만 재계산한다. |
| DATA-18 | 모든 활성 장소는 활동 대응 AI-Hub 카테고리의 최빈 체류시간 `30분`을 권장 체류로 사용한다. 최소는 `20분`, 최대는 문화시설 `120분`·나머지 활동 `60분`으로 통일한다. | 자동 | 관계: 대체(2026-08-23 장소별·카테고리 중앙값 정책). AI-Hub는 30분 단위 기록이며 현행 다섯 활동 대응 카테고리의 최빈값은 모두 30분이다. 20·60·120분은 원본 관측값으로 가장하지 않는 자투리 활동 정책이다. `short-stay-catalog.test.mjs`, `recommendation-data-contract.test.mjs`가 근거 출처와 범위를 검증한다. |
| DATA-19 | 활성 또는 재검토 장소는 `identity`, `placeKind`, `activityEvidence`, `stayEvidence`, `availability`, `accessFriction`, 필드별 원천·확인일을 가진다. | 미착수 | 관계: 신규(기존 approved/conditional 재분류). 누락 필드, 출처 없는 값, 허용하지 않은 enum을 카탈로그 계약 테스트로 차단한다. |
| DATA-25 | 시장·거리·골목·해변·공원 등 권역형 후보는 `representative` / `area_access` / `conditional_visit` 자격을 분리한다. `area_access`는 공식·검토된 권역 접근 시간 또는 공개 야외 접근 근거를 가진 자동 탐색 자격이다. `conditional_visit`는 구조화 운영시간이 없더라도 시장·거리·골목으로 식별되고 시설·개별 점포·도매/새벽 전용·장기 이용 장소를 제외한 경우에만 제품 탐색 창과 사용자 확인 필요 상태를 가진다. 조건부 탐색은 출발/도착 중 가까운 쪽 기준 직선거리 8개 페이지로만 로컬 정렬하며 route/API 호출을 만들지 않는다. 내부 점포·시설 운영시간을 상속하거나 카카오맵 표시 시간을 수집·추정하지 않는다. | 구현 전 | 관계: 대체(DATA-06, 보완(DATA-12, DATA-15~17, REC-29). 시장·거리·야외·시설·근거 없음 fixture로 자격·대표 제외·노출 창·8개 거리 페이지·명시 확인 뒤 상태를 검증한다. |
| DATA-20 | 장소는 `representative_core`, `representative_standard`, `conditional_more`, `hold` 중 하나로만 분류하며, 분류 이유와 누락 근거를 보존한다. | 미착수 | 대표/조건부/보류 fixture를 고정해 분류가 재현되는지 검증한다. |
| DATA-21 | `representative_core`는 장소·권역 단위 활동·체류 근거와 확인된 이용 가능성을, `representative_standard`는 확인된 장소·운영 가능성과 검토된 활동 템플릿 근거를 가져야 한다. | 미착수 | core/standard 경계 fixture와 실제 경로 통과 fixture를 검증한다. |
| DATA-22 | `conditional_more`는 장소 동일성·활동 적합성은 있으나 운영 가능성이 미확인인 경우에만 쓴다. 장소 동일성·활동·체류 근거가 없는 장소는 `hold`다. | 미착수 | 운영시간 미확인, 활동 근거 미확인, 장소 중복 fixture의 분류와 노출 경계를 검증한다. |
| DATA-23 | 권역 장소는 대표 권역의 공식 이용 가능 시간·활동 근거만 사용하며 내부 상점·시설의 운영시간·체류 근거를 상속하지 않는다. 같은 `siteGroupId`의 대표 권역과 내부 장소는 함께 자동 추천하지 않는다. | 미착수 | 시장·거리·내부 점포 fixture와 중복 조합 차단 테스트를 추가한다. |
| DATA-24 | 시설·상권 근거는 90일, 검토된 야외 상시 접근 근거는 365일, 행사 근거는 시작 전 재확인 주기를 넘기면 자동 추천에서 재검토 큐로 이동한다. | 미착수 | 고정 현재 시각 fixture로 기한 경계·행사 종료·재검토 이동을 검증한다. |
| REC-16 | 지도 후보와 장소 상세는 같은 진입·귀환 이동수단 규칙으로 시간 적합성을 판정한다 | 대체됨 | 2026-08-21 `REC-17`, `REC-19`, `REC-20`으로 대체. 수동 진입 수단 선택이 아닌 자동 도보·대중교통 조합과 체류 범위를 사용한다. |

### C. 추천 지도와 화면 UX (UX-01~32)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| UX-01 | iPhone 상태 영역과 앱 버튼·텍스트가 겹치지 않는다 | 수동확인 | 주요 화면 확인. 모든 화면 캡처 회귀 테스트 필요 |
| UX-02 | 시간 설정에서 뒤로가기는 메인으로, 코스 만들기에서 뒤로가기는 시간 설정으로 돌아간다 | 수동확인 | 현재 화면 흐름 확인 |
| UX-03 | 결과 화면은 지도와 하단 장소 시트를 함께 보여준다 | 수동확인 | 시뮬레이터 확인 |
| UX-04 | 시트 상단 핸들 스와이프로 지도 중심·장소 중심 상태를 바꾼다 | 수동확인 | 제스처 수정 후 확인 |
| UX-05 | 시트 내부 스크롤과 시트 높이 조절 제스처가 충돌하지 않는다 | 수동확인 | 현재 확인. 다양한 기기 높이 E2E 필요 |
| UX-06 | 후보 마커를 누르면 지도와 장소 목록이 같은 후보에 포커스된다 | 수동확인 | 마커 선택 확인 |
| UX-07 | 사진 마커·기본 마커·현재 위치 마커는 서로 구분되고, 장소명으로 하단 목록과 대응된다 | 부분 | 관계: 보완(UX-06). 사진 없는 장소는 카카오 SDK 기본 핀, 현재 위치는 빨간 위치 점, 장소명은 핀 위 라벨로 구현하고 계약 테스트를 추가했다. 실기기 지도 렌더링 확인 필요 |
| UX-08 | 선택 마커는 확대되고 지도 가시 영역 중앙에 포커스된다 | 부분 | 구현됨. 접힘/펼침 상태별 실기기 확인 필요 |
| UX-09 | 후보 카테고리 필터는 해당 카테고리만 일관되게 표시한다 | 부분 | 구현됨. 자동 UI 테스트 필요 |
| UX-10 | 빈 후보·API 실패·지도 로드 실패 상태가 사용자에게 명확히 표시된다 | 미검증 | 오류 시나리오 E2E 필요 |
| UX-11 | 입력 단계에서 이동수단을 고정하지 않고, 지도는 시간 안에 성립 가능한 장소를 보여주며 이동수단은 장소 미리보기의 구간 아이콘에서만 조정한다 | 대체됨 | 2026-08-21 `UX-18`로 대체. 장소별 수단 선택도 제거하고 도보·대중교통 자동 조합만 추천에 사용한다. |
| UX-12 | 지도 우측 하단의 현재 위치 버튼은 추천을 시작한 현재 위치 마커로 지도를 다시 이동시킨다 | 부분 | 관계: 신규. 후보 지도 기본 시트에서는 가시 지도 영역의 중앙, 접힌 시트와 코스 확인 지도에서는 전체 지도 중앙으로 이동하도록 WebView 오프셋을 적용했다. 계약 테스트 추가, 실기기 확인 필요 |
| UX-13 | 후보 목록 마지막 장소는 기본·확장 시트에서 하단 바나 보이지 않는 시트 영역에 가려지지 않고 끝까지 스크롤된다 | 부분 | 관계: 보완(UX-05). 시트의 숨김 높이를 목록 끝 여백에 반영했다. 실제 제스처 E2E는 [UIUX 테스트 명세](03_product/UIUX_테스트명세.md)의 UXV-12에서 추가한다. |
| UX-14 | 장소 상세는 카드 선택 전의 기본·확장 시트 높이를 유지한 채 사진·시간·엔진이 정한 구간 정보·코스 확인을 보여 주고, 별도 모달을 열지 않는다 | 부분 | 관계: 대체(수단 선택·장바구니). 상세 열기·닫기 전환, 사진 대체 화면, 카카오맵 확인 링크를 구현했다. E2E 명세는 UXV-26~29, UXV-38~39를 따른다. |
| UX-15 | 후보 필터는 분위기·활동 조합이 아닌 단일 가로 스크롤 카테고리 칩이며, 직관적인 카테고리 이름으로 해당 장소만 표시한다 | 부분 | 관계: 대체(기존 분위기·활동 필터). UI 계약 테스트를 추가했고, E2E 명세는 UXV-15를 따른다. |
| UX-16 | 장소 후보 수는 기본·확장 시트에서 공통으로 보이며 접힘·상세에서는 중복되지 않는다 | 부분 | 관계: 신규. UI 계약 테스트를 추가했고, E2E 명세는 UXV-16을 따른다. |
| UX-17 | 뒤로가기·남은 시간·코스 확인은 기본·접힘 시트에서 지도 상단에 고정되고, 확장 시트는 이보다 높은 레이어로 표시되며 시트 내부에 중복되지 않는다 | 부분 | 관계: 대체(장바구니). UI 계약 테스트를 추가했고, E2E 명세는 UXV-17을 따른다. |
| UX-18 | 첫 결과는 도착 전 여유와 실제 검증된 one-stop 대표 1개·서로 다른 대안 최대 3개를 먼저 보여 준다. 사용자는 10분 기본값의 여유 슬라이더를 조절할 수 있고 후보별 이동수단 버튼을 고르지 않는다. | 부분 | 관계: 기존 1~3곳 출시 표현은 REC-30으로 대체되고 다장소는 배포 후 보류됐다. one-stop 실제 8개 공급 게이트는 통과했으며 상태 변경 E2E는 필요하다. |
| UX-19 | 로그인 여부와 무관하게 시간 입력 → 장소 추천 → 카카오맵 길찾기 → 명시 `코스 마치기`가 한 흐름으로 완료된다. 같은 앱 프로세스에서는 외부 지도·background 복귀와 앱 안 메인 이동 뒤에도 활성 V1 코스와 마지막 확정 단계를 이어가며, 완료·명시 취소만 이를 제거한다. 안정적인 `courseRunId`와 강제 종료·재부팅 복구는 App Group 기반 Live Activity 작업에서 추가한다. 후기는 완료와 분리된 선택 행동이다. | 검증 완료 | 관계: 보완(TEST-04, INT-04, UX-23). `U-PROGRESS-RESUME-01` 자동 fixture와 `QA-PROGRESS-RESUME-01` 비로그인 iOS 실기기 `카카오맵 복귀→Home 이어가기→완료 뒤 제거`를 통과했다. 강제 종료 복구·안정적인 run ID·완료 저장은 `U-LIVE-ACTIVITY-01` 범위다. 다른 활성 코스의 silent replace는 새 사용자 관찰에 따라 별도 교체 확인 UX로 보완해야 한다. |
| UX-20 | 현재 위치 권한은 `내 위치 사용`을 누른 뒤에만 요청하고, 거절·GPS 실패 시 지도 직접 선택으로 동일한 추천 흐름을 완료할 수 있다 | 부분 | 관계: 신규(UX-18 보완). 권한 요청 시점·지도 직접 선택 경로 UI 계약 테스트를 추가했다. 허용/거절/GPS 실패 fixture E2E가 필요하다. |
| UX-21 | `경로 설정하기`에서 출발지·도착지를 함께 설정한다. 도착지 기본값은 비어 있으며 적용 시 출발지 복귀이고, 사용자는 공통 위치 선택 화면의 지도·검색·현위치로 두 필드를 바꿀 수 있다. 도착 시각은 분 단위로 설정하되 출시 UI에서 현재 시각부터 120분 이내다. | 부분 | 관계: 기존 180분은 REC-30 one-stop 출시 상한으로 대체됐다. 카카오맵식 route setup·빈 도착지 복귀·출발/도착 공통 선택과 120/121분 fixture를 사용한다. |
| UX-22 | 결과는 실제 경로 검증을 통과한 one-stop 대표 1개와 첫 대안 최대 3개를 제공한다. 미검증 single 후보가 남으면 첫 결과가 0건이어도 `다른 장소 더 보기`로 한 번에 새 검증 장소 최대 3개를 기존 목록 뒤에 누적하며 대표·기존 순서를 바꾸거나 전체 결과를 다시 3개로 자르지 않는다. 별도 `운영시간 확인 후 들러볼 곳`은 조건부 시장·거리 정보 확인용이며, 실제 기기 시각 10:00–18:00에서만 거리순 로컬 페이지와 카카오맵 확인을 제공한다. | 부분 | 관계: REC-31이 REC-30의 continuation 비노출 범위를 one-stop에 한해 교체한다. 2-V 엔진, U-ONE-MORE-01 UI와 QA-ONE-MORE-01 자동 게이트를 수락했다. 다장소 continuation은 계속 보류하며 native 누적·상세 왕복은 UIUX 정리 뒤 출시 후보 수동 smoke 1회에 합친다. |
| UX-23 | `기록` 탭은 이번 달 명시 `코스 마치기` 완료를 기준으로 활동 카테고리 비율과 완료 장소 수를 보여 준다. 후기는 선택이며 없어도 완료가 생성된다. 선택·코스 보기·카카오맵 실행만으로는 성취 수치를 올리지 않는다. 사용자 확인 실제 체류가 없는 완료는 장소/카테고리에 포함하되 완료 활동 시간과 개인화 표본에 넣지 않고 `0분`이 아닌 미측정으로 구분한다. | 구현 전환 필요 | 관계: 기존 완료 후기 기반 집계를 DEC-COMPLETION-RECORD-01로 교체한다. DB-COMPLETION-RECORD-01의 로컬 멱등 repository와 legacy 후기 호환 read model 뒤 UI fixture·빈/미측정 상태를 전환한다. Supabase 완료 이력 동기화는 이번 작업 범위가 아니다. |
| UX-24 | 메인에는 `다음 일정 전, 부산을 한 곳 더 만나볼까요?`를, 결과에는 사실 기반 지역·활동 맥락을 표시한다. 완료 기록이 없는 사용자에게 근거 없는 첫 방문·숨은 장소·인기 표현을 쓰지 않는다. | 부분 | 관계: 신규(REC-24 보완). `course-v1-discovery-context.test.ts`가 카탈로그의 지역·활동 근거가 모두 있을 때만 표시하고 금지 문구를 만들지 않음을 검증했다. iOS 실기기에서 긴 문구의 줄바꿈·VoiceOver 읽기 순서는 미확인이다. |
| UX-25 | `경로 설정하기`의 출발/도착 필드는 하나의 위치 선택 화면에서 현위치·앱 내부 기기 위치 중심의 물방울형 중앙 고정 핀·장소/주소 제안 중 하나를 명시 확정한다. 허용된 GPS 출발지는 provider 주소(없으면 행정구역)를 자동 표시한다. 장소/주소는 카카오 단일 제공사의 우선 검색 후 0건일 때만 반대 검색 1회 fallback하며 동시에 호출하지 않는다. 지도 이동 중 위치 라벨 요청은 0회, 핀 확정은 주소 1회와 주소 없음일 때만 지역 1회다. | 구현 보완 필요 | 관계: 보완(UX-20, UX-21). API-S-7, 2글자/400ms debounce, 10분 메모리 TTL·동일 요청 합치기, 공통 route setup payload, 현위치 권한, 기기 중심 지도·해변 좌표 fallback fixture와 실제 기기 확인이 필요하다. |
| UX-26 | 코스 확인 지도는 추천을 검증한 동일 route geometry로 `설정 출발지 → 추천 장소 → 설정 목적지/복귀` 두 구간을 표시한다. 대중교통 leg는 provider 명시 mode의 `도보→대중교통→필요한 환승 구간→마지막 도보`를 순서대로 보존하고 선이 실제 leg 목적지까지 이어져야 한다. 상세 진입 시 새 route 호출·실시간 GPS 재계산·직선 근사는 하지 않는다. 불완전 geometry는 완전한 선처럼 표시하지 않되 추천을 폐기하지 않고 안내·마커·시간표·CTA를 유지한다. 공개 POI 구간 geometry만 기존 TTL 캐시를 쓰고 private 위치 geometry는 영속 저장·로그에 남기지 않는다. | 구현 보완 필요 | 관계: 1차 marker-only 정책을 `DEC-COURSE-GEOMETRY-01`로 교체했고, 운영 smoke에서 하차역→장소 마지막 도보선 누락을 확인해 하위 구간·종점 완결성 계약을 보완했다. API H1/H2/H3 진단→engine/DB/UI fixture→실기기 1건 순서다. |
| UX-27 | 장소 선택 후 `코스 보기`로 최종 코스에 진입했을 때만 transit geometry와 실제 leg endpoint의 50m 초과 gap을 private Kakao walk로 1곳 최대 4개·2곳 최대 6개 보충해, 하차지점→추천 장소를 포함한 누락 도보선을 지도에 이어 보인다. 이 보충은 추천·transit 검증·시간·순위를 바꾸지 않고, 기존 익명 Auth 세션을 재사용하며 CAPTCHA를 다시 열지 않는다. private 좌표·geometry는 DB·영속 cache·로그에 남기지 않고, 실패해도 상세·CTA는 유지한다. | 기존 검증·4개 일괄 제한 복원 대기 | 관계: UX-26의 `상세 route 0회`를 endpoint walk에 한해 교체한다. 자동 계약과 transit 실기기 1/1에서 하차지점→파란 도보선→장소 marker 연결을 확인했다. |
| UX-28 | 앱 소유의 모든 버튼·탭 카드·칩은 press-in 즉시 눌린 scale/opacity 상태를 보이고 release/cancel 때 복원한다. 일반 CTA·카드·칩·뒤로가기·더보기·tray 삭제에는 햅틱을 적용하지 않는다. disabled·busy에서는 실행·눌림을 막고 Reduce Motion에서는 scale 없이 opacity/색 변화만 사용한다. | 구현 전 | 관계: 신규. 화면별 임의 `Pressable` 상태를 공용 animated pressable 경계로 통일하되 지도 WebView·외부 제어는 제외한다. 공용 pressable의 햅틱 0회, 연타에도 API/navigation 1회인 기존 lock, 주요 CTA·카드·아이콘의 press-in/out/cancel을 검증한다. |
| UX-29 | 도착/개발 테스트 시각은 오전/오후·시·분 wheel로 선택하고 `도착 전 남길 시간`은 5분 단위 slider로 조절한다. wheel의 사용자 새 행과 slider의 사용자 새 5분 값에만 selection haptic 한 번을 허용한다. 최종 값은 momentum 종료 또는 slider drag 값으로 확정하며 mount·programmatic 동기화·중복 값·complete 재호출은 햅틱을 만들지 않고 일반 버튼은 0회를 유지한다. | 검증 완료 | 관계: 기존 UX-29의 `wheel만 햅틱`을 사용자 관찰로 보완했다. 공용 adapter 성공/reject/unavailable과 wheel·slider 호출 횟수 fixture 16/16, 전체 UI/core/typecheck, `ExpoHaptics` 포함 새 internal build의 사용자 실기기 촉각 확인을 통과했다. |
| UX-30 | 계층형 화면은 iOS native stack push/pop과 edge-swipe, 임시 작업은 bottom modal/sheet, Results 내부 선택·로딩은 해당 영역만 짧게 전환한다. 최상위 탭은 스택을 쌓거나 좌우 push하지 않으며 Reduce Motion에서는 큰 이동을 제거한다. | 구현 전 | 관계: 기존 전역 `250ms fade`를 대체한다. navigation option·stack depth·뒤로가기/취소·modal·in-place 상태 fixture와 출시 후보 실기기 전환 체감으로 확인한다. |
| UX-31 | 다크·파란 테마를 유지하면서 메인 CTA와 진행 영역을 분리하고, 대표·대안 카드를 작게 비교하며, 코스 확인 지도는 헤더 safe area 아래에서 좌우 full-bleed로 표시하고 세로 순서를 연속 guide로 읽게 한다. 행동 없는 헤더 우측에는 빈 버튼을 그리지 않는다. 계산 단계는 실제 runtime 상태만 pulse/완료로 표시하며 결과를 늦추는 장식 timer를 추가하지 않는다. | 구현 전 | 관계: 신규(UX-01/03/22/26/28/30 보완). U-RELEASE-VISUAL-01에서 loading callback 순서·호출 0 증가·카드 identity/접근성·geometry/CTA 불변을 자동 검증하고 작은/큰 iPhone은 통합 smoke 1회로 제한한다. |
| UX-32 | 다른 V1 코스가 진행 중이어도 새 추천 탐색은 허용하되, 다른 snapshot의 최종 `코스 시작하기`에서는 취소·기존 코스 이어가기·기존 진행 종료 후 새 코스 시작을 선택하게 한다. 같은 active snapshot 재진입은 묻거나 초기화하지 않고, 교체된 코스는 완료 기록으로 만들지 않는다. | 검증 완료 | 관계: UX-19 보완·기존 silent replace 계약 교체. `U-RUNTIME-GUARD-01 → QA-RUNTIME-GUARD-01`의 집중 26/26, UI 289 통과·의도 skip 1, core 117/117과 제한 수동 active 교체 확인을 통과했다. |
| UX-33 | 추천 카드는 선택 전에 현재 위치·설정 출발지·후보 장소를 함께 보는 전체 지도형 상세를 연다. 하단 floating sheet의 명시 `선택하기`만 기존 Results의 A/B 상태를 바꾸며 닫기는 목록·스크롤·호출량을 유지한다. 1곳/2곳 코스는 엔진 최적 방문 순서로 표시하고, `코스 시작하기` 전후를 같은 지도·세로 순서 화면의 review/active 상태로 운영한다. active에서는 현재 다음 구간의 카카오 길찾기만 활성화한다. | 구현 전 | 관계: UX-14·30, REC-32, COURSE-10/12를 보완·부분 교체한다. `DEC-PLACE-COURSE-FLOW-01` 완료 UI 시나리오는 2026-09-05 최종 승인됐다. 상세 data snapshot, UI 상태 전환, route 0·최적 순서·현재 구간 행동 fixture와 제한 실기기로 검증한다. |

대체 이력: 기존 UX-11 `지도에서 전체·도보·대중교통·차량 필터와 수단별 가능 상태를 비교`는 2026-08-16에 `장소 미리보기의 구간 아이콘 조정`으로 대체했다. 전역 필터는 코스 전체 수단을 고정하는 인상을 주고, 장소 선택 전 불필요한 판단을 요구해 현재 충족률 계산에서 제외한다.

### D. 코스 확인·진행 (15건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| COURSE-01 | 이번 출시의 대표·대안은 실제 검증 one-stop이며 카드 tap은 선택이 아니라 장소 상세를 연다. 사용자는 상세에서 A를 명시 선택하고, 원하면 exact 검증 B 한 곳을 추가해 최대 2곳 pair-only 코스를 확정할 수 있다. 자유 장바구니·3곳·수동 순서 편집은 제공하지 않고 조건부 후보는 대표·대안과 분리한다. | 구현 전환 필요 | 이전 `사용자는 장소를 담거나 빼지 않는다`는 one-stop 전용 출시 범위였으나 `REC-32`와 `DEC-PLACE-COURSE-FLOW-01`의 명시 선택 최대 2곳으로 부분 교체했다. one-stop 공급 게이트와 pair exact 엔진은 확인됐고 새 상세 선택 UI·최종 smoke가 남았다. |
| COURSE-02 | 시간·공간·운영시간 조건을 통과하지 못한 후보는 이유를 표시하고 대표 코스 또는 조건부 수동 포함 코스에 넣지 못하게 한다. | 미검증 | 시간 초과·운영시간 미확인 확인 전/후·경로 실패 fixture가 필요하다. |
| COURSE-03 | 출시 코스 화면은 상단 실제 경로 지도와 하단 `출발지 → 실제 이동 → 장소 [→ 실제 이동 → 장소] → 실제 이동 → 도착/복귀 → 도착 여유`의 세로 순서로 선택된 1곳 또는 엔진 최적 순서의 2곳을 보여 주며 자유 장바구니를 제공하지 않는다. 대중교통 이동은 명시된 도보·대중교통 하위 구간을 실제 순서대로 표시하고 마지막 선이 장소/도착지에 닿아야 한다. 지도선은 추천 검증 snapshot만 사용하고 geometry가 없거나 종점에 닿지 않으면 직선·근사선을 그리지 않는다. 코스 확인(review)의 장소 단계에서만 선택 체류는 `둘러보기 약 N분`, short는 `가볍게 둘러보기 약 N분`으로만 표시한다. active·결과 카드·장소 정보 상세 B에서는 체류 분을 표시하지 않는다. | 구현 보완 필요 | 이전 one-stop 전용 문구를 `COURSE-15`의 최대 2곳 최적 순서로 보완했다. provider 구조·subsegment, 1/2곳·왕복/도착지·정보 상세 route 0과 iOS 한 건 확인이 필요하다. |
| COURSE-04 | 추천은 현재 시각 기준으로만 시작하며 미래 코스를 `내 코스`에 예약 저장하지 않는다. 코스 화면 review에서 명시 `코스 시작하기`를 누른 뒤에만 active course를 만들고 길찾기는 active의 현재 구간에서 별도로 실행한다. | 구현 전환 필요 | 이전 `현재 코스 저장 후 즉시 길찾기/미래 코스 내 코스 저장`을 120분 현재 시각 추천과 review→active 단일 화면으로 교체했다. 시작 전 저장·길찾기 0, 시작 뒤 active 1건 fixture가 필요하다. |
| COURSE-05 | active 코스 화면을 닫으면 메인으로 돌아가고 코스는 유지된다. Home의 `진행 중인 코스`로 같은 `courseRunId`·마지막 확정 단계에 재진입하며 명시 취소·완료만 active를 제거한다. | 자동·비로그인 실기기 확인 | 이전 `내 코스 탭으로 이동`은 미래 코스 저장 흐름과 함께 철회했다. `U-PROGRESS-RESUME-01/QA-PROGRESS-RESUME-01`에서 Home 왕복과 외부 지도 복귀를 확인했으며 review/active 단일 화면 연결 뒤 재검증한다. |
| COURSE-06 | 진행 중 코스는 취소·삭제할 수 있다 | 수동확인 | 내 코스 삭제 UI 확인 |
| COURSE-07 | 카카오맵 길찾기에서 현재 위치를 출발점, 다음 장소를 목적지로 전달한다 | 부분 | 웹·실기기 외부 전환 회귀 테스트 필요 |
| COURSE-08 | 진행 중 현재 위치·현재 시각으로 남은 다장소 코스를 재조립하는 기능은 이번 one-stop 출시 범위에 포함하지 않는다. 향후 재개해도 사용자가 임의로 장소를 추가·삭제하는 자유 장바구니로 되돌리지 않는다. | 배포 후 보류 | 다장소 public store와 진행 상태 저장 계약이 안정된 뒤 별도 결정한다. |
| COURSE-09 | 사용자는 코스 구간의 이동수단을 선택하지 않는다. 엔진은 도보·대중교통의 현실적인 구간 조합만으로 코스를 조립·정밀 검증하고, 길찾기 화면은 결정된 구간별 수단·소요시간·다음 목적지를 보여 준다. | 미검증 | 관계: 대체(사용자 구간 수단 선택 COURSE-09). 차량 전용 후보 제외와 도보/대중교통 혼합 fixture가 필요하다. |
| COURSE-10 | 결과의 대표·대안 요약 카드 전체를 누르면 선택하지 않고 현재 위치·설정 출발지·후보 장소와 근거 있는 상세를 보는 전체 지도형 장소 상세를 연다. 상세의 `선택하기`만 기존 Results로 돌아와 compact tray와 코스 보기 CTA를 만든다. 닫기는 진입 전 A0/A1 상태·목록·스크롤을 유지하고 상세 열기·닫기 route/API 호출은 0이다. | 구현 전환 필요 | 관계: 기존 카드 tap 즉시 선택은 `DEC-PLACE-COURSE-FLOW-01`로 철회했다. 동일 Results/ScrollView와 선택 뒤 B region 전환은 재사용하며 전체 지도·위치 권한 fallback·사전 정제 상세·명시 선택 fixture가 필요하다. |
| COURSE-11 | COURSE-10의 장소 정보 상세 열기·닫기는 route 0회다. 사용자가 장소를 명시 선택하고 `코스 보기`로 최종 코스 화면을 연 경우에만, 선택 snapshot의 transit leg에서 50m 초과로 끊긴 시작·종점 도보 connector를 1곳 합계 최대 4개·2곳 합계 최대 6개 조회한다. 같은 코스의 재렌더·재진입은 세션 메모리 결과를 재사용하고 성공 walk geometry만 기존 transit 선 앞·뒤에 붙인다. | 기존 검증·잘못된 제한 복원 대기 | 관계: 기존 connector 자동 통합·실기기 1회는 통과했다. 새 장소 상세에서는 호출하지 않고 최종 코스 view에서만 기존 보충기를 실행하는 연결 fixture가 필요하다. |
| COURSE-12 | 상세에서 A를 명시 선택하면 A가 기존 Results 상단에 고정되고 exact 검증된 B만 같은 화면에 점진 표시된다. B도 상세의 명시 선택을 거친다. A만 선택한 CTA는 one-stop, B까지 선택한 CTA는 exact 비교로 최적화된 2곳 코스를 연다. 선택 tray는 방문 번호를 암시하지 않고 사용자는 순서를 편집하지 않는다. A의 `×`는 선택 전 목록·더보기·스크롤을 복원하고 B의 `×`는 A와 후보를 유지한다. | 엔진·기존 복원 자동 확인 / 새 상세 UI 구현 전 | 관계: B 정확성·호출 예산·frozen seed·취소 복원은 유지하고 카드 tap 즉시 tray/CTA만 철회한다. 상세 닫기/선택, 최적 순서 표시와 route 0 fixture를 추가한다. |
| COURSE-13 | Kakao 길찾기 handoff 성공 뒤 iOS 17 Live Activity를 시작한다. 예상 이동 `moveMin`에 20% 유예(최소 3·최대 10분)를 더한 시각에 도착을 묻고, 최종 도착/복귀 시각에서 사용자 여유·남은 검증 snapshot 이동·이후 선택 체류를 뺀 안전 출발시각 5분 전에 알린다. 이 이동은 GPS 실측이 아니다. `도착했어요`와 다음/목적지/복귀 handoff 성공 또는 명시 완료만 체류 경계이며, 실패 handoff·미응답·예상시각은 도착/출발로 합성하지 않는다. | 구현 전 | 관계: COURSE-07/12 보완. 1곳·2곳, 이른 도착·유예·1회 snooze·늦은 도착·중복 action·handoff 실패·취소/완료 cleanup을 고정 clock/activity/notification fixture와 iOS 17 제한 실기기로 검증한다. |
| COURSE-14 | 진행 화면의 `코스 마치기`는 같은 `courseRunId`에 대해 후기 없이도 완료 기록을 정확히 한 번 만든다. 선택·코스 보기·코스 시작·길찾기 실행·취소는 완료가 아니며, 저장 재시도·연타·앱 복구도 중복 완료를 만들지 않는다. 저장 실패가 코스 마치기 자체를 막지는 않되 기록 실패를 성공으로 위장하지 않는다. | 검증 완료 | 관계: 신규(UX-19/23, COURSE-13 보완). DB repository와 UI 완료 controller의 created/already/failed·연타·재시도 fixture 및 비로그인 iOS 1곳 명시 완료→기록 즉시 반영을 통과했다. process 생존 중 같은 run 멱등성을 보장하며 force quit 복구는 App Group 후속이다. |
| COURSE-15 | 최종 코스의 지도 번호·세로 장소 카드·구간·카카오 길찾기는 사용자의 선택 순서가 아니라 엔진이 exact 비교한 최소 이동 유효 순서를 사용한다. 코스 검토와 진행은 동일 snapshot·화면의 review/active 상태로 운영하고, active에서는 현재 다음 구간만 길찾기 가능하며 완료/미래 구간은 상태만 보인다. | 구현 전 | 관계: COURSE-03/07/12와 UX-30을 보완하고 별도 CourseConfirm→Progress push를 교체한다. 두 순서/한 순서만 유효/둘 다 실패/동률, 지도·카드·handoff 일치, 시작 전 route 0, 현재 구간만 활성, Home 재진입 fixture가 필요하다. |

### E. 인증·DB·개인화 기반 (11건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| DB-01 | 이메일·비밀번호 회원가입과 이메일 인증 후 로그인이 가능하다 | 수동확인 | Supabase 테스트 계정으로 확인 |
| DB-02 | 로그인 사용자는 코스를 저장하고 내 코스에서 불러올 수 있다 | 수동확인 | 실제 저장·조회 확인 |
| DB-03 | 사용자별 RLS 때문에 다른 사용자의 코스를 읽거나 수정할 수 없다 | 부분 | 정책 적용. 2계정 권한 테스트 필요 |
| DB-04 | 코스에는 출발·약속·시간·이동수단·장소 순서·구간 시간·체류시간 스냅샷이 저장된다 | 부분 | 저장 구현. DB 행 구조 자동 검증 필요 |
| DB-05 | 코스와 장소를 삭제하면 사용자 데이터도 삭제된다 | 미검증 | 삭제·cascade·복원 불가 E2E 필요 |
| DB-06 | 생년월일·필수 약관·개인정보 동의가 가입 과정에서 검증된다 | 미검증 | UI·DB 구현 전 |
| DB-07 | 후기·행동 데이터는 계정 삭제 시 함께 삭제되며, 이후 개인화 근거로만 사용한다 | 미검증 | 후기/계정삭제 기능 구현 전 |
| DB-08 | `맞춤 추천을 위한 체류 기록`은 일반 로그인 사용자의 별도 선택 동의다. Route Proxy anonymous Auth·비로그인·미동의·동의 전 과거 기록은 서버에 저장하지 않으며, 내 정보에서 언제든 끄기·다시 켜기·전체 초기화가 가능하다. | 구현 전 | 관계: 신규. 동의 on/off/재활성·anonymous 격리·소급 0건·초기화·계정 삭제 RLS fixture를 DB-DWELL-01에서 검증한다. |
| DB-09 | 서버 체류 표본은 사용자 확인 완료의 최소 정보만 저장하고 좌표·GPS/polyline을 포함하지 않는다. 파생 프로필은 사용자+`category`+`subCategory` 복합 키로 격리하며, missing subCategory와 incomplete/cancelled/expired는 적용하지 않는다. | 구현 전 | 관계: 신규. 같은 subCategory·다른 category, idempotent completion, 타 사용자 RLS, 최소 필드, raw/profile cascade, 보유 기간 제안을 검증한다. 정확 보유 기간은 사용자 승인 전 운영 정리 job을 활성화하지 않는다. |
| DB-10 | 비로그인도 사용할 수 있는 로컬 `CourseCompletionRecordV1`은 안정적인 `courseRunId`로 멱등 저장하며 후기·활성 진행·서버 체류 표본·로그인 코스 저장과 key/계약을 분리한다. 실제 체류 미확인은 `null`이고 좌표·주소·geometry·receipt·사용자 ID·token은 저장하지 않는다. | 구현 전 | 관계: 신규(DEC-COMPLETION-RECORD-01). 1/2곳, 순차·동시 중복, 손상/실패 storage, legacy 후기 보존, 민감 필드 부재, 외부 호출 0을 DB-COMPLETION-RECORD-01에서 검증한다. |
| DB-11 | Supabase Route Proxy anonymous session은 네트워크 인증용 raw session으로만 사용하고, 내정보·계정 코스 저장·체류 개인화에서는 비로그인으로 처리한다. 사용자 account 상태는 `user.is_anonymous === true`와 일반 계정을 중앙에서 구분하며, 화면 수정을 위해 anonymous를 sign-out하거나 CAPTCHA를 반복하지 않는다. | 검증 완료 | 관계: 신규(DB-08/UX-19 보완). `U-RUNTIME-GUARD-01 → QA-RUNTIME-GUARD-01`에서 null/anonymous/account/loading, Profile·saved course gate, raw session 재사용과 CAPTCHA·anonymous sign-in 추가 0회를 검증하고 익명 내정보 수동 확인을 통과했다. |

### F. 알림·외부 연동·실기기 (6건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| INT-01 | 첫 실행 시 알림 권한을 요청하고, 코스 종료·출발 5분 전 알림을 예약한다 | 대체됨 | 첫 실행 권한 요청은 맥락 없는 요청이라 철회한다. INT-05가 Live Activity 시작/로컬 알림이 필요한 시점의 설명·요청과 안전 출발 계산으로 대체한다. |
| INT-02 | 코스를 취소·삭제하면 예약 알림도 취소된다 | 부분 | 구현됨. 알림센터 확인 시나리오 필요 |
| INT-03 | TMAP·ODsay 호출량 카운터는 날짜 기준으로 유지되고 터미널 로그에 출력된다 | 수동확인 | 호출 누적 확인 |
| INT-04 | 카카오맵 외부 전환·앱 복귀, 권한 거절, 네트워크 실패는 실기기에서 안전하게 처리된다 | 미검증 | 실기기 E2E 및 수동 시나리오 필요 |
| INT-05 | iOS 17에서 길찾기 성공 뒤 Lock Screen Live Activity를 시작하고 지원 기기에는 Dynamic Island를 추가한다. 도착 유예와 안전 출발 5분 전 주의 환기는 로컬 알림으로 처리하며 원격 ActivityKit push/APNs는 사용하지 않는다. 알림·Live Activity 거절/실패가 추천과 Kakao 길찾기를 막지 않는다. | 구현 전 | 관계: INT-01 대체. Activity/notification adapter 성공·거절·실패 fixture, iOS 17 build와 실기기 1곳·2곳 제한 확인이 필요하다. |
| INT-06 | 내 정보의 체류 기록만 앱 토글로 제어하고 알림·위치·Live Activity 시스템 권한은 현재 상태와 `설정에서 변경` 행동으로 표시한다. 최초 거절 뒤에도 다시 활성화할 수 있으며 GPS/background location/Always 권한을 요청하지 않는다. | 구현 전 | 관계: UX-20 보완. 시스템 상태 전환, 설정 deep link, 재활성, 소급 저장 0, Info.plist/capability 검사와 권한 거절 실기기 확인이 필요하다. |

### G. 테스트 기반 자체 (4건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| TEST-01 | 타입 오류, 데이터 계약, UI 상태 계산을 자동 검사한다 | 자동 | `npm run test:typecheck`, `npm test`, `npm run test:ui` |
| TEST-02 | 추천 우회율 하드 필터가 재도입되지 않는다 | 자동 | `test/planner-policy.test.mjs` |
| TEST-03 | 추천 자동진단은 후보 부족의 병목을 시간·최소 체류·운영시간·코스 조립으로 구분한다 | 부분 | 스크립트 반영. 고정 입력 리포트 재생성 필요 |
| TEST-04 | 모바일 E2E는 고정 시각·위치·외부 API 응답을 주입해 반복 가능하다 | 부분 | 관계: 보완. 개발+exact diagnostics 전용 8개 launcher와 무호출 fixture의 입력 교차 계약, 실제 Proxy 8개 실행을 완료했다. 결과 화면은 확보했지만 다른 Metro 연결 때문에 구조화 receipt를 회수하지 못했고 CourseConfirm 복원 1건이 남았다. 두 항목은 출시 후보 단일 smoke로 확인하며 8개 전체를 반복하지 않는다. |

## 4. 다음 자동화 우선순위

1. `REC-17`~`REC-20`, `REC-23`~`REC-24`, `DATA-17`: 1~3곳 코스·도착 여유·체류 범위·운영시간 미확인·완료 이력 기반 발견성 정책을 고정 좌표·시각·이동시간으로 단위 테스트한다.
2. `UX-18`, `UX-19`, `TEST-04`: 시간 설정부터 후기까지의 고정 fixture E2E와 외부 지도 복귀 상태를 만든다.
3. `DB-03`~`DB-05`: 두 계정·삭제·RLS를 Supabase 테스트 데이터로 검증한다.
4. `UX-01`~`UX-10`, `INT-04`: `testID`와 고정 테스트 모드를 만든 뒤 모바일 E2E를 도입한다.

## 5. 실행 기록

| 날짜 | 대상 | 결과 |
|---|---|---|
| 2026-08-15 | `npm run test:typecheck` | 통과 |
| 2026-08-15 | `npm test` | 21개 통과. 코스 변경 RPC·현재 위치/현재 시각 재추천 계약 테스트 포함 |
| 2026-08-15 | `supabase migration list --db-url "$DATABASE_URL"` | 로컬·원격 모두 `202608150009`까지 일치 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 23개 통과. 이동수단 선입력 제거·지도 필터·수단별 상태 UI 계약 테스트 추가 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 26개 통과. 도보·대중교통 자동 혼합 구간, 정밀 검증, DB·카카오 길찾기 수단 전달 계약 테스트 추가 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 27개 통과. 장소 추가 직전 수단 시나리오 선택과 선택 도착 구간 저장 계약 테스트 추가 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 27개 통과. 장소 미리보기의 아이콘 경로·약속 전 여유·체류 가능 시간·구간 수단 변경 UI 계약 갱신 |
| 2026-08-16 | `npm run test:typecheck && npm test` | 28개 통과. 사진 없는 장소의 카카오 기본 핀과 현재 위치 전용 마커 계약 테스트 추가 |
| 2026-08-16 | `npm run test:typecheck && npm test` | 31개 통과. 지도 후보의 세 이동수단 OR 통과 조건, 후보·장바구니 이동수단 규칙 통일, 카카오 보조시설 오매칭 차단, 종교시설 자연관광지 폴백 보류 계약을 갱신했다. |
| 2026-08-16 | `npm run test:typecheck && npm test` | 29개 통과. 후보·장바구니 지도 현재 위치 재중심 이동 계약 테스트 추가 |
| 2026-08-16 | `npm run test:typecheck && npm test` | 29개 통과. 지도 전역 이동수단 필터 제거와 장소 미리보기 구간 조정 계약 테스트로 갱신 |
| 2026-08-15 | `npm test` | 18개 통과 |
| 2026-08-15 | 경성대 스타벅스 → 더바이널, 180분 | 도보 후보 31개 → 시간·최소 체류 31개 → 운영시간 통과 7개 → 코스 10개. 가까운 약속장소에서도 후보가 사라지지 않음 확인 |
| 2026-08-15 | 시뮬레이터 GPS: 스타벅스 경성대점 → 더바이널, 180분 | 가상 위치를 `35.137383, 129.100666`으로 설정해 검증. 장소 검색 좌표와 동일하게 후보 31개·운영시간 통과 7개·코스 10개 생성 |
| 2026-08-15 | 코스 변경 DB RPC | `replace_course_plan` 마이그레이션을 원격 Supabase에 적용하고 함수 존재 확인. 최초 시작·약속 도착 시각을 보존하는 계약 테스트 추가 |
| 2026-08-16 | `npm run test:typecheck && npm run test:ui && npm test` | 통과. 추천 시트 레이아웃·후보 평가·장바구니 코스·장소 상세·지도 제어·진행 일정·진행 화면·지도 경로 세그먼트의 순수/계약 테스트 23건과 전체 계약 테스트 45건을 확인했다. |
| 2026-08-16 | `npx expo export --platform ios --output-dir /private/tmp/timefit-ios-export` | 통과. iOS JavaScript 번들을 실제 생성해 TypeScript 외의 모듈 연결도 확인했다. |
| 2026-08-16 | `npm run test:typecheck && npm run test:ui && npm test` | 통과. 약속이 있는 후보 수집을 출발지·약속지 생활권과 경로 축으로 제한했고, UI 상태 테스트 25건과 전체 계약 테스트 45건을 확인했다. |
| 2026-08-17 | `npx tsx --test test/ui/actual-route-search-scope.test.ts && npm run test:typecheck` | 통과. 실제 경로 기반 범위 모듈의 왕복·우회 경로·수단 합집합·전체 실패·반경 정규화 fixture 5건을 추가했다. 플래너 연결 전 단계다. |
| 2026-08-17 | `npx tsx --test test/ui/route-baseline-service.test.ts && npm run test:typecheck` | 통과. 도보·차량·대중교통 기준 경로의 성공 캐시, 동시 요청 공유, TTL, 부분 실패 재시도, 전체 실패 미저장 fixture 6건을 추가했다. 플래너 연결 전 단계다. |
| 2026-08-17 | `npm run test:typecheck && npm run test:ui && npm test && npx expo export --platform ios` | 통과. 플래너 후보 수집을 실제 기준 경로 범위로 교체하고 기존 직선 범위 모듈을 제거했다. UI 34건·계약 45건과 iOS JavaScript 번들을 확인했다. |
| 2026-08-17 | `npx expo install @react-native-community/slider && pod install` | 완료. 기본 1km·수동 최대 8km 탐색 반경 슬라이더를 추가하고 iOS 네이티브 모듈을 연결했다. |
| 2026-08-22 | `npm run test:typecheck && npm run test:ui && npm test && git diff --check` | 통과. 단일 장소 결과·상세·확인 화면이 공통 `TimeJourney` 모델로 이동·활동·도착 전 여유를 같은 시간 예산에서 표시하는지 UI 50건·계약 48건으로 확인했다. iOS 시각 수동 확인은 남아 있다. |
| 2026-08-23 | `DWELL_ONLY=1 node scripts/build_dwell.mjs && node scripts/build_short_stay_catalog.mjs && node scripts/build_runtime_poi_catalog.mjs && npm run test:typecheck && npm run test:ui && npm test && git diff --check` | 통과. AI-Hub 활동 대응 카테고리의 최빈 체류시간 30분을 권장값으로 사용하고, 356개 활성 장소를 최소 20분·최대 60분(문화시설 120분)으로 재생성했다. UI 50건·계약 49건을 확인했다. |
| 2026-08-23 | `npm run test:typecheck && npm run test:ui && npm test && pod install && npx expo export --platform ios` | 통과. 완료 후기 기반 이번 달 활동 시간·장소 수·카테고리 비율 집계 UI 테스트 2건을 추가해 UI 52건·계약 49건을 확인했다. `react-native-svg`의 iOS Pod `RNSVG` 연결과 iOS JavaScript 번들도 확인했다. |
| 2026-08-23 | `npm run test:typecheck && npm run test:ui && npm test && git diff --check` | 통과. 대표·새 추천·상세 장소만 실제 도보/대중교통 2구간을 정밀 계산하고 동일 요청을 캐시하는 서비스 fixture 4건, 정밀 성공 수단 제한 fixture 1건, 기본 1km·명시적 확장 후보 범위 fixture 1건을 추가했다. UI 58건·계약 49건을 확인했다. |
| 2026-08-23 | `npx tsx --test test/ui/time-setup-clock.test.ts && npm run test:typecheck` | 통과. 개발 빌드 전용 현재 시각 주입과 2시간 종료 시각 재설정·자정 상한 fixture 2건을 추가했다. 실제 요일·위치·API fixture를 포함한 E2E는 후속 작업이다. |

## 2026-09-05 현행 계약 복원 기록

- 사용자 승인: 기존 계약 복원. 관계는 REC-30·COURSE-03/11/15·UX-27·UXV-44/46의 정합성 보완이며 새 정책 도입이 아니다.
- 이전 지시 → 문제: 통합 세션이 U-PLACE-COURSE-FLOW-01 보완에서 과거 전체 체류 분 비노출과 one-stop 전용 connector 4개를 모든 코스에 적용해 review 정보와 2곳 마지막 도보선을 누락시켰다.
- 교체 방식: 최종 코스 **review만** 각 snapshot stop의 계획 체류를 `둘러보기 약 N분`/short `가볍게 둘러보기 약 N분`으로 표시한다. Results·선택 전 장소 정보 상세 B·active는 체류 분 비노출이며 active short 문구는 `가볍게 둘러보기`다. 같은 지도·세로 코스 트리는 유지한다.
- 교체 방식: 선택 후 CourseConfirm의 누락 endpoint walk는 **1곳 최대 4개 / 2곳 최대 6개**, 동시 최대 2개다. 50m 초과의 실제 누락만 조회하고 기존 in-flight/10분 메모리·TTL을 유지한다. 유효 캐시 재사용은 신규 시도 0이며 TTL 만료까지 무기한 0으로 해석하지 않는다. 상세 B 진입·추천/transit 재계산은 0이다.
- 이유: 2026-09-02의 상세 시간 맥락화와 기존 two-stop의 세 legs×두 endpoint 계약을 회복한다. 과거 이력의 `최대 4개`는 one-stop 범위이며 2곳에 적용하지 않는다. 전체 비노출·모든 코스 4개 제한은 **철회**, 위 기준은 **현행·복원 구현 대기**다.
- 실행 단일 기준: `docs/work/uiux/place-detail-and-optimized-course-flow.md`의 마지막 `현행 계약 복원`. 기존 상세 후 명시 선택·모드 통합·패널 실측·실행형 테스트는 보존한다. 과거 카드 즉시 선택·B 전체시간 표시·별도 진행 화면을 복원하지 않는다.


## 2026-09-05 통합 입력 구성 확정 — U-SETUP-UNIFIED-01

- 관계: REC-01/REC-18과 위치 입력 UX의 보완. 이전 중간 `경로 설정→경로 적용` 및 별도 도착 시간 화면 → 필드 편집마다 왕복하는 문제 → TimeSetup에 출발지·목적지 버튼과 상시 도착 시간 휠·여유 slider·하단 추천 CTA를 통합 → 입력 관계를 한눈에 확인하기 위함 → **현행 결정·구현 전**. 중간 경로 화면과 시간 휠 sheet 초안은 철회하며 검색/지도 선택기 자체는 유지한다. 공통 규칙 3-2의 과거 중간 화면/경로 적용 문구보다 이 결정이 우선한다.
- 개발 테스트 시각·출시 추천 QA 버튼은 삭제/비활성화하지 않는다. 기존 internal gate와 기능을 보존한다. 개발 영역이 없는 일반 입력 영역 기준으로 기본 글자 크기 세로 375×812, 390×844, 402×874pt에서 본문 스크롤·입력/CTA 가림 없이 배치한다. 실제 safe area/footer를 반영하고 터치 최소 44pt를 지킨다. 작은 화면/큰 글씨/오류 확장에서는 접근성을 위한 overflow를 허용한다. internal 개발 영역으로 인한 추가 높이는 일반 화면 높이 산정에서 제외하며 개발 버튼 접근을 별도로 검증한다.
- 시간 휠의 정착 값은 입력에 즉시 반영한다. 별도 시간 적용 버튼과 편집만으로 추천 호출은 없다. 120분·목적지 null=복귀·여유 기본10/5~30분·선택적 햅틱·API 예산·인증·저장 경계는 변경하지 않는다.
- 검증: production-used handler 고정 fixture로 직접 입력/위치 확정·취소/늦은 GPS/시간 경계/추천 중복/개발 gate를 확인한다. layout fixture는 기본 화면 contentHeight≤viewportHeight, 가림0; native 캡처 확인과 별도 구분한다. 완료 체크는 구현 및 검증 후에만 한다. 상세 명령: `docs/work/uiux/unified-time-route-setup.md`.


## 2026-09-05 위치 검색 상호작용 확정 — U-LOCATION-SEARCH-01

기존 선택 후 키보드 유지·동일 검색 선택 해제·출발/도착 draft 공유·검색 중 지도 숨김 → 사용자 확정 동작 혼란 → 자동 검색 유지, 결과 선택 시 키보드 닫기/임시 선택, 명시 확정만 부모 적용, 선택이 있을 때만 keyboard-aware CTA 표시 → 검색·선택·확정 분리 → **현행 결정·구현 전**. 동일 검색어 성공/진행 중 재검색은 중복하지 않고 선택 유지, 실패는 명시 재시도 가능하며 실제 검색어 수정은 선택 해제다. 미선택 submit은 검색/키보드 닫기만 하며 첫 결과 자동선택0이다.

출발/도착 새 편집은 빈 draft로 시작하되 기존 확정 좌표는 유지한다. 같은 target의 지도 취소/검색 복귀는 검색 draft·선택·목록 위치를 복원한다. 전체 닫기/필드 전환은 draft를 폐기하고 늦은 검색·GPS·주소 응답을 차단한다. 검색창 아래 현재위치/지도에서 선택은 항상 동일 너비 가로 배치한다. GPS도 임시 선택 뒤 명시 확정하며 권한 거절/실패에 검색/지도 대안을 유지한다. 다크/파랑·읽기 쉬운 장소명/주소·선택됨 표시·필수 아이콘만 유지한다. 추천/DB/API provider·검색 debounce/TTL·시간 안전성 정책은 불변이다.

관계: 기존 위치 입력/통합 입력 계약 보완. 검증은 검색/선택/submit/포커스/실제 편집, 필드 전환 및 같은 필드 지도 왕복, GPS stale·중복 확정0, 선택×키보드 상태·접근성 fixture와 최소 native smoke로 구분한다. 상세 실행 기준 및 검사 항목: `docs/work/uiux/location-search-interaction.md`. 과거 지도 버튼 !query 조건과 항상 노출 비활성 확정 버튼은 철회하며 이 절을 우선한다.


## 2026-09-05 U-LOCATION-SEARCH-01 실기기 반환 보완

사용자 실기기에서 출발/도착 분리·현위치 흐름과 검색 기능은 정상 확인했으나 재포커스 시 키보드보다 확정 버튼이 늦게 이동했다. keyboard와 footer는 동일 전환으로 동기화하고 종료 후 추가 점프를 제거한다(원인 조사·native 검증 필요). 기존 지도 하단 검색 대안 유지 규칙은 **부분 철회**: 뒤로가기가 같은 검색 draft를 복원하므로 하단 `검색으로 선택`을 제거하고 실패 안내도 뒤로가기 검색 대안에 맞춘다. 기존 노선 별도 줄 유지도 **부분 철회**: 결과 정보는 장소명·주소만, 선택됨은 상태로 유지하며 provider 데이터는 보존한다. 미선택 행 경계 부족은 결과 사이 가는 구분선으로 보완한다. 이유는 중복 행동/정보 감소와 버튼 인지성 향상이다. 상태: **사용자 확정·보완 구현 전**. 기존 검색/권한/시간/엔진 정책 불변. 최신 명령과 회귀/native 기준은 `docs/work/uiux/location-search-interaction.md` 상단을 따른다.


## DEC-NEARBY-BROWSE-01 / UX-34 — 주변 둘러보기 (2026-09-05)

관계: 기존 내 코스 최상위 탭을 대체하는 신규 정보 탐색 UX, 추천 정책은 불변. 약속 시각과 무관하게 기준 위치 직선거리3km 이내 기존 공개 카탈로그 장소를 지도+하단 거리순 목록으로 제공한다. 사진 마커/없음·실패 기본 마커, 겹침 cluster, 선택 마커 강조·이름, 목록/마커/상세 연결을 사용한다. 지도 이동은 기준 위치/목록 순서를 바꾸지 않는다. 동일 거리 ID tie, 동일좌표 cluster도 목록에서 전수 접근 가능해야 한다. 장소 상세는 시간 검증/코스 선택이 없는 정보 표시이며 인기·방문자 데이터나 운영시간을 합성하지 않는다. 기존 카카오 장소 opener만 재사용한다.

기존 내 코스 탭 → 주변 발견과 다른 저장 중심 진입 → 주변 둘러보기로 교체 → 장소 발견 목적 분리 → **확정·구현 전**. legacy 저장 데이터 삭제는 포함하지 않는다. 적격 공개 장소만 사용하고 hold/잘못된 좌표/중복 제외, 조건부 장소는 운영시간 확인 필요로 표시한다. 추천 Results의 조건부 시장10~18 gate는 유지하되 신규 정보 탐색을 검증 코스로 취급하지 않는다. 현위치 거절/실패는 수동 기준 위치 대안, 지도 실패는 목록 유지. 자동 추천·route·저장·체류 측정 호출0. 지도 타일/사진 로딩은 별도 네트워크임을 구분한다.

검증: 반경 경계/정렬·pan 불변·사진 fallback/cluster·시트와 선택 ID·권한 실패/지도 실패·외부 쓰기0 fixture와 마지막 지도 native smoke. 실행 명령 및 범위: `docs/work/uiux/nearby-browse.md`. 상태: 구현/검증 전.


## 주변 둘러보기 시트 배치 확정 — U-NEARBY-BROWSE-01 / UX-34 보완

지도 표시 사용자 확인 후, 이중 floating 패널의 가림/틈 문제를 다음으로 교체한다: 시트는 좌우0·하단0의 연결된 바텀시트(상단만 둥근 모서리), 탭바는 기존 위치/형태 그대로 시트 위에 floating 유지. 접힘/펼침 모두 아래 배경 연결, 접힘은 제목/첫 행 가시성, 목록 끝·상세 CTA는 실제 탭바 가림 frame과 끝 여백으로 접근 보장. 기존 safeBottom+72 기준의 떠 있는 시트는 철회하며 탭바 자체 변경/숨김은 하지 않는다. 상태: **사용자 확정·구현 전**. 실제 visible sheet viewport와 map occlusion inset, 큰 글씨/마지막 행/CTA 터치 검증은 `docs/work/uiux/nearby-browse.md` 최신 절을 따른다. 데이터·추천·지도 HTML 복구 정책 불변.

## DEC-PROFILE-SETTINGS-01 — 내정보 구성 확정 (2026-09-05)

기존 인증 구분·권한 설정 요구의 보완이다. 내정보는 로그인 강제 없이 **내 프로필 / 권한 및 맞춤 추천 / 앱 안내** 세 영역으로 구성한다. 비로그인 계정 영역은 로그인 진입, 로그인은 닉네임·프로필 관리이며 로그아웃/계정 삭제는 관리 안쪽에 둔다. 메인 우상단 프로필 버튼은 비로그인→로그인, 로그인→내정보; 완료/취소 뒤 원래 화면·선택·진행 상태를 유지한다. anonymous는 비로그인으로 취급한다.

권한은 실제 상태/설정 이동, 체류 동의는 별도 opt-in이며 Live Activity 항목은 구현 시 연결한다. 앱 안내는 개인정보/위치정보 안내·문의·버전이다. 내정보의 방문 기록 관리 중복 제안과 내정보 진입 로그인 강제는 철회한다. 방문 기록 삭제는 기록 탭, 개인화 초기화는 별도 계약이며 guest 자동 병합·자동 동의는 금지한다. 이유: 중복을 줄이고 비로그인 설정 접근을 보존한다. 상태: **사용자 확정·구현 전**. 기존 추천 계산·저장 정책은 변경하지 않는다.

상세 입력/예외/검증/미결정 계약: [내정보 결정](work/integration-decision/profile-settings.md).
## 2026-09-06 Live Activity 실행 순서 보완

DEC-LIVE-LOCAL-FIRST-01: DB/개인화 일괄 연결 대기 → 기기 기술 검증 A·로컬 진행 B·QA-LIVE-LOCAL-01 → 서버 동의/저장/개인화 후속 연결로 분리한다. 기기 기능부터 검증하기 위한 순서 변경이며 추천·동의 정책은 불변이다. 로컬 단계는 서버 표본/나중 업로드 큐0, exact 시각은 활성 복구 한정, 완료/취소 정리와 기존 명시 완료 멱등성을 보존한다. handoff 성공 후 Activity 시작은 iOS17 실행 제약을 먼저 검증하고 실패 시 정책을 임의 변경하지 않는다. 상태: **계획 현행·구현 전**. 상세 명령: docs/work/integration-decision/live-activity-local-first.md. 개인화 토글/서버 저장의 과거 일괄 구현 지시는 로컬 단계에 적용하지 않는다.
