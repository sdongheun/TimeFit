# TimeFit 테스트 현황

> 기준일: 2026-08-15 16:50 KST  
> 목적: 사용자가 확정한 동작 요구사항을 단일 목록으로 관리하고, 구현 여부가 아니라 **검증 근거** 기준으로 테스트 충족률을 갱신한다.

## 1. 갱신 규칙

- 사용자가 새 요구사항이나 이상 사례를 말하면 먼저 이 문서에 ID와 기대 결과를 추가한다.
- 새 요구사항을 등록하기 전, 기존 ID와 비교해 관계를 `동일`, `보완`, `충돌`로 판정한다.
  - `동일`: 새 항목을 만들지 않고 기존 ID의 근거·테스트 시나리오만 갱신한다.
  - `보완`: 기존 ID를 유지하고 세부 조건을 추가한다. 기존 결과에 영향을 주면 변경 내용을 함께 기록한다.
  - `충돌`: 기존 규칙을 임의로 덮어쓰지 않는다. 사용자에게 기존 유지·새 규칙으로 대체·두 규칙 병행 중 선택을 물은 뒤, 확정된 방향만 반영한다.
- 대체된 요구사항은 삭제하지 않고 `대체됨` 상태와 대체한 ID·결정일을 남긴다. 현재 적용되는 요구사항만 충족률에 포함한다.
- 구현 전에는 `미검증`, 테스트 코드가 실패를 재현하면 `부분`, 자동 테스트 또는 재현 가능한 수동 시나리오가 통과하면 상태를 갱신한다.
- 추천·데이터·DB 정책을 변경한 커밋에는 관련 ID, 실행 명령, 결과를 함께 기록한다.
- UIUX-35는 기존 UXV-18의 `시간 예산 우선 표시` 요구를 **보완**한다. 결과·상세·확인 화면의 시간 값은 별도 계산하지 않고 공통 시간 여정 모델에서 파생한다.
- DATA-18은 2026-08-23 중앙값 권장 정책을 **대체**한다. 권장 체류는 활동 대응 AI-Hub 카테고리의 최빈값 30분이며, 최소 20분·최대 60분(문화시설 120분)은 자투리 활동 제품 정책으로 둔다.
- 퍼센트는 코드 라인 수가 아니라 **요구사항 수** 기준이다. 수동 확인은 기능 확인이지 자동 회귀 방지는 아니므로 자동화율과 분리한다.

| 상태 | 의미 |
|---|---|
| `자동` | CI/로컬 명령으로 반복 검증 가능 |
| `수동확인` | 시뮬레이터 또는 실기기에서 사용자가 확인 |
| `부분` | 구현은 있으나 핵심 분기·실기기·회귀 검증이 부족 |
| `미검증` | 요구사항은 확정됐지만 검증 근거가 없음 |
| `대체됨` | 이후 확정된 요구사항으로 더 이상 적용하지 않음. 충족률 계산에서 제외 |

### 요구사항 등록 형식

```text
ID: REC-XX
관계: 신규 | 동일(REC-XX) | 보완(REC-XX) | 충돌(REC-XX)
기대 결과: 사용자가 확인 가능한 한 문장
검증 방식: 단위 | 통합 | E2E | 수동
결정: 유지 | 대체 | 병행 (충돌일 때 사용자 확정 후 기록)
```

## 2. 현재 요약

| 지표 | 요구사항 수 | 비율 |
|---|---:|---:|
| 자동 검증 완료 | 이전 집계 16 / 75 | **21%** |
| 수동 확인 완료 | 이전 집계 16 / 75 | **21%** |
| 확인 완료 합계 | 이전 집계 32 / 75 | **43%** |
| 부분 검증 | 이전 집계 26 / 75 | 35% |
| 미검증 | 이전 집계 17 / 75 | 23% |

위 수치는 이번에 추가한 `REC-24`, `UX-24` 및 대체 처리한 항목을 반영하기 전 **이전 집계**다. 현행 요구사항의 상태별 재집계는 다음 QA 세션에서 수행한다. 기능 구현률이 아니라 자동 또는 수동으로 기대 결과를 확인한 비율이며, 공모전 제출 전 목표는 `확인 완료 90% 이상`, 그중 핵심 흐름 자동화 `50% 이상`이다. 후보 지도와 상세 시트의 세부 UIUX 요구사항은 [UIUX 테스트 명세](03_product/UIUX_테스트명세.md)에서 별도로 관리한다.

## 3. 요구사항 목록

### A. 추천 엔진과 시간 안전성 (REC-01~24, 대체 ID 포함)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| REC-01 | 입력 시간은 1~180분 범위이며 종료 시각이 시작보다 뒤여야 한다 | 미검증 | 2026-08-23 사용자 결정으로 120/240분 충돌을 180분으로 통일했다. 1분·180분·181분과 자정 경계의 고정 시각 테스트가 필요하다. |
| REC-02 | 지도 후보는 도보·대중교통·차량 중 하나 이상의 진입 수단으로 최소 체류·안전 여유를 충족할 때만 표시한다 | 대체됨 | 2026-08-21 `REC-17`, `REC-19`로 대체. 차량 OR·다중 장소 기준은 새 단일 경유지 정책과 충돌한다. |
| REC-03 | 후보는 출발지 근처·도착지 근처·실제 이동 경로 주변에서 함께 수집하고, 세 범위 밖 장소는 후보로 넣지 않는다 | 부분 | 철회 이력: 직선 생활권·중간 표본 방식은 서면→사상에서 부산역 후보가 섞일 수 있어 실제 경로 기반 방식으로 교체했다. 추천 실행 시 실제 경로 범위 안 최대 8km 공간 후보를 한 번 준비하고, 지도는 기본 1km만 표시하며 슬라이더로 이미 준비된 후보의 표시 범위만 확장한다. 실제 경로 polyline fixture 5건, 기준 경로 캐시·부분/전체 실패 fixture 6건, 지도 슬라이더 API 미호출 UI 계약을 추가했다. 실제 API E2E가 남아 `부분`이다. [전환 기록](archive/product-history/추천_실경로탐색전환계획.md) 참조. |
| REC-04 | 약속이 가까워도 시간 안의 후보를 우회율 때문에 제거하지 않는다 | 자동 | `planner-policy.test.mjs` 및 경성대 사례 호출 재현 완료 |
| REC-05 | 후보 상태는 최소 체류·수단별 안전 여유를 하드 조건으로 하고, 추가 여유 10분으로 여유·짧게 가능·빠듯을 구분한다 | 부분 | 관계: 보완. 최소 체류 통과 테스트는 있으나 10분 상태 경계 자동 테스트가 필요하다. |
| REC-06 | 시설형·유료 성격 장소는 추가 이동보다 체류가 짧으면 제외한다 | 부분 | 정책 구현. 카페·식당 경계 테스트 필요 |
| REC-07 | 자연·거리·광장 성격 장소는 짧은 접근 시 15~20분 체류를 허용한다 | 부분 | 정책 구현. 거리별 경계 테스트 필요 |
| REC-08 | 이동 효율은 하드 제외가 아니라 전체 자투리 예산 대비 추가 이동 비중으로 순위에만 반영한다 | 자동 | `routeMovementOverhead()` 및 정책 테스트 |
| REC-09 | 후보 목록은 근사 이동시간, 코스 확정은 TMAP·ODsay 실제 이동시간으로 재검증한다 | 대체됨 | 2026-08-23 `REC-21`로 대체. 대표 추천·새 추천·상세는 실제 경로값만 표시하며, 근사값은 내부 후보 선별·순위에만 사용한다. |
| REC-10 | 3곳 이상도 시간 안이면 장바구니에 담을 수 있다 | 대체됨 | 2026-08-23 `REC-17`로 대체. 자유 장바구니 대신 엔진이 최대 3곳 대표 코스를 조립한다. |
| REC-11 | 장소 순서를 자동 정렬하고 사용자가 바꾸면 시간 적합성을 다시 계산한다 | 대체됨 | 2026-08-23 `REC-17`로 대체. 1~3곳 순서는 엔진이 조립하며 사용자가 직접 편집하지 않는다. |
| REC-12 | 같은 `siteGroupId`의 포괄 장소와 내부 장소는 함께 담을 수 없다 | 자동 | 데이터 계약 테스트 및 엔진 조합 규칙 |
| REC-13 | GPS로 받은 부산 좌표도 장소 검색 좌표와 동일하게 출발지로 사용해 후보를 생성한다 | 수동확인 | 관계: 보완(REC-03). 시뮬레이터 GPS를 스타벅스 경성대점으로 설정한 뒤 180분 도보 추천에서 후보 31개·운영시간 통과 7개·코스 10개 확인. 자동 UI 테스트 필요 |
| REC-14 | 지도 기본 반경에서 보이는 장소는 공간·근사 시간뿐 아니라 현재 운영 가능 여부를 통과해야 하며, 코스 확인 단계는 실제 경로·운영시간으로 최종 재검증한다 | 부분 | 관계: 보완(REC-03, REC-09, DATA-06). 2026-08-17 발견: 지도 탐색 모드가 운영시간 게이트보다 먼저 반환되어, 지도 후보 수가 기존 코스 수보다 눈에 띄게 늘었다. 로컬 운영시간 게이트를 추가했다. 시설형 운영시간 경계, 미확인 카페·식당·시설 제외, 야외 자연관광 허용, 시간대 미확인 시장·거리 제외 단위 테스트 4건 통과. 서면 왕복 120분 기준 기본 1km 지도 후보는 36개에서 24개로 줄었다. 실제 API 확정 차단과의 일관성 E2E가 남아 `부분`이다. |
| REC-15 | 장소 상세의 이동·체류·남는 시간은 같은 전체 코스 시간 예산으로 표시되며 세 값의 합은 입력 시간과 일치한다 | 자동 | 관계: 보완(UIUX 상세 시트). 2026-08-17 발견: 기존 화면은 새 장소의 접근·다음 구간 이동만 보여주면서, 남는 시간은 이미 담긴 장소를 포함한 전체 코스로 계산해 기준이 섞였다. `총 이동`, `코스 체류`, `남는 시간`으로 통일했고 `candidate-evaluation.test.ts`가 다중 장소 시나리오의 합산 일치와 실제 이동 합계를 검증한다. |
| REC-17 | 추천의 기본 단위는 엔진이 조립한 1~3곳 코스이며, `출발 → 1~3개 장소 → 도착지/복귀` 전체 이동·**권장 체류**·도착 여유가 시간 예산 안이어야 한다. 결과는 상단 대표 1개와 검증 대안 목록으로 구성한다. | 대체됨 | 2026-08-30 `REC-27`로 대체. 코스 단위·대표/대안 구조는 유지하되, 권장 체류를 전 코스의 하드 조건으로 둔 규칙을 철회했다. |
| REC-18 | 사용자가 약속 전 남길 도착 여유를 슬라이더로 설정하며 기본값은 10분, 0분은 허용하지 않는다 | 부분 | 관계: 신규(REC-01, REC-05 보완). 출시 UI에 5~30분 슬라이더와 기본 10분을 적용했다. 고정 시각 UI/E2E 경계 검증이 남았다. |
| REC-19 | 차량은 추천 후보 통과·공간 탐색·순위에 사용하지 않고, 도보·대중교통 조합만 추천 판정에 사용한다 | 부분 | 관계: 대체(REC-02, REC-03). 단일 장소 추천 모듈은 차량을 타입 수준에서 받지 않으며 차량 전용 후보 제외 fixture가 통과한다. 실제 경로 범위 서비스의 차량 baseline 제거는 후속 정리다. |
| REC-20 | 대표·대안 코스는 권장 체류 합계를 충족해야 하며, 남는 시간은 시간 여정에 그대로 표시한다. 최소 체류만 가능한 코스를 `짧게 가능`으로 대표·대안에 노출하거나, 남는 시간을 채우려고 체류·장소 수를 늘리지 않는다. | 대체됨 | 2026-08-30 `REC-27`로 대체. 최소 체류만 가능한 검증 코스도 숨기지 않되, 같은 장소를 체류시간 변형으로 중복하거나 남는 시간을 채우려 체류를 늘리는 규칙은 계속 금지한다. |
| REC-21 | 대표 추천·검증 대안 목록·사용자가 상세로 연 장소는 후보 단계와 선택 단계에서 동일한 실제 도보·대중교통 구간 시간으로 이동·**엔진 선택 체류**·도착 여유·남는 시간을 표시한다. 같은 좌표쌍·수단의 재조회는 TTL 캐시를 재사용한다. | 구현 전환 필요 | 관계: 보완(REC-27), 대체(REC-09의 근사 후보 표시). 코스 V1은 사전선정 뒤 제한된 후보만 정확 경로로 검증한다. 선택 체류와 `추천/짧게 가능` 상태가 목록·상세·저장에 같은 스냅샷으로 전달되는 fixture가 필요하다. |
| REC-22 | 기본 지도는 실제 경로·출발/도착 생활권의 좁은 활동 범위 안에서 `추천` 또는 `짧게 가능`인 후보만 보인다. 먼 후보와 불가능 후보는 사용자가 명시적으로 범위를 넓히는 탐색에서만 보여 준다. | 부분 | 관계: 대체(REC-03의 8km 선조회·슬라이더 표시 확장). 초기 최대 8km 후보 풀은 유지하되 기본 표시는 1km, 사용자 슬라이더의 `2·3·5·8km`에서만 확장한다. `one-stop-search-scope.test.ts`가 기본/확장 후보 경계를 고정 fixture로 검증한다. 지도 실기기 상호작용과 가까운·왕복·먼 약속 E2E가 남았다. |
| REC-23 | 운영시간 미확인 조건부 후보는 대표 코스·자동 확정에서 제외하고 더보기에만 `운영시간 확인 필요`로 표시한다. 카카오맵 확인 뒤 사용자의 명시 확인이 있으면 조건부 수동 포함 코스에만 넣을 수 있으며, `추천/검증됨`으로 승격하지 않는다. | 대체됨 | 2026-08-31 `REC-28`, `DATA-25`로 결과 역할을 세분화했다. **접근 시간 근거가 없는** 조건부 후보의 대표 제외·조건부 상태 유지 원칙은 그대로다. |
| REC-24 | 시간·운영시간·실제 경로 하드 조건과 기본 적합성이 같은 후보 사이에서만, 앱의 완료 기록에 없는 지역·활동을 보조 순위에 쓸 수 있다. GPS 방문·소비 이력이나 근거 없는 첫 방문·인기도는 추정하지 않는다. | 미착수 | 관계: 신규(제품 발견성 보완). 완료 기록 있음/없음·동점 후보 fixture에서 완료 이력 기반 순위만 달라지고, 시간 적합성·운영시간 통과 여부는 바뀌지 않는지 검증한다. |
| REC-25 | 대표·대안은 표시 전 실제 경로를 검증한다. 서버 Route Proxy는 공개 장소 간 방향·수단별 구간 캐시와 Kakao 수단별 일일 호출량을 관리하며, 정밀 GPS·사용자 ID·IP를 장기 경로 캐시나 호출량 집계에 저장하지 않는다. | 구현 전환 필요 | 관계: 보완(REC-21). 2026-08-28 결정: 대중교통·도보는 Kakao REST 단일 제공사이며, 동일 구간 cache hit를 우선한다. Kakao 실패·한도 초과는 TMAP·ODsay·근사시간으로 우회하지 않고 검증 불가로 반환한다. ODsay Basic(30/일)과 TMAP은 자동 추천 런타임에서 제외한다. mode·방향·공개 POI identity·route version cache, 도보/대중교통의 독립 soft/hard 일일 한도, 개인정보 비영속, provider fallback 0회를 fixture로 검증해야 한다. 자동 대표 ID·좌표·분류 변경 때는 mobile snapshot과 Edge `ROUTE_PROXY_CATALOG_SNAPSHOT_JSON`을 같은 version으로 배포하고, stale public version은 receipt를 가진 안전 `rejected`로 구분하며 provider 호출 0임을 fixture로 검증한다. |
| REC-26 | 첫 추천은 새 Kakao provider attempt 최대 8회로 대표 포함 검증 코스 4개를 목표로 한다. 8회 뒤 4개 미만이면서 사전 선별한 미검증 코스가 남은 경우에만 전체 최대 16회까지 보충한다. `다른 검증 코스 더 보기` 한 번은 같은 세션의 다음 후보만 최대 8회 검증해 새 코스 최대 3개를 추가한다. cache hit·세션 동일 구간 재사용은 새 attempt로 세지 않으며, 후보 큐 소진·전역 한도·provider 실패 때는 목표를 채우려 호출하지 않는다. | 배포 후 보류 | 관계: 대체(2026-08-29의 고정 8회 REC-26), 보완(REC-21, REC-25, REC-28). 이 행의 1~3곳·첫 8→16회 규칙은 계속 보류한다. one-stop 첫 8회와 사용자 요청당 8회는 별도 `REC-31`이 현행 기준이다. |
| REC-27 | `출발 → 1~3개 장소 → 도착지/복귀`의 실제 도보·대중교통, 구조화 운영시간, 도착 여유를 통과하면서 각 장소에 최소 체류 이상을 배정할 수 있으면 대표·대안 후보가 된다. 권장 30분 충족은 우선순위·`추천` 상태이고, 최소 20분만 가능한 실제 경로 코스는 `짧게 가능` 상태로 함께 반환한다. 한 장소·같은 순서는 체류시간 변형으로 중복하지 않으며, 최대 60/120분이나 남는 시간만으로 체류를 자동 연장하지 않는다. | 배포 후 다장소 보류 | 관계: 보완(REC-17, REC-20, REC-21). 1곳의 최소/권장 체류·운영시간·실제 경로 규칙은 `REC-30` 출시 entry에 그대로 적용한다. 2곳 혼합 체류와 3곳은 public store 원인 해결 뒤 재개한다. |
| REC-28 | 결과는 실제 경로·운영시간·최소 체류·도착 여유를 통과한 상단 대표 코스 1개와 첫 대안 최대 3개로 구성한다. `다른 검증 코스 더 보기`는 `continueLimitedRepresentativeCourseV1({ ...originalInput, continuation })`에 첫 계산과 같은 입력·provider·route ports를 다시 전달하고, continuation의 cursor·후보 ID/signature·검증/탈락 signature·opaque receipt key로 다음 후보만 로컬 재수화한다. 새로 검증된 코스만 기존 목록 뒤에 최대 3개 추가한다. continuation에 좌표·사용자 ID·함수/provider 객체를 넣거나 서버에 세션/큐를 저장하지 않는다. 재수화 불일치는 route 호출 없이 `continuation_unavailable`으로 종료한다. 이미 보인 동일 코스·순서만 다른 동일 집합·탈락 코스는 재검증하지 않으며, 화면의 총 대안 수에는 고정 상한을 두지 않는다. | 배포 후 다장소 보류 | 관계: 대체(2026-08-31의 무경로 탐색 목록 REC-28), 보완(REC-21, REC-26). 이 행의 `continueLimitedRepresentativeCourseV1`와 다장소 queue는 계속 보류한다. `REC-31`은 같은 안전 원칙을 별도 one-stop entry로 재사용하며 2·3곳을 재활성화하지 않는다. |
| REC-29 | 구조화 운영시간이 없는 시장·거리·골목은 검증 코스가 아니라 별도 `운영시간 확인 후 들러볼 곳`에만 표시한다. 데이터가 권역형 조건부 후보로 식별하고 시설·개별 점포·도매/새벽 전용·장기 이용 장소를 제외했을 때만 **기기의 실제 현재 시각** `10:00 ≤ t < 18:00`에 노출한다(09:59 숨김, 10:00·17:59 노출, 18:00 숨김). 18:00 경계를 넘거나 foreground 복귀 시 이미 보인 조건부 카드·더 보기도 숨기며, 개발용 추천 시작 시각은 이 창을 연장하지 않는다. 출발/도착 중 가까운 쪽과의 직선거리로 가까운 8개를 먼저 보이고 로컬 `더 보기`로 다음 8개를 기존 뒤에 추가하며, 이 탐색은 route/API 호출 0이다. 카카오맵 확인 뒤 사용자가 명시적으로 계산을 요청한 경우에는 실제 **확인 시각도** `10:00 ≤ t < 18:00`일 때만, 최초 추천 뒤 경과 시간을 차감한 입력으로 실제 경로를 계산한다. 이후 시각은 route 0·재추천 안내이며 성공해도 `운영시간 확인 필요(사용자 확인)` 상태를 유지한다. | 구현 전 | 관계: 보완(REC-23, DATA-25). 실제 표시 시각과 추천 세션 시각을 분리한 09:59/10:00/17:59/18:00·18:00 경계 경과·foreground 복귀, 경과 시간 차감, 8개 페이지·거리순·중복 없음·더 보기 0 route, 제외 분류, 외부 지도 열기 0 route call, 명시 확인 전 route call 0, 확인 뒤 조건부 상태 유지 fixture를 검증한다. |
| REC-30 | 공모전 출시 게이트에서 현재 시각부터 최대 120분 안의 자동 추천은 실제 경로·운영시간·최소 20분 체류·도착 여유를 통과한 **1곳**만 계산·표시하며 2·3곳 public segment 요청은 시작하지 않는다. 결과 카드는 사진·장소·활동 카테고리와 도착 여유를 제외한 `이동+선택 체류` 합계만 `약 N분 코스`로 표시한다. 코스 확인 상세는 실제 이동과 선택 체류를 세로 순서로 보여 주며 체류는 `둘러보기 약 N분`, short는 `가볍게 둘러보기 약 N분`으로만 표현한다. 최소·최대·범위·조절기와 진행 화면 체류 분은 노출하지 않는다. | 부분 | 관계: 2-U·U-RELEASE-ONESTOP-01·U-COURSE-CARD-DETAIL-01 구현과 카드 상세 fixture 7/7을 수락했다. 추천 선택 폭 확장은 별도 REC-31이며, 출시 후보의 실제 상세·길찾기 단일 smoke가 남아 부분 상태를 유지한다. |
| REC-31 | 출시 one-stop 첫 결과는 신규 provider attempt 최대 8회로 대표 1개와 첫 대안 최대 3개를 반환한다. 미검증 single 후보가 남을 때만 `다른 장소 더 보기`를 표시하며, 한 번 누를 때 다음 후보를 신규 attempt 최대 8회로 검증해 새 코스 최대 3개를 기존 목록 뒤에 누적한다. 대표·기존 순서와 카드는 유지하고 전체 표시 수에는 고정 상한을 두지 않는다. 첫 결과가 0건이어도 후보가 남으면 최종 실패로 닫지 않고 명시 더보기를 제공한다. cache/session reuse는 새 attempt가 아니며, 중복·기탈락 후보 재검증, 자동 연속 페이지, 2·3곳 조립, 조건부 자동 승격은 금지한다. | 자동 확인 | 관계: 보완(REC-30), one-stop 범위에서만 REC-26/28의 요청형 검증 원칙을 재사용한다. `2-V`, `U-ONE-MORE-01`, `QA-ONE-MORE-01`을 수락했다. 밀집 fixture에서 고유 장소 `4→7→10`, initial 0건 뒤 `0→3`, 호출 상한과 금지 경계를 확인했다. native 체감은 전체 UIUX 정리 뒤 출시 후보 수동 smoke 1회에 합친다. |

### B. 장소 데이터·운영시간 신뢰도 (DATA-01~24 및 대체 REC-16)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| DATA-01 | 추천 카테고리는 자연관광지·상업지구·레저/스포츠·문화시설·카페만 사용한다 | 자동 | 일반 식당은 자투리 활동 카탈로그에서 제외한다. `recommendation-data-contract.test.mjs` |
| DATA-02 | 숙박·교통·의료·주차 시설은 추천 데이터에서 제외한다 | 자동 | 데이터 계약 테스트 |
| DATA-03 | 이름 차이 중복 장소는 대표 장소로 병합하고, 병합 근거는 검토 원천에 남긴다 | 자동 | 데이터 계약 테스트 |
| DATA-04 | 포괄 장소는 대표 권역으로 사용하고 내부 개별 장소는 런타임에서 제외한다 | 자동 | 데이터 계약 테스트 |
| DATA-05 | 카카오에서 상세 확인되지 않은 장소도 활동 근거가 있으면 조건부 후보로 남기되, 카카오 검색 링크와 운영시간 상태를 제공한다 | 자동 | 데이터 계약 테스트 |
| DATA-06 | 시설형·권역형 장소는 신뢰할 수 있는 운영시간 근거가 없으면 자동 추천하지 않는다 | 대체됨 | 2026-08-31 `DATA-25`로 세분화했다. 시설형은 계속 구조화 운영시간이 필수이고, 권역형은 전체 점포 운영시간이 아닌 검토된 **권역 접근 근거**가 있을 때만 탐색 후보가 될 수 있다. |
| DATA-07 | 새벽에는 검토된 야외 상시 접근 장소 외 시설·상권을 추천하지 않는다 | 자동 | 자연관광 성격 게이트 테스트 |
| DATA-08 | 카카오 보조시설 결과는 `weak`으로 보존하되, 상세 링크 대신 안전한 장소 검색 링크를 제공한다 | 자동 | 데이터 계약 테스트 |
| DATA-09 | 자동 추천 장소는 일반 관광·업종 카테고리가 아니라 자투리 활동 유형과 활동 완결성 검토를 통과한 장소만 사용한다 | 자동 | 관계: 신규(REC-01, DATA-01 보완). 런타임은 `approved 49 + conditional 307 = 356개`의 `scenic_pause / quick_browse / compact_culture / quick_rest` 활동 카탈로그만 사용한다. 일반 식당·장기 활동·내부 개별 시설은 제외 또는 보류 기록으로 남긴다. `busan-poi-catalog.test.mjs`와 `short-stay-catalog.test.mjs`가 검증한다. |
| DATA-10 | 승인·조건부 장소는 최소·권장·최대 체류 범위와 그 산출 근거를 가지며 `min <= recommended <= max`를 만족한다 | 자동 | 관계: 대체(단일 `dwell`). 직접 AIHub 분포는 권장 범위 근거로, 활동 유형·수동 검토는 최소/최대 활동 상한 근거로 분리한다. 런타임 `shortStay` 정책과 단일 장소 추천 단위 테스트가 우선 사용을 검증한다. |
| DATA-11 | 상권 데이터는 검토 후보 발굴에만 사용하며, 상호·업종·좌표만으로 카페·베이커리·서점·소품샵을 자동 추천하지 않는다 | 부분 충족 | 관계: 신규(DATA-06 보완). 런타임에는 검토 카탈로그에서 활동 유형을 받은 장소만 들어간다. 카페·베이커리 등 `quick_rest`는 운영시간·활동 완결성 근거가 있는 승인 또는 조건부 후보이며, 운영시간 미확인 상태를 추천 통과로 표현하지 않는다. 개별 장소 수동 검토는 계속 필요하다. |
| DATA-12 | 명시된 시장·거리·골목·마을·공원·해변의 내부 장소는 대표 권역을 추천하고, 내부 후보는 대표 장소 ID와 함께 제외 또는 보류한다 | 자동 | 관계: 보완(DATA-04, DATA-09). 활성 런타임 원천에 `scope.type = 내부장소`가 남지 않는지 데이터 계약으로 검사한다. 좌표 근접만으로 관계를 추정하지 않으며, 대표 권역이 없는 내부 후보는 보류·제외 목록으로 추적한다. |
| DATA-13 | 카페·베이커리·서점·소품샵은 대기시간을 추정하지 않고, 파싱 가능한 공식 운영시간과 활동 유형을 통과할 때만 초기 체류 템플릿으로 승인한다 | 부분 충족 | 관계: DATA-11 보완. `행사별/프로그램별/매장별 상이`는 운영시간 근거가 아니며, 대기 없음이라는 표현도 금지한다. 활동 범위 카탈로그는 런타임에 적용했지만, 조건부 후보의 운영시간 미확인 UI·선택 뒤 실제 확인 E2E는 남았다. |
| DATA-14 | 도시공원·공공개방시설 표준데이터는 장소 수 확대만을 위해 런타임 후보에 넣지 않는다 | 충족 | 관계: DATA-09 보완. 큰 공원은 기존 TourAPI·부산 공식 원천을 사용하고, 도시공원은 운영시간·사진·활동 완결성 부족으로 추천 원천에서 제외한다. 공공개방시설은 대표 권역 확인 외 사용하지 않는다. |
| DATA-15 | 전통시장 표준데이터는 내부 점포가 아닌 대표 시장만 추가하며, 기존 장소와 정확한 이름·300m 이내 좌표가 중복될 때만 병합한다 | 부분 충족 | 관계: DATA-12 보완. 추가 시장은 대표 시장 단위와 활동 유형을 충족한 조건부 후보만 런타임에 넣고, 음식 중심 시장 권역은 제외한다. 운영시간 미확인 시장은 권역 시간대 게이트와 카카오맵 확인 안내를 따른다. |
| DATA-16 | 장소 부족 시 활동 유형·좌표·출처가 있는 보류 후보는 `conditional`로 지도 탐색에 포함하되, 운영시간 미확인은 명시와 카카오맵 확인 링크를 제공한다 | 부분 충족 | 관계: DATA-09, DATA-13 보완. 유형 없는 보류와 하드 제외는 승격하지 않는다. 307개 조건부 후보를 런타임에 연결했고, 데이터 계약은 카카오 URL과 상태를 검증한다. 후보 상세의 `운영시간 확인 필요` 표현과 확정 차단 E2E는 남았다. |
| DATA-17 | 운영시간 미확인 시설·카페·권역 후보는 삭제하지 않되 더보기에만 `운영시간 확인 필요`로 구분하고, 시간 내 이용을 보장하거나 대표 추천·자동 확정으로 표현하지 않는다 | 미검증 | 사용자 확인 뒤에도 공식 근거·추천 상태로 승격하지 않고 조건부 상태를 저장한 채 경로·시간만 재계산한다. |
| DATA-18 | 모든 활성 장소는 활동 대응 AI-Hub 카테고리의 최빈 체류시간 `30분`을 권장 체류로 사용한다. 최소는 `20분`, 최대는 문화시설 `120분`·나머지 활동 `60분`으로 통일한다. | 자동 | 관계: 대체(2026-08-23 장소별·카테고리 중앙값 정책). AI-Hub는 30분 단위 기록이며 현행 다섯 활동 대응 카테고리의 최빈값은 모두 30분이다. 20·60·120분은 원본 관측값으로 가장하지 않는 자투리 활동 정책이다. `short-stay-catalog.test.mjs`, `recommendation-data-contract.test.mjs`가 근거 출처와 범위를 검증한다. |
| DATA-19 | 활성 또는 재검토 장소는 `identity`, `placeKind`, `activityEvidence`, `stayEvidence`, `availability`, `accessFriction`, 필드별 원천·확인일을 가진다. | 미착수 | 관계: 신규(기존 approved/conditional 재분류). 누락 필드, 출처 없는 값, 허용하지 않은 enum을 카탈로그 계약 테스트로 차단한다. |
| DATA-25 | 시장·거리·골목·해변·공원 등 권역형 후보는 `representative` / `area_access` / `conditional_visit` 자격을 분리한다. `area_access`는 공식·검토된 권역 접근 시간 또는 공개 야외 접근 근거를 가진 자동 탐색 자격이다. `conditional_visit`는 구조화 운영시간이 없더라도 시장·거리·골목으로 식별되고 시설·개별 점포·도매/새벽 전용·장기 이용 장소를 제외한 경우에만 제품 탐색 창과 사용자 확인 필요 상태를 가진다. 조건부 탐색은 출발/도착 중 가까운 쪽 기준 직선거리 8개 페이지로만 로컬 정렬하며 route/API 호출을 만들지 않는다. 내부 점포·시설 운영시간을 상속하거나 카카오맵 표시 시간을 수집·추정하지 않는다. | 구현 전 | 관계: 대체(DATA-06, 보완(DATA-12, DATA-15~17, REC-29). 시장·거리·야외·시설·근거 없음 fixture로 자격·대표 제외·노출 창·8개 거리 페이지·명시 확인 뒤 상태를 검증한다. |
| DATA-20 | 장소는 `representative_core`, `representative_standard`, `conditional_more`, `hold` 중 하나로만 분류하며, 분류 이유와 누락 근거를 보존한다. | 미착수 | 대표/조건부/보류 fixture를 고정해 분류가 재현되는지 검증한다. |
| DATA-21 | `representative_core`는 장소·권역 단위 활동·체류 근거와 확인된 이용 가능성을, `representative_standard`는 확인된 장소·운영 가능성과 검토된 활동 템플릿 근거를 가져야 한다. | 미착수 | core/standard 경계 fixture와 실제 경로 통과 fixture를 검증한다. |
| DATA-22 | `conditional_more`는 장소 동일성·활동 적합성은 있으나 운영 가능성이 미확인인 경우에만 쓴다. 장소 동일성·활동·체류 근거가 없는 장소는 `hold`다. | 미착수 | 운영시간 미확인, 활동 근거 미확인, 장소 중복 fixture의 분류와 노출 경계를 검증한다. |
| DATA-23 | 권역 장소는 대표 권역의 공식 이용 가능 시간·활동 근거만 사용하며 내부 상점·시설의 운영시간·체류 근거를 상속하지 않는다. 같은 `siteGroupId`의 대표 권역과 내부 장소는 함께 자동 추천하지 않는다. | 미착수 | 시장·거리·내부 점포 fixture와 중복 조합 차단 테스트를 추가한다. |
| DATA-24 | 시설·상권 근거는 90일, 검토된 야외 상시 접근 근거는 365일, 행사 근거는 시작 전 재확인 주기를 넘기면 자동 추천에서 재검토 큐로 이동한다. | 미착수 | 고정 현재 시각 fixture로 기한 경계·행사 종료·재검토 이동을 검증한다. |
| REC-16 | 지도 후보와 장소 상세는 같은 진입·귀환 이동수단 규칙으로 시간 적합성을 판정한다 | 대체됨 | 2026-08-21 `REC-17`, `REC-19`, `REC-20`으로 대체. 수동 진입 수단 선택이 아닌 자동 도보·대중교통 조합과 체류 범위를 사용한다. |

### C. 추천 지도와 화면 UX (UX-01~26)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| UX-01 | iPhone 상태 영역과 앱 버튼·텍스트가 겹치지 않는다 | 수동확인 | 주요 화면 확인. 모든 화면 캡처 회귀 테스트 필요 |
| UX-02 | 시간 설정에서 뒤로가기는 메인으로, 코스 만들기에서 뒤로가기는 시간 설정으로 돌아간다 | 수동확인 | 현재 화면 흐름 확인 |
| UX-03 | 결과 화면은 지도와 하단 장소 시트를 함께 보여준다 | 수동확인 | 시뮬레이터 확인 |
| UX-04 | 시트 상단 핸들 스와이프로 지도 중심·장소 중심 상태를 바꾼다 | 수동확인 | 제스처 수정 후 확인 |
| UX-05 | 시트 내부 스크롤과 시트 높이 조절 제스처가 충돌하지 않는다 | 수동확인 | 현재 확인. 다양한 기기 높이 E2E 필요 |
| UX-06 | 후보 마커를 누르면 지도와 장소 목록이 같은 후보에 포커스된다 | 수동확인 | 마커 선택 확인 |
| UX-07 | 사진 마커·기본 마커·현재 위치 마커는 서로 구분되고, 장소명으로 하단 목록과 대응된다 | 부분 | 관계: 보완(UX-06). 사진 없는 장소는 카카오 SDK 기본 핀, 현재 위치는 빨간 위치 점, 장소명은 핀 위 라벨로 구현하고 계약 테스트를 추가했다. 실기기 지도 렌더링 확인 필요 |
| UX-08 | 선택 마커는 확대되고 지도 가시 영역 중앙에 포커스된다 | 부분 | 구현됨. 접힘/펼침 상태별 실기기 확인 필요 |
| UX-09 | 후보 카테고리 필터는 해당 카테고리만 일관되게 표시한다 | 부분 | 구현됨. 자동 UI 테스트 필요 |
| UX-10 | 빈 후보·API 실패·지도 로드 실패 상태가 사용자에게 명확히 표시된다 | 미검증 | 오류 시나리오 E2E 필요 |
| UX-11 | 입력 단계에서 이동수단을 고정하지 않고, 지도는 시간 안에 성립 가능한 장소를 보여주며 이동수단은 장소 미리보기의 구간 아이콘에서만 조정한다 | 대체됨 | 2026-08-21 `UX-18`로 대체. 장소별 수단 선택도 제거하고 도보·대중교통 자동 조합만 추천에 사용한다. |
| UX-12 | 지도 우측 하단의 현재 위치 버튼은 추천을 시작한 현재 위치 마커로 지도를 다시 이동시킨다 | 부분 | 관계: 신규. 후보 지도 기본 시트에서는 가시 지도 영역의 중앙, 접힌 시트와 코스 확인 지도에서는 전체 지도 중앙으로 이동하도록 WebView 오프셋을 적용했다. 계약 테스트 추가, 실기기 확인 필요 |
| UX-13 | 후보 목록 마지막 장소는 기본·확장 시트에서 하단 바나 보이지 않는 시트 영역에 가려지지 않고 끝까지 스크롤된다 | 부분 | 관계: 보완(UX-05). 시트의 숨김 높이를 목록 끝 여백에 반영했다. 실제 제스처 E2E는 [UIUX 테스트 명세](03_product/UIUX_테스트명세.md)의 UXV-12에서 추가한다. |
| UX-14 | 장소 상세는 카드 선택 전의 기본·확장 시트 높이를 유지한 채 사진·시간·엔진이 정한 구간 정보·코스 확인을 보여 주고, 별도 모달을 열지 않는다 | 부분 | 관계: 대체(수단 선택·장바구니). 상세 열기·닫기 전환, 사진 대체 화면, 카카오맵 확인 링크를 구현했다. E2E 명세는 UXV-26~29, UXV-38~39를 따른다. |
| UX-15 | 후보 필터는 분위기·활동 조합이 아닌 단일 가로 스크롤 카테고리 칩이며, 직관적인 카테고리 이름으로 해당 장소만 표시한다 | 부분 | 관계: 대체(기존 분위기·활동 필터). UI 계약 테스트를 추가했고, E2E 명세는 UXV-15를 따른다. |
| UX-16 | 장소 후보 수는 기본·확장 시트에서 공통으로 보이며 접힘·상세에서는 중복되지 않는다 | 부분 | 관계: 신규. UI 계약 테스트를 추가했고, E2E 명세는 UXV-16을 따른다. |
| UX-17 | 뒤로가기·남은 시간·코스 확인은 기본·접힘 시트에서 지도 상단에 고정되고, 확장 시트는 이보다 높은 레이어로 표시되며 시트 내부에 중복되지 않는다 | 부분 | 관계: 대체(장바구니). UI 계약 테스트를 추가했고, E2E 명세는 UXV-17을 따른다. |
| UX-18 | 첫 결과는 도착 전 여유와 실제 검증된 one-stop 대표 1개·서로 다른 대안 최대 3개를 먼저 보여 준다. 사용자는 10분 기본값의 여유 슬라이더를 조절할 수 있고 후보별 이동수단 버튼을 고르지 않는다. | 부분 | 관계: 기존 1~3곳 출시 표현은 REC-30으로 대체되고 다장소는 배포 후 보류됐다. one-stop 실제 8개 공급 게이트는 통과했으며 상태 변경 E2E는 필요하다. |
| UX-19 | 로그인 → 시간 입력 → 장소 추천 → 카카오맵 길찾기 → 후기 기록이 한 흐름으로 완료되며, 외부 지도 복귀 뒤에도 코스·후기 대상이 보존된다 | 미검증 | 관계: 신규(TEST-04, INT-04 보완). 고정 인증·시각·위치·경로 fixture 및 Linking mock으로 E2E를 작성하고, 실기기에서 카카오맵 복귀를 별도 확인한다. |
| UX-20 | 현재 위치 권한은 `내 위치 사용`을 누른 뒤에만 요청하고, 거절·GPS 실패 시 지도 직접 선택으로 동일한 추천 흐름을 완료할 수 있다 | 부분 | 관계: 신규(UX-18 보완). 권한 요청 시점·지도 직접 선택 경로 UI 계약 테스트를 추가했다. 허용/거절/GPS 실패 fixture E2E가 필요하다. |
| UX-21 | `경로 설정하기`에서 출발지·도착지를 함께 설정한다. 도착지 기본값은 비어 있으며 적용 시 출발지 복귀이고, 사용자는 공통 위치 선택 화면의 지도·검색·현위치로 두 필드를 바꿀 수 있다. 도착 시각은 분 단위로 설정하되 출시 UI에서 현재 시각부터 120분 이내다. | 부분 | 관계: 기존 180분은 REC-30 one-stop 출시 상한으로 대체됐다. 카카오맵식 route setup·빈 도착지 복귀·출발/도착 공통 선택과 120/121분 fixture를 사용한다. |
| UX-22 | 결과는 실제 경로 검증을 통과한 one-stop 대표 1개와 첫 대안 최대 3개를 제공한다. 미검증 single 후보가 남으면 첫 결과가 0건이어도 `다른 장소 더 보기`로 한 번에 새 검증 장소 최대 3개를 기존 목록 뒤에 누적하며 대표·기존 순서를 바꾸거나 전체 결과를 다시 3개로 자르지 않는다. 별도 `운영시간 확인 후 들러볼 곳`은 조건부 시장·거리 정보 확인용이며, 실제 기기 시각 10:00–18:00에서만 거리순 로컬 페이지와 카카오맵 확인을 제공한다. | 부분 | 관계: REC-31이 REC-30의 continuation 비노출 범위를 one-stop에 한해 교체한다. 2-V 엔진, U-ONE-MORE-01 UI와 QA-ONE-MORE-01 자동 게이트를 수락했다. 다장소 continuation은 계속 보류하며 native 누적·상세 왕복은 UIUX 정리 뒤 출시 후보 수동 smoke 1회에 합친다. |
| UX-23 | `기록` 탭은 이번 달 완료 후기를 기준으로 활동 카테고리 비율, 완료한 활동 시간, 완료 장소 수를 보여 준다. 선택·저장·카카오맵 실행만으로는 성취 수치를 올리지 않으며, 기록이 없으면 빈 상태를 보인다. | 부분 | 관계: 신규(UX-19, DB-07 보완). 완료 후기 로컬 저장을 읽는 순수 집계 테스트를 추가했다. Supabase 완료 기록 동기화, 화면 `testID`, 실기기 원그래프·빈 상태 확인이 남았다. |
| UX-24 | 메인에는 `다음 일정 전, 부산을 한 곳 더 만나볼까요?`를, 결과에는 사실 기반 지역·활동 맥락을 표시한다. 완료 기록이 없는 사용자에게 근거 없는 첫 방문·숨은 장소·인기 표현을 쓰지 않는다. | 부분 | 관계: 신규(REC-24 보완). `course-v1-discovery-context.test.ts`가 카탈로그의 지역·활동 근거가 모두 있을 때만 표시하고 금지 문구를 만들지 않음을 검증했다. iOS 실기기에서 긴 문구의 줄바꿈·VoiceOver 읽기 순서는 미확인이다. |
| UX-25 | `경로 설정하기`의 출발/도착 필드는 하나의 위치 선택 화면에서 현위치·앱 내부 기기 위치 중심의 물방울형 중앙 고정 핀·장소/주소 제안 중 하나를 명시 확정한다. 허용된 GPS 출발지는 provider 주소(없으면 행정구역)를 자동 표시한다. 장소/주소는 카카오 단일 제공사의 우선 검색 후 0건일 때만 반대 검색 1회 fallback하며 동시에 호출하지 않는다. 지도 이동 중 위치 라벨 요청은 0회, 핀 확정은 주소 1회와 주소 없음일 때만 지역 1회다. | 구현 보완 필요 | 관계: 보완(UX-20, UX-21). API-S-7, 2글자/400ms debounce, 10분 메모리 TTL·동일 요청 합치기, 공통 route setup payload, 현위치 권한, 기기 중심 지도·해변 좌표 fallback fixture와 실제 기기 확인이 필요하다. |
| UX-26 | 코스 확인 지도는 추천을 검증한 동일 route geometry로 `설정 출발지 → 추천 장소 → 설정 목적지/복귀` 두 구간을 표시한다. 대중교통 leg는 provider 명시 mode의 `도보→대중교통→필요한 환승 구간→마지막 도보`를 순서대로 보존하고 선이 실제 leg 목적지까지 이어져야 한다. 상세 진입 시 새 route 호출·실시간 GPS 재계산·직선 근사는 하지 않는다. 불완전 geometry는 완전한 선처럼 표시하지 않되 추천을 폐기하지 않고 안내·마커·시간표·CTA를 유지한다. 공개 POI 구간 geometry만 기존 TTL 캐시를 쓰고 private 위치 geometry는 영속 저장·로그에 남기지 않는다. | 구현 보완 필요 | 관계: 1차 marker-only 정책을 `DEC-COURSE-GEOMETRY-01`로 교체했고, 운영 smoke에서 하차역→장소 마지막 도보선 누락을 확인해 하위 구간·종점 완결성 계약을 보완했다. API H1/H2/H3 진단→engine/DB/UI fixture→실기기 1건 순서다. |

대체 이력: 기존 UX-11 `지도에서 전체·도보·대중교통·차량 필터와 수단별 가능 상태를 비교`는 2026-08-16에 `장소 미리보기의 구간 아이콘 조정`으로 대체했다. 전역 필터는 코스 전체 수단을 고정하는 인상을 주고, 장소 선택 전 불필요한 판단을 요구해 현재 충족률 계산에서 제외한다.

| UX-27 | 코스 카드를 선택했을 때만 transit geometry와 실제 leg endpoint의 50m 초과 gap을 private Kakao walk로 최대 4개 보충해, 하차지점→추천 장소를 포함한 누락 도보선을 지도에 이어 보인다. 이 보충은 추천·transit 검증·시간·순위를 바꾸지 않고, 기존 익명 Auth 세션을 재사용하며 CAPTCHA를 다시 열지 않는다. private 좌표·geometry는 DB·영속 cache·로그에 남기지 않고, 실패해도 상세·CTA는 유지한다. | 검증 완료 | 관계: UX-26의 `상세 route 0회`를 endpoint walk에 한해 교체한다. 자동 계약과 transit 실기기 1/1에서 하차지점→파란 도보선→장소 marker 연결을 확인했다. |

### D. 코스 확인·진행 (11건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| COURSE-01 | 이번 출시의 대표·대안은 엔진이 실제 검증한 one-stop 코스이며 사용자는 장소를 담거나 빼지 않는다. 2·3곳 자동 조립과 자유 장바구니는 출시 화면에서 사용하지 않는다. 조건부 후보는 명시 확인 뒤에도 대표·대안과 분리한다. | 부분 | 관계: 자유 장바구니와 출시 1~3곳을 REC-30으로 대체했다. 실제 one-stop 8개 공급 게이트는 통과했으며 최종 출시 smoke가 남았다. |
| COURSE-02 | 시간·공간·운영시간 조건을 통과하지 못한 후보는 이유를 표시하고 대표 코스 또는 조건부 수동 포함 코스에 넣지 못하게 한다. | 미검증 | 시간 초과·운영시간 미확인 확인 전/후·경로 실패 fixture가 필요하다. |
| COURSE-03 | 출시 코스 확인은 상단 실제 경로 지도와 하단 `출발지 → 실제 이동 → 추천 장소 → 실제 이동 → 도착/복귀 → 도착 여유`의 세로 순서로 한 곳 코스를 보여 주며 자유 장바구니를 제공하지 않는다. 대중교통 이동은 명시된 도보·대중교통 하위 구간을 실제 순서대로 표시하고 마지막 선이 장소/도착지에 닿아야 한다. 지도선은 추천 검증 snapshot만 사용하고, geometry가 없거나 종점에 닿지 않으면 직선·근사선을 그리지 않는다. 장소 단계의 선택 체류는 `둘러보기 약 N분`, short는 `가볍게 둘러보기 약 N분`으로만 표시한다. | 구현 보완 필요 | 관계: 2026-09-03 `UX-26`이 기존 marker-only 범위를 교체했고 운영 smoke의 하차역 종점 누락으로 완결성 조건을 추가했다. provider 구조 진단, subsegment fixture, 두 실제 leg·왕복/도착지·상세 route 0과 iOS 한 건 확인이 필요하다. |
| COURSE-04 | 코스 선택 시 현재 시간 코스는 저장 후 바로 길찾기 시작, 미래 코스는 내 코스에 저장한다 | 부분 | 흐름 구현. 시간 분기 E2E 필요 |
| COURSE-05 | 코스 진행 화면의 뒤로가기는 내 코스 탭으로 이동한다 | 수동확인 | 화면 흐름 확인 |
| COURSE-06 | 진행 중 코스는 취소·삭제할 수 있다 | 수동확인 | 내 코스 삭제 UI 확인 |
| COURSE-07 | 카카오맵 길찾기에서 현재 위치를 출발점, 다음 장소를 목적지로 전달한다 | 부분 | 웹·실기기 외부 전환 회귀 테스트 필요 |
| COURSE-08 | 진행 중 현재 위치·현재 시각으로 남은 다장소 코스를 재조립하는 기능은 이번 one-stop 출시 범위에 포함하지 않는다. 향후 재개해도 사용자가 임의로 장소를 추가·삭제하는 자유 장바구니로 되돌리지 않는다. | 배포 후 보류 | 다장소 public store와 진행 상태 저장 계약이 안정된 뒤 별도 결정한다. |
| COURSE-09 | 사용자는 코스 구간의 이동수단을 선택하지 않는다. 엔진은 도보·대중교통의 현실적인 구간 조합만으로 코스를 조립·정밀 검증하고, 길찾기 화면은 결정된 구간별 수단·소요시간·다음 목적지를 보여 준다. | 미검증 | 관계: 대체(사용자 구간 수단 선택 COURSE-09). 차량 전용 후보 제외와 도보/대중교통 혼합 fixture가 필요하다. |
| COURSE-10 | 결과의 대표·대안 요약 카드 전체를 누르면 선택한 원본 one-stop snapshot의 코스 확인이 상단 지도와 첫 세로 구간부터 열린다. 뒤로가기는 결과의 선택·스크롤 상태를 보존하며 카드 tap·상세 렌더에서 추천/route API를 다시 호출하지 않는다. | 구현 전환 필요 | 관계: 기존 별도 `추천 확인` 버튼과 자유 장바구니 전환을 대체한다. 카드 전체 tap·원본 snapshot identity·뒤로가기·호출 0 fixture가 필요하다. |
| COURSE-11 | COURSE-10의 카드 tap·상세 route 0회는 **추천과 transit 재조회 0회**로 범위를 줄인다. 선택한 one-stop의 transit leg에서만 50m 초과로 끊긴 시작·종점 도보 connector를 합계 최대 4개 조회한다. 같은 코스의 연타·재렌더·뒤로갔다 재진입은 세션 메모리 결과를 재사용하고, 성공한 walk geometry만 파란 실선으로 기존 transit 선 앞·뒤에 붙인다. | 검증 완료 | 관계: UX-27 보완. 자동 통합과 실기기 1회가 통과했으며 추천 결과·시간·진행 snapshot은 불변이다. |

### E. 인증·DB·개인화 기반 (7건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| DB-01 | 이메일·비밀번호 회원가입과 이메일 인증 후 로그인이 가능하다 | 수동확인 | Supabase 테스트 계정으로 확인 |
| DB-02 | 로그인 사용자는 코스를 저장하고 내 코스에서 불러올 수 있다 | 수동확인 | 실제 저장·조회 확인 |
| DB-03 | 사용자별 RLS 때문에 다른 사용자의 코스를 읽거나 수정할 수 없다 | 부분 | 정책 적용. 2계정 권한 테스트 필요 |
| DB-04 | 코스에는 출발·약속·시간·이동수단·장소 순서·구간 시간·체류시간 스냅샷이 저장된다 | 부분 | 저장 구현. DB 행 구조 자동 검증 필요 |
| DB-05 | 코스와 장소를 삭제하면 사용자 데이터도 삭제된다 | 미검증 | 삭제·cascade·복원 불가 E2E 필요 |
| DB-06 | 생년월일·필수 약관·개인정보 동의가 가입 과정에서 검증된다 | 미검증 | UI·DB 구현 전 |
| DB-07 | 후기·행동 데이터는 계정 삭제 시 함께 삭제되며, 이후 개인화 근거로만 사용한다 | 미검증 | 후기/계정삭제 기능 구현 전 |

### F. 알림·외부 연동·실기기 (4건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| INT-01 | 첫 실행 시 알림 권한을 요청하고, 코스 종료·출발 5분 전 알림을 예약한다 | 수동확인 | 알림 수신 확인 |
| INT-02 | 코스를 취소·삭제하면 예약 알림도 취소된다 | 부분 | 구현됨. 알림센터 확인 시나리오 필요 |
| INT-03 | TMAP·ODsay 호출량 카운터는 날짜 기준으로 유지되고 터미널 로그에 출력된다 | 수동확인 | 호출 누적 확인 |
| INT-04 | 카카오맵 외부 전환·앱 복귀, 권한 거절, 네트워크 실패는 실기기에서 안전하게 처리된다 | 미검증 | 실기기 E2E 및 수동 시나리오 필요 |

### G. 테스트 기반 자체 (4건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| TEST-01 | 타입 오류, 데이터 계약, UI 상태 계산을 자동 검사한다 | 자동 | `npm run test:typecheck`, `npm test`, `npm run test:ui` |
| TEST-02 | 추천 우회율 하드 필터가 재도입되지 않는다 | 자동 | `test/planner-policy.test.mjs` |
| TEST-03 | 추천 자동진단은 후보 부족의 병목을 시간·최소 체류·운영시간·코스 조립으로 구분한다 | 부분 | 스크립트 반영. 고정 입력 리포트 재생성 필요 |
| TEST-04 | 모바일 E2E는 고정 시각·위치·외부 API 응답을 주입해 반복 가능하다 | 부분 | 관계: 보완. 개발+exact diagnostics 전용 8개 launcher와 무호출 fixture의 입력 교차 계약, 실제 Proxy 8개 실행을 완료했다. 결과 화면은 확보했지만 다른 Metro 연결 때문에 구조화 receipt를 회수하지 못했고 CourseConfirm 복원 1건이 남았다. 두 항목은 출시 후보 단일 smoke로 확인하며 8개 전체를 반복하지 않는다. |

## 4. 다음 자동화 우선순위

1. `REC-17`~`REC-20`, `REC-23`~`REC-24`, `DATA-17`: 1~3곳 코스·도착 여유·체류 범위·운영시간 미확인·완료 이력 기반 발견성 정책을 고정 좌표·시각·이동시간으로 단위 테스트한다.
2. `UX-18`, `UX-19`, `TEST-04`: 시간 설정부터 후기까지의 고정 fixture E2E와 외부 지도 복귀 상태를 만든다.
3. `DB-03`~`DB-05`: 두 계정·삭제·RLS를 Supabase 테스트 데이터로 검증한다.
4. `UX-01`~`UX-10`, `INT-04`: `testID`와 고정 테스트 모드를 만든 뒤 모바일 E2E를 도입한다.

## 5. 실행 기록

| 날짜 | 대상 | 결과 |
|---|---|---|
| 2026-08-15 | `npm run test:typecheck` | 통과 |
| 2026-08-15 | `npm test` | 21개 통과. 코스 변경 RPC·현재 위치/현재 시각 재추천 계약 테스트 포함 |
| 2026-08-15 | `supabase migration list --db-url "$DATABASE_URL"` | 로컬·원격 모두 `202608150009`까지 일치 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 23개 통과. 이동수단 선입력 제거·지도 필터·수단별 상태 UI 계약 테스트 추가 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 26개 통과. 도보·대중교통 자동 혼합 구간, 정밀 검증, DB·카카오 길찾기 수단 전달 계약 테스트 추가 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 27개 통과. 장소 추가 직전 수단 시나리오 선택과 선택 도착 구간 저장 계약 테스트 추가 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 27개 통과. 장소 미리보기의 아이콘 경로·약속 전 여유·체류 가능 시간·구간 수단 변경 UI 계약 갱신 |
| 2026-08-16 | `npm run test:typecheck && npm test` | 28개 통과. 사진 없는 장소의 카카오 기본 핀과 현재 위치 전용 마커 계약 테스트 추가 |
| 2026-08-16 | `npm run test:typecheck && npm test` | 31개 통과. 지도 후보의 세 이동수단 OR 통과 조건, 후보·장바구니 이동수단 규칙 통일, 카카오 보조시설 오매칭 차단, 종교시설 자연관광지 폴백 보류 계약을 갱신했다. |
| 2026-08-16 | `npm run test:typecheck && npm test` | 29개 통과. 후보·장바구니 지도 현재 위치 재중심 이동 계약 테스트 추가 |
| 2026-08-16 | `npm run test:typecheck && npm test` | 29개 통과. 지도 전역 이동수단 필터 제거와 장소 미리보기 구간 조정 계약 테스트로 갱신 |
| 2026-08-15 | `npm test` | 18개 통과 |
| 2026-08-15 | 경성대 스타벅스 → 더바이널, 180분 | 도보 후보 31개 → 시간·최소 체류 31개 → 운영시간 통과 7개 → 코스 10개. 가까운 약속장소에서도 후보가 사라지지 않음 확인 |
| 2026-08-15 | 시뮬레이터 GPS: 스타벅스 경성대점 → 더바이널, 180분 | 가상 위치를 `35.137383, 129.100666`으로 설정해 검증. 장소 검색 좌표와 동일하게 후보 31개·운영시간 통과 7개·코스 10개 생성 |
| 2026-08-15 | 코스 변경 DB RPC | `replace_course_plan` 마이그레이션을 원격 Supabase에 적용하고 함수 존재 확인. 최초 시작·약속 도착 시각을 보존하는 계약 테스트 추가 |
| 2026-08-16 | `npm run test:typecheck && npm run test:ui && npm test` | 통과. 추천 시트 레이아웃·후보 평가·장바구니 코스·장소 상세·지도 제어·진행 일정·진행 화면·지도 경로 세그먼트의 순수/계약 테스트 23건과 전체 계약 테스트 45건을 확인했다. |
| 2026-08-16 | `npx expo export --platform ios --output-dir /private/tmp/timefit-ios-export` | 통과. iOS JavaScript 번들을 실제 생성해 TypeScript 외의 모듈 연결도 확인했다. |
| 2026-08-16 | `npm run test:typecheck && npm run test:ui && npm test` | 통과. 약속이 있는 후보 수집을 출발지·약속지 생활권과 경로 축으로 제한했고, UI 상태 테스트 25건과 전체 계약 테스트 45건을 확인했다. |
| 2026-08-17 | `npx tsx --test test/ui/actual-route-search-scope.test.ts && npm run test:typecheck` | 통과. 실제 경로 기반 범위 모듈의 왕복·우회 경로·수단 합집합·전체 실패·반경 정규화 fixture 5건을 추가했다. 플래너 연결 전 단계다. |
| 2026-08-17 | `npx tsx --test test/ui/route-baseline-service.test.ts && npm run test:typecheck` | 통과. 도보·차량·대중교통 기준 경로의 성공 캐시, 동시 요청 공유, TTL, 부분 실패 재시도, 전체 실패 미저장 fixture 6건을 추가했다. 플래너 연결 전 단계다. |
| 2026-08-17 | `npm run test:typecheck && npm run test:ui && npm test && npx expo export --platform ios` | 통과. 플래너 후보 수집을 실제 기준 경로 범위로 교체하고 기존 직선 범위 모듈을 제거했다. UI 34건·계약 45건과 iOS JavaScript 번들을 확인했다. |
| 2026-08-17 | `npx expo install @react-native-community/slider && pod install` | 완료. 기본 1km·수동 최대 8km 탐색 반경 슬라이더를 추가하고 iOS 네이티브 모듈을 연결했다. |
| 2026-08-22 | `npm run test:typecheck && npm run test:ui && npm test && git diff --check` | 통과. 단일 장소 결과·상세·확인 화면이 공통 `TimeJourney` 모델로 이동·활동·도착 전 여유를 같은 시간 예산에서 표시하는지 UI 50건·계약 48건으로 확인했다. iOS 시각 수동 확인은 남아 있다. |
| 2026-08-23 | `DWELL_ONLY=1 node scripts/build_dwell.mjs && node scripts/build_short_stay_catalog.mjs && node scripts/build_runtime_poi_catalog.mjs && npm run test:typecheck && npm run test:ui && npm test && git diff --check` | 통과. AI-Hub 활동 대응 카테고리의 최빈 체류시간 30분을 권장값으로 사용하고, 356개 활성 장소를 최소 20분·최대 60분(문화시설 120분)으로 재생성했다. UI 50건·계약 49건을 확인했다. |
| 2026-08-23 | `npm run test:typecheck && npm run test:ui && npm test && pod install && npx expo export --platform ios` | 통과. 완료 후기 기반 이번 달 활동 시간·장소 수·카테고리 비율 집계 UI 테스트 2건을 추가해 UI 52건·계약 49건을 확인했다. `react-native-svg`의 iOS Pod `RNSVG` 연결과 iOS JavaScript 번들도 확인했다. |
| 2026-08-23 | `npm run test:typecheck && npm run test:ui && npm test && git diff --check` | 통과. 대표·새 추천·상세 장소만 실제 도보/대중교통 2구간을 정밀 계산하고 동일 요청을 캐시하는 서비스 fixture 4건, 정밀 성공 수단 제한 fixture 1건, 기본 1km·명시적 확장 후보 범위 fixture 1건을 추가했다. UI 58건·계약 49건을 확인했다. |
| 2026-08-23 | `npx tsx --test test/ui/time-setup-clock.test.ts && npm run test:typecheck` | 통과. 개발 빌드 전용 현재 시각 주입과 2시간 종료 시각 재설정·자정 상한 fixture 2건을 추가했다. 실제 요일·위치·API fixture를 포함한 E2E는 후속 작업이다. |
