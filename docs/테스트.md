# TimeFit 테스트 현황

> 기준일: 2026-08-15 16:50 KST  
> 목적: 사용자가 확정한 동작 요구사항을 단일 목록으로 관리하고, 구현 여부가 아니라 **검증 근거** 기준으로 테스트 충족률을 갱신한다.

## 1. 갱신 규칙

- 사용자가 새 요구사항이나 이상 사례를 말하면 먼저 이 문서에 ID와 기대 결과를 추가한다.
- 새 요구사항을 등록하기 전, 기존 ID와 비교해 관계를 `동일`, `보완`, `충돌`로 판정한다.
  - `동일`: 새 항목을 만들지 않고 기존 ID의 근거·테스트 시나리오만 갱신한다.
  - `보완`: 기존 ID를 유지하고 세부 조건을 추가한다. 기존 결과에 영향을 주면 변경 내용을 함께 기록한다.
  - `충돌`: 기존 규칙을 임의로 덮어쓰지 않는다. 사용자에게 기존 유지·새 규칙으로 대체·두 규칙 병행 중 선택을 물은 뒤, 확정된 방향만 반영한다.
- 대체된 요구사항은 삭제하지 않고 `대체됨` 상태와 대체한 ID·결정일을 남긴다. 현재 적용되는 요구사항만 충족률에 포함한다.
- 구현 전에는 `미검증`, 테스트 코드가 실패를 재현하면 `부분`, 자동 테스트 또는 재현 가능한 수동 시나리오가 통과하면 상태를 갱신한다.
- 추천·데이터·DB 정책을 변경한 커밋에는 관련 ID, 실행 명령, 결과를 함께 기록한다.
- 퍼센트는 코드 라인 수가 아니라 **요구사항 수** 기준이다. 수동 확인은 기능 확인이지 자동 회귀 방지는 아니므로 자동화율과 분리한다.

| 상태 | 의미 |
|---|---|
| `자동` | CI/로컬 명령으로 반복 검증 가능 |
| `수동확인` | 시뮬레이터 또는 실기기에서 사용자가 확인 |
| `부분` | 구현은 있으나 핵심 분기·실기기·회귀 검증이 부족 |
| `미검증` | 요구사항은 확정됐지만 검증 근거가 없음 |
| `대체됨` | 이후 확정된 요구사항으로 더 이상 적용하지 않음. 충족률 계산에서 제외 |

### 요구사항 등록 형식

```text
ID: REC-XX
관계: 신규 | 동일(REC-XX) | 보완(REC-XX) | 충돌(REC-XX)
기대 결과: 사용자가 확인 가능한 한 문장
검증 방식: 단위 | 통합 | E2E | 수동
결정: 유지 | 대체 | 병행 (충돌일 때 사용자 확정 후 기록)
```

## 2. 현재 요약

| 지표 | 요구사항 수 | 비율 |
|---|---:|---:|
| 자동 검증 완료 | 13 / 57 | **23%** |
| 수동 확인 완료 | 19 / 57 | **33%** |
| 확인 완료 합계 | 32 / 57 | **56%** |
| 부분 검증 | 19 / 57 | 33% |
| 미검증 | 6 / 57 | 11% |

**현재 검증 충족률은 56%**다. 기능 구현률이 아니라 자동 또는 수동으로 기대 결과를 확인한 비율이다. 공모전 제출 전 목표는 `확인 완료 90% 이상`, 그중 핵심 흐름 자동화 `50% 이상`이다.

## 3. 요구사항 목록

### A. 추천 엔진과 시간 안전성 (13건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| REC-01 | 입력 시간은 1~240분 범위이며 종료 시각이 시작보다 뒤여야 한다 | 수동확인 | 시간 설정 화면에서 확인. 자동 입력 경계 테스트 필요 |
| REC-02 | 지도 후보는 도보·대중교통·차량 중 하나 이상의 진입 수단으로 최소 체류·안전 여유를 충족할 때만 표시한다 | 부분 | 관계: 대체(사전 수단 선택). 후보·상세·장바구니가 같은 구간 수단 규칙을 쓰는 경계 테스트를 추가해야 한다. |
| REC-03 | 후보는 출발지 근처·약속지 근처·중간 경로 근처에서 함께 수집한다 | 부분 | `searchCenters()` 구현. 전략별 후보가 나오는 고정 입력 테스트 필요 |
| REC-04 | 약속이 가까워도 시간 안의 후보를 우회율 때문에 제거하지 않는다 | 자동 | `planner-policy.test.mjs` 및 경성대 사례 호출 재현 완료 |
| REC-05 | 후보 상태는 최소 체류·수단별 안전 여유를 하드 조건으로 하고, 추가 여유 10분으로 여유·짧게 가능·빠듯을 구분한다 | 부분 | 관계: 보완. 최소 체류 통과 테스트는 있으나 10분 상태 경계 자동 테스트가 필요하다. |
| REC-06 | 시설형·유료 성격 장소는 추가 이동보다 체류가 짧으면 제외한다 | 부분 | 정책 구현. 카페·식당 경계 테스트 필요 |
| REC-07 | 자연·거리·광장 성격 장소는 짧은 접근 시 15~20분 체류를 허용한다 | 부분 | 정책 구현. 거리별 경계 테스트 필요 |
| REC-08 | 이동 효율은 하드 제외가 아니라 전체 자투리 예산 대비 추가 이동 비중으로 순위에만 반영한다 | 자동 | `routeMovementOverhead()` 및 정책 테스트 |
| REC-09 | 후보 목록은 근사 이동시간, 코스 확정은 TMAP·ODsay 실제 이동시간으로 재검증한다 | 부분 | 구현됨. 실제 경로 오차·확정 차단 E2E 필요 |
| REC-10 | 3곳 이상도 시간 안이면 장바구니에 담을 수 있다 | 수동확인 | 시뮬레이터에서 확인. 자동 장바구니 계산 테스트 필요 |
| REC-11 | 장소 순서를 자동 정렬하고 사용자가 바꾸면 시간 적합성을 다시 계산한다 | 수동확인 | 화면 확인. 순서 변경 경계 테스트 필요 |
| REC-12 | 같은 `siteGroupId`의 포괄 장소와 내부 장소는 함께 담을 수 없다 | 자동 | 데이터 계약 테스트 및 엔진 조합 규칙 |
| REC-13 | GPS로 받은 부산 좌표도 장소 검색 좌표와 동일하게 출발지로 사용해 후보를 생성한다 | 수동확인 | 관계: 보완(REC-03). 시뮬레이터 GPS를 스타벅스 경성대점으로 설정한 뒤 180분 도보 추천에서 후보 31개·운영시간 통과 7개·코스 10개 확인. 자동 UI 테스트 필요 |

### B. 장소 데이터·운영시간 신뢰도 (8건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| DATA-01 | 추천 카테고리는 자연관광지·상업지구·레저/스포츠·문화시설·식당·카페만 사용한다 | 자동 | `recommendation-data-contract.test.mjs` |
| DATA-02 | 숙박·교통·의료·주차 시설은 추천 데이터에서 제외한다 | 자동 | 데이터 계약 테스트 |
| DATA-03 | 이름 차이 중복 장소는 대표 장소로 병합한다 | 자동 | 데이터 계약 테스트 |
| DATA-04 | 포괄 장소와 내부 장소는 데이터상 유지하되 그룹으로 묶는다 | 자동 | 데이터 계약 테스트 |
| DATA-05 | 카카오에서 확인되지 않은 장소는 자동 추천에서 제외한다 | 자동 | 데이터 계약 테스트 |
| DATA-06 | 시설형·권역형 장소는 신뢰할 수 있는 운영시간 근거가 없으면 자동 추천하지 않는다 | 자동 | 권역 시간대 정책 테스트 |
| DATA-07 | 새벽에는 검토된 야외 상시 접근 장소 외 시설·상권을 추천하지 않는다 | 자동 | 자연관광 성격 게이트 테스트 |
| DATA-08 | 카카오 보조시설 결과는 `weak`이고 잘못된 상세 링크를 제공하지 않는다 | 자동 | 데이터 계약 테스트 |
| REC-06 | 지도 후보와 장소 상세는 같은 진입·귀환 이동수단 규칙으로 시간 적합성을 판정한다 | 자동 | 추천·혼합 이동 계약 테스트 |

### C. 추천 지도와 화면 UX (12건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| UX-01 | iPhone 상태 영역과 앱 버튼·텍스트가 겹치지 않는다 | 수동확인 | 주요 화면 확인. 모든 화면 캡처 회귀 테스트 필요 |
| UX-02 | 시간 설정에서 뒤로가기는 메인으로, 코스 만들기에서 뒤로가기는 시간 설정으로 돌아간다 | 수동확인 | 현재 화면 흐름 확인 |
| UX-03 | 결과 화면은 지도와 하단 장소 시트를 함께 보여준다 | 수동확인 | 시뮬레이터 확인 |
| UX-04 | 시트 상단 핸들 스와이프로 지도 중심·장소 중심 상태를 바꾼다 | 수동확인 | 제스처 수정 후 확인 |
| UX-05 | 시트 내부 스크롤과 시트 높이 조절 제스처가 충돌하지 않는다 | 수동확인 | 현재 확인. 다양한 기기 높이 E2E 필요 |
| UX-06 | 후보 마커를 누르면 지도와 장소 목록이 같은 후보에 포커스된다 | 수동확인 | 마커 선택 확인 |
| UX-07 | 사진 마커·기본 마커·현재 위치 마커는 서로 구분되고, 장소명으로 하단 목록과 대응된다 | 부분 | 관계: 보완(UX-06). 사진 없는 장소는 카카오 SDK 기본 핀, 현재 위치는 빨간 위치 점, 장소명은 핀 위 라벨로 구현하고 계약 테스트를 추가했다. 실기기 지도 렌더링 확인 필요 |
| UX-08 | 선택 마커는 확대되고 지도 가시 영역 중앙에 포커스된다 | 부분 | 구현됨. 접힘/펼침 상태별 실기기 확인 필요 |
| UX-09 | 후보 카테고리 필터는 해당 카테고리만 일관되게 표시한다 | 부분 | 구현됨. 자동 UI 테스트 필요 |
| UX-10 | 빈 후보·API 실패·지도 로드 실패 상태가 사용자에게 명확히 표시된다 | 미검증 | 오류 시나리오 E2E 필요 |
| UX-11 | 입력 단계에서 이동수단을 고정하지 않고, 지도는 시간 안에 성립 가능한 장소를 보여주며 이동수단은 장소 미리보기의 구간 아이콘에서만 조정한다 | 부분 | 관계: 대체(2026-08-16, 기존 지도 전역 이동수단 필터). 상단 필터·수단별 후보 상태 계산을 제거했고, 마커 선택 시 아이콘 경로와 `약속 전 여유 / 체류 가능` 지표를 먼저 표시한다. 실기기에서 기본 경로, 세 수단 변경, 취소 동작 확인 필요 |
| UX-12 | 지도 우측 하단의 현재 위치 버튼은 추천을 시작한 현재 위치 마커로 지도를 다시 이동시킨다 | 부분 | 관계: 신규. 후보 지도 기본 시트에서는 가시 지도 영역의 중앙, 접힌 시트와 장바구니 지도에서는 전체 지도 중앙으로 이동하도록 WebView 오프셋을 적용했다. 계약 테스트 추가, 실기기 확인 필요 |

대체 이력: 기존 UX-11 `지도에서 전체·도보·대중교통·차량 필터와 수단별 가능 상태를 비교`는 2026-08-16에 `장소 미리보기의 구간 아이콘 조정`으로 대체했다. 전역 필터는 코스 전체 수단을 고정하는 인상을 주고, 장소 선택 전 불필요한 판단을 요구해 현재 충족률 계산에서 제외한다.

### D. 장바구니·코스 진행 (9건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| COURSE-01 | 후보를 담거나 빼면 남은 시간과 추가 가능 여부가 즉시 갱신된다 | 수동확인 | 장바구니 테스트 완료 |
| COURSE-02 | 시간 초과 장소는 이유를 표시하고 추가를 막는다 | 부분 | 구현됨. 고정 데이터 경계 테스트 필요 |
| COURSE-03 | 장바구니는 지도 경로와 선택 장소 목록을 분리해 스크롤한다 | 수동확인 | 시뮬레이터 확인 |
| COURSE-04 | 코스 선택 시 현재 시간 코스는 저장 후 바로 길찾기 시작, 미래 코스는 내 코스에 저장한다 | 부분 | 흐름 구현. 시간 분기 E2E 필요 |
| COURSE-05 | 코스 진행 화면의 뒤로가기는 내 코스 탭으로 이동한다 | 수동확인 | 화면 흐름 확인 |
| COURSE-06 | 진행 중 코스는 취소·삭제할 수 있다 | 수동확인 | 내 코스 삭제 UI 확인 |
| COURSE-07 | 카카오맵 길찾기에서 현재 위치를 출발점, 다음 장소를 목적지로 전달한다 | 부분 | 웹·실기기 외부 전환 회귀 테스트 필요 |
| COURSE-08 | 진행 중 사용자는 현재 위치·현재 시각·고정 약속을 기준으로 남은 장소를 교체·삭제·추가해 코스를 다시 구성할 수 있다 | 부분 | 관계: 신규(코스 진행 변경). 진행 화면의 `코스 변경`은 GPS·현재 시각으로 후보를 다시 만들고, 확정 시 같은 `courseId`의 현재 계획을 원자적으로 교체한다. 최초 시작·약속 도착 시각은 유지하며 마지막 변경 시각만 저장한다. 실기기 GPS·장바구니 확정·앱 재시작 E2E 필요 |
| COURSE-09 | 사용자는 장소를 담기 직전에 해당 도착 구간의 이동수단을 고르고, 나머지 구간은 자동 조합한다. 마지막 장소→약속은 도보 약 14분 이하면 도보, 초과하면 대중교통이며 차량은 자동 선택하지 않는다. 길찾기 화면은 구간별 수단·소요시간·다음 목적지를 명확히 보여 준다. | 부분 | 관계: 보완(COURSE-07, COURSE-08). 선택한 도착 수단을 장소별로 저장하고, 선택 순서는 유지한다. 자동 혼합 경로·정밀 검증·구간별 안내 UI 계약 테스트를 추가한다. 서면→인근 장소(도보)→해운대(대중교통) 실기기 E2E 필요 |

### E. 인증·DB·개인화 기반 (7건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| DB-01 | 이메일·비밀번호 회원가입과 이메일 인증 후 로그인이 가능하다 | 수동확인 | Supabase 테스트 계정으로 확인 |
| DB-02 | 로그인 사용자는 코스를 저장하고 내 코스에서 불러올 수 있다 | 수동확인 | 실제 저장·조회 확인 |
| DB-03 | 사용자별 RLS 때문에 다른 사용자의 코스를 읽거나 수정할 수 없다 | 부분 | 정책 적용. 2계정 권한 테스트 필요 |
| DB-04 | 코스에는 출발·약속·시간·이동수단·장소 순서·구간 시간·체류시간 스냅샷이 저장된다 | 부분 | 저장 구현. DB 행 구조 자동 검증 필요 |
| DB-05 | 코스와 장소를 삭제하면 사용자 데이터도 삭제된다 | 미검증 | 삭제·cascade·복원 불가 E2E 필요 |
| DB-06 | 생년월일·필수 약관·개인정보 동의가 가입 과정에서 검증된다 | 미검증 | UI·DB 구현 전 |
| DB-07 | 후기·행동 데이터는 계정 삭제 시 함께 삭제되며, 이후 개인화 근거로만 사용한다 | 미검증 | 후기/계정삭제 기능 구현 전 |

### F. 알림·외부 연동·실기기 (4건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| INT-01 | 첫 실행 시 알림 권한을 요청하고, 코스 종료·출발 5분 전 알림을 예약한다 | 수동확인 | 알림 수신 확인 |
| INT-02 | 코스를 취소·삭제하면 예약 알림도 취소된다 | 부분 | 구현됨. 알림센터 확인 시나리오 필요 |
| INT-03 | TMAP·ODsay 호출량 카운터는 날짜 기준으로 유지되고 터미널 로그에 출력된다 | 수동확인 | 호출 누적 확인 |
| INT-04 | 카카오맵 외부 전환·앱 복귀, 권한 거절, 네트워크 실패는 실기기에서 안전하게 처리된다 | 미검증 | 실기기 E2E 및 수동 시나리오 필요 |

### G. 테스트 기반 자체 (4건)

| ID | 충족 요건 | 상태 | 근거 / 다음 조치 |
|---|---|---|---|
| TEST-01 | 타입 오류와 데이터 계약을 자동 검사한다 | 자동 | `npm run test:typecheck`, `npm test` |
| TEST-02 | 추천 우회율 하드 필터가 재도입되지 않는다 | 자동 | `test/planner-policy.test.mjs` |
| TEST-03 | 추천 자동진단은 후보 부족의 병목을 시간·최소 체류·운영시간·코스 조립으로 구분한다 | 부분 | 스크립트 반영. 고정 입력 리포트 재생성 필요 |
| TEST-04 | 모바일 E2E는 고정 시각·위치·외부 API 응답을 주입해 반복 가능하다 | 미검증 | 도구 선정, `testID`, API 어댑터 분리 필요 |

## 4. 다음 자동화 우선순위

1. `REC-02`, `REC-03`, `REC-06`, `REC-07`, `REC-09`: 추천 엔진을 고정 좌표·시각·이동시간으로 단위 테스트한다.
2. `COURSE-01`, `COURSE-02`, `COURSE-04`: 장바구니 시간 계산을 화면에서 분리해 단위 테스트한다.
3. `DB-03`~`DB-05`: 두 계정·삭제·RLS를 Supabase 테스트 데이터로 검증한다.
4. `UX-01`~`UX-10`, `INT-04`: `testID`와 고정 테스트 모드를 만든 뒤 모바일 E2E를 도입한다.

## 5. 실행 기록

| 날짜 | 대상 | 결과 |
|---|---|---|
| 2026-08-15 | `npm run test:typecheck` | 통과 |
| 2026-08-15 | `npm test` | 21개 통과. 코스 변경 RPC·현재 위치/현재 시각 재추천 계약 테스트 포함 |
| 2026-08-15 | `supabase migration list --db-url "$DATABASE_URL"` | 로컬·원격 모두 `202608150009`까지 일치 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 23개 통과. 이동수단 선입력 제거·지도 필터·수단별 상태 UI 계약 테스트 추가 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 26개 통과. 도보·대중교통 자동 혼합 구간, 정밀 검증, DB·카카오 길찾기 수단 전달 계약 테스트 추가 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 27개 통과. 장소 추가 직전 수단 시나리오 선택과 선택 도착 구간 저장 계약 테스트 추가 |
| 2026-08-15 | `npm run test:typecheck && npm test` | 27개 통과. 장소 미리보기의 아이콘 경로·약속 전 여유·체류 가능 시간·구간 수단 변경 UI 계약 갱신 |
| 2026-08-16 | `npm run test:typecheck && npm test` | 28개 통과. 사진 없는 장소의 카카오 기본 핀과 현재 위치 전용 마커 계약 테스트 추가 |
| 2026-08-16 | `npm run test:typecheck && npm test` | 31개 통과. 지도 후보의 세 이동수단 OR 통과 조건, 후보·장바구니 이동수단 규칙 통일, 카카오 보조시설 오매칭 차단, 종교시설 자연관광지 폴백 보류 계약을 갱신했다. |
| 2026-08-16 | `npm run test:typecheck && npm test` | 29개 통과. 후보·장바구니 지도 현재 위치 재중심 이동 계약 테스트 추가 |
| 2026-08-16 | `npm run test:typecheck && npm test` | 29개 통과. 지도 전역 이동수단 필터 제거와 장소 미리보기 구간 조정 계약 테스트로 갱신 |
| 2026-08-15 | `npm test` | 18개 통과 |
| 2026-08-15 | 경성대 스타벅스 → 더바이널, 180분 | 도보 후보 31개 → 시간·최소 체류 31개 → 운영시간 통과 7개 → 코스 10개. 가까운 약속장소에서도 후보가 사라지지 않음 확인 |
| 2026-08-15 | 시뮬레이터 GPS: 스타벅스 경성대점 → 더바이널, 180분 | 가상 위치를 `35.137383, 129.100666`으로 설정해 검증. 장소 검색 좌표와 동일하게 후보 31개·운영시간 통과 7개·코스 10개 생성 |
| 2026-08-15 | 코스 변경 DB RPC | `replace_course_plan` 마이그레이션을 원격 Supabase에 적용하고 함수 존재 확인. 최초 시작·약속 도착 시각을 보존하는 계약 테스트 추가 |
