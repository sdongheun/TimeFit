# 외부 API 현재 작업

현재 활성 구현 작업은 없다. 새 작업 전에는 `AGENTS.md`, `docs/README.md`, `docs/작업조정_보드.md`, `docs/04_backend/외부API_작업기록.md`의 현재 anchor만 읽는다.

Route Proxy·Kakao cache·CAPTCHA의 과거 기록은 `docs/04_backend/외부API_작업기록.md`에 보존한다. 추천 B12 internal 비교는 API 계약을 바꾸지 않는다.
