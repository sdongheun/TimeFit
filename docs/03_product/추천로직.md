# TimeFit 추천 로직 — 현행 사양 + 개선 이력 ⭐

> **이 문서는 추천 로직의 단일 진실(living doc)이다.** 로직 수정 시마다 반드시 갱신한다 (사용자 확정 원칙, 2026-07-05).
> 기준 코드: `src/engine/planner.ts` · `data.ts` · 검증: `scripts/test_lazy_engine.ts` (라이브 2시나리오)

---

## 0. 변경 이력

| 날짜 | 변경 | 이유/효과 |
|---|---|---|
| 2026-08-16 | **약속 동반 후보 수집을 생활권·경로 축 범위로 교체** | 기존에는 출발지·약속지·중간점마다 최대 8km 원을 그려, 서면→사상처럼 가까운 이동에서도 부산역 등 이동 축 밖 장소가 후보에 들어왔다. 약속이 있으면 출발지·약속지 생활권은 최대 2.5km, 그 사이는 1.4~2.0km 반경의 연속 표본으로 수집한다. 장소는 세 범위 중 하나에 실제로 속해야 하며, 출발·약속·경로 밖 후보는 시간 여유가 있어도 추천하지 않는다. |
| 2026-08-16 | **지도 후보를 세 이동수단 OR 조건으로 전환** | 지도에서 장소를 먼저 고르는 UX와 달리 후보 수집이 대중교통 하나의 안전 여유·왕복 시간에 묶여 있던 기준을 제거했다. 도보·대중교통·차량 중 하나라도 `이동 + 최소 체류 + 해당 수단 안전 여유`를 만족하면 후보로 남긴다. 장소를 담을 때 선택한 진입 수단과 이후 자동 구간 수단으로 다시 계산하고, 코스 확정 때 실제 경로로 재검증한다. |
| 2026-08-16 | **후보·장바구니 이동수단 규칙 통일** | 후보 단계는 한 수단으로 왕복하고, 상세 단계는 진입 수단+귀환 자동 수단으로 계산하던 불일치를 제거했다. 이제 후보도 상세와 같이 `선택한 진입 수단 -> 이후 자동 수단`으로 계산하므로, 지도에 보이지만 세 수단 모두 담을 수 없는 후보가 생기지 않는다. |
| 2026-08-16 | **카카오 보조시설 오매칭 정정** | 키워드 유사도만으로 `서면시장 물품보관함`, `공원 주차장` 등을 장소 본체와 같은 `verified`로 처리하던 기준을 수정했다. 보조시설 결과 37건을 `weak`으로 낮추고 잘못된 Kakao Place URL을 제거했다. 위치 단서만 남기며, 장소 본체 상세 링크·운영시간 근거로 쓰지 않는다. |
| 2026-08-16 | **종교시설 자연관광지 폴백 보류** | 남부산교회·초량교회·한국 이슬람 부산성원이 관광 활동·운영시간 근거 없이 자연관광지 카테고리 폴백으로 통과할 수 있던 것을 확인했다. 삭제하지 않고 `hold`로 전환해 자동 추천에서 보류한다. |
| 2026-08-15 | **우회율 하드 필터 제거, 시간 예산 기반 후보 판정으로 통합** | 출발지·약속지가 가까우면 `경유/직행` 비율의 분모가 작아져 모든 후보가 탈락하던 문제를 제거했다. 출발지·약속지·중간 경로 후보는 모두 `이동 + 최소 체류 + 안전 여유 ≤ 입력 시간`으로만 추천 가능 여부를 판단한다. 경유로 추가되는 이동은 전체 자투리 시간 대비 비중으로만 순위를 낮춘다. 가까운 약속·먼 약속·왕복에 예외 없이 같은 규칙을 적용한다. |
| 2026-08-15 | **자연관광지 오분류 성격 게이트 적용** | 상권형 5곳은 `area`, 입장·관리형 시설 9곳은 `facility`, 야외 접근형 4곳은 `outdoor`, 판단 보류 2곳은 `hold`로 명시했다. S-train 1곳은 철도 운행 서비스라 후보에서 제외했다. `hold`는 런타임 후보 수집 단계에서 차단하고, `area`는 공식 권역 시간대 없이는 통과하지 못한다. |
| 2026-08-15 | **일반명 백화점 중복 제거·쇼핑복합공간 게이트 강화** | 부산시 쇼핑 원천의 일반명 `백화점`은 롯데백화점 부산본점과 중복되고 주소·운영시간이 없어 추천에서 제거했다. 대표 장소는 유지하며, 백화점·쇼핑몰을 포함한 쇼핑복합공간을 시설형으로 분류해 운영시간 확인 때만 추천한다. |
| 2026-08-15 | **부산 영화의 전당 시설형 분류 정정** | TourAPI 콘텐츠 타입 `관광지(12)`를 따라 자연관광지로 잘못 분류돼 새벽 게이트를 우회하던 문제를 수정했다. AIHub 분류와 실제 이용 성격에 맞춰 영화의 전당·라이브러리 자료실을 문화시설로 바꾸고, 프로그램별 운영시간이 명확하지 않으면 자동 추천에서 보류한다. |
| 2026-08-15 | **전포공구길 권역형 재분류** | AIHub 개별 상점 `오브젝트 에디트`와 122m 떨어진 매칭을 해제했다. 전포공구길은 `상업지구 / 거리·골목` 포괄 장소로 전환하고 카테고리 체류시간만 사용한다. TourAPI 상세도 `매장별 상이`만 제공하므로 공식 권역 시간대가 확보될 때까지 자동 추천에서 보류한다. |
| 2026-08-13 | **권역형 공식 출처 조사 대기열 생성** | TourAPI 직접 매핑이 없는 1순위 16곳을 구·군과 필요한 공식 출처별로 분리했다. 보수동 책방골목·오시리아 해안산책로·충무동 해안시장은 공식 장소 근거만 확인됐고 반복 운영시간 근거는 없어 보류한다. 광안종합시장은 수영구청의 2019년 시간 정보(10:00~20:00)를 찾았지만 갱신 전까지 적용하지 않는다. |
| 2026-08-13 | **권역형 장소 TourAPI ID 재탐색 완료** | TourAPI 원본 ID가 없던 1순위 권역형 16건을 키워드 검색 뒤 이름 완전 일치+250m 좌표 기준으로 재대조했으나 직접 매핑 후보는 0건이었다. 유사명·원거리 결과를 억지로 연결하지 않고, 구청·상인회·공식 관광 페이지의 시간 근거가 확인될 때까지 보류한다. |
| 2026-08-13 | **권역형·시설형 운영시간 게이트 적용** | 1순위 권역형 후보 중 TourAPI 원본이 있는 40건을 `detailIntro2`로 재조회했다. 실제 시각 범위가 명시된 14건만 `area_availability_policy.json`에 등록해 자동 추천하고, 시각 범위와 `점포별 상이`가 함께 있는 4건 및 시간 범위 없는 22건은 보류한다. 카페·식당·문화·레저 등 시설형도 실제 시간 파싱에 실패하면 통과시키지 않도록 수정했다. |
| 2026-08-13 | **권역형 장소 운영시간 근거 수집 시작** | 시장·거리·골목·문화마을 86개를 별도 점검했다. 공식 데이터에 명시 시간이 있는 11개는 추천 가능 시간대 초안으로 분리했고, `가게별 상이` 15개와 시간 미기재 57개는 공식 관광·구청·상인회·대표 안내시설 근거를 추가 확인하기 전 자동 추천 시간대에 사용하지 않는다. |
| 2026-08-13 | **동일 장소 병합·포괄-내부 그룹화** | 이름·주소·좌표가 같은 11쌍을 대표명 하나와 별칭·출처·운영시간으로 병합해 924개를 913개로 정제했다. 영화의전당/자료실 등 포괄-내부 4쌍은 `siteGroupId`로 묶어 서로 다른 후보로는 유지하되 같은 코스·장바구니에 함께 담지 못하게 했다. |
| 2026-08-10 | **부산 공식 관광 데이터 통합 카탈로그 적용** | TourAPI·부산시 명소·쇼핑·맛집 원천을 정제해 매칭 127개, 미매칭 797개를 로컬 카탈로그로 전환했다. 포괄 장소 내부·동명 지점·복수 장소 표기 후보는 보류하고, 기존 TourAPI 중심 카탈로그는 보관본으로 유지한다. |
| 2026-08-08 | **후보 풀 단계별 자동진단 추가** | 카탈로그 반경 후보, 시간·방향 1차 컷 통과, 운영시간 상세 확인, 운영시간 통과 수를 함께 반환한다. 추천 0건을 데이터 부족과 시간 제약으로 구분하고, 상단 후보의 카테고리·전략 분포를 `충분/제한/부족`으로 진단한다. |
| 2026-08-08 | **장소 매력 데이터 원칙 확정** | 장소 매력은 외부 인기·추정값이 아니라, 사용자가 코스를 완료한 뒤 남기는 후기 만족도를 누적해 산출한다. 충분한 후기 전에는 중립 점수를 사용한다. |
| 2026-08-08 | **추천 UX·우선순위 원칙 확정** | 결과의 기본 단위는 장바구니에 담을 장소 후보로 유지한다. 약속 도착 가능성은 하드 조건으로 고정하고, 그 안에서 데이터 신뢰도·시간대 적합도·카테고리 다양성을 순위화한다. 지도 마커 중심 후보 탐색은 구현됐으며, 클러스터링은 후속 UX 작업이다. |
| 2026-08-02 | **장바구니형 코스 빌더로 결과 화면 전환** | 서면 등 후보가 적은 지역에서 같은 장소 조합이 반복되는 문제를 줄이기 위해, 완성 코스 10개를 그대로 보여주지 않고 장소 후보를 보여준 뒤 사용자가 담을 때마다 이동/체류/여유 시간을 재계산하도록 변경. 초기 계획은 `docs/archive/product-plans/`에 보관 |
| 2026-07-26 | **유사 점수 후보 순환**: 랭킹 점수 차이가 작은 후보끼리만 약한 랜덤성을 적용하고, 대중교통 fallback은 약하게 감점 | 같은 입력에서 추천을 다시 눌렀을 때 완전히 같은 정렬만 반복되는 문제 완화. 정확도 높은 후보 우선순위는 유지 |
| 2026-07-25 | **대중교통 1차 구현**: transit 모드 추가, ODsay 총 소요시간 기반 추천, 지하철 포함 경로 소폭 가점, 대중교통 전용 테스트 추가 | 버스/지하철 조합 중 효율적인 경로를 추천 기준으로 사용. 상세 길찾기는 이후 카카오맵 위임 |
| 2026-07-25 | **2단계 구현**: 추천 배치를 10개로 확장하고, 상위 결과에 자연스러운 미니코스가 섞이도록 안정화 | 사용자가 더 유연하게 선택할 수 있게 하고, 2곳 코스가 생성돼도 단일 코스에 밀려 보이지 않는 문제를 완화 |
| 2026-07-25 | **1단계 구현**: 홈 화면 이동수단 선택 UI 추가, 도보/차량 선택 수단 기준으로 추천 가능 여부·랭킹 분리, 추천 목록 TMAP 0건 유지 | 도보 사용자가 차량-only 코스를 받는 문제를 1차 차단. 대중교통은 버튼만 준비중 처리 |
| 2026-07-25 | **이동수단별 추천 로직 계획 확정**: 추천 전 도보/대중교통/차량을 선택하고, 선택 수단 기준으로 후보 필터·랭킹·코스 조합을 분리 | 도보 사용자가 차량 기준 코스를 받는 문제를 막고, 대중교통은 ODsay 기반으로 별도 확장. 초기 계획은 `docs/archive/product-plans/`에 보관 |
| 2026-07-23 | **음식점/카페 후보 포함으로 전환**: TourAPI 음식점(39)과 AI-Hub 식당/카페 데이터를 추천 후보에 포함 | 추천 카테고리 부족 문제를 줄임. 단, 웨이팅 리스크 때문에 체류시간 강제 판단이 아니라 이동 후 체류 가능 시간 안내 중심으로 사용 |
| 2026-07-23 | ~~통합 이동 추천 로직 v1 설계/구현~~ | 2026-07-25 테스트 결과, 추천 전 이동수단 선택 방식으로 대체 |
| 2026-07-23 | **부산 매칭 후보 제한**: TourAPI 부산 장소 중 AI-Hub 부산 방문지와 매칭된 장소만 추천 후보로 사용. 음식점·숙박·축제·여행코스 제외, 자투리 시간 240분 제한 | 출장/약속 전 자투리 시간 타깃에 맞춰 “둘러볼 수 있는 장소” 중심으로 좁힘. 카테고리 폴백 남발을 줄이고 체류시간 근거가 있는 후보만 추천 |
| 2026-07-05 | **랭킹 v1: 결정적 점수 함수 + 그리디 다양성** | "총소요 큰 순" 단일 기준의 왜곡 제거. 오후 뷔페 3개 → 문화·자연·시장·카페 골고루 |
| 2026-07-05 | **배치식 추천**: 3개 고정 → 5개 + 대기열(≤24) + [다른 코스 보기] | "추천이 아니라 강제" 문제 해소. 이후 2026-07-25에 10개 추천으로 확장 |
| 2026-07-05 | **혼잡배수 완화**: 영향 50% + [0.9, 1.2] 클램프 + 5분 반올림 | 60분→108분 비현실 값 제거(저신뢰 데이터 과반영 억제). 부수효과: 게이트 통과 9→14 |
| 2026-07-05 | **축제(15) 행사기간 게이트** | 종료된 박람회가 추천되던 버그(playtime만 검사) 수정 |
| 2026-07-05 | **지연 정밀화 + 실경로 geometry + 캐시 24h TTL + 모드별 캐시 키** | TMAP 54건→10건/추천(한도 대응) · 지도 직선→실경로 · 약관 준수 · 도보/자차 캐시 오염 수정 |
| 2026-06 (초기) | 스파이크 이식: 선계산 + 스팟수/총소요 정렬 + 3개 고정 | 최초 구현 |

---

## 1. 확정 원칙 (2026-08-08)

### 1.0 실제 경로 기반 탐색 전환 결정 (2026-08-17, 구현 전)

현재의 좌표 반경 후보 수집은 최종 정책이 아니다. 도착지가 있을 때 도보·차량·대중교통 기준 경로를 각각 조회하고, 성공한 실제 경로 주변 단일 반경 `R` 안에서 후보를 수집하는 정책으로 전환하기로 결정했다. 기본 `R=1km`, 사용자가 직접 조작할 때만 최대 `R=8km`까지 확장한다. 초기 후보 시간은 근사값, 장소 선택과 장바구니 확정은 실제 구간 경로로 다시 판정한다. 기준 경로 캐시·동일 요청 공유·전체 실패 시 양 끝 생활권 처리까지 구현되기 전에는 현행 규칙을 유지한다. 상세 변경표와 전환 순서는 [추천_실경로탐색전환계획](추천_실경로탐색전환계획.md)을 따른다.

### 1.1 결과와 우선순위

- 결과의 기본 단위는 **완성 코스가 아닌 장바구니에 담을 장소 후보**다.
- **1순위 하드 조건**: 선택한 이동수단으로 이동하고 최소 체류시간을 확보한 뒤, 안전 여유시간을 남기고 약속 장소에 도착하거나 출발지로 복귀할 수 있어야 한다.
- **2순위 랭킹 조건(현행)**: 1순위를 통과한 후보 안에서 체류 여유, 이동 대비 체류, 전체 자투리 시간 대비 추가 이동 비중, 시간대 적합도, AIHub 매칭 신뢰도, 장소 간 밀집도와 카테고리 다양성을 반영한다.
- 음식점·카페와 관광 활동 장소는 동일 후보 풀에서 다룬다. 다만 시간대 적합도·이동·운영시간·시설형 체류 조건에 따라 순위는 달라질 수 있다.
- 장소 매력은 사용 후 후기 만족도로 만들 계획이며, 현재는 **랭킹 점수에 반영하지 않는다.** 로컬 후기 수집만 구현돼 있다.

### 1.2 후보 데이터 범위

| 등급 | 현재 추천 사용 | 체류시간 근거 | 비고 |
|---|---|---|---|
| 직접 매칭 82개 | 사용 | 독립 장소의 이름+좌표 AIHub 체류시간 중앙값 | 개별 장소 체류시간 근거가 가장 강함 |
| 포괄 매칭 41개 | 사용 | 시장·공원·거리 등 지역 맥락의 AIHub 체류시간 중앙값 | 내부 상점에 상속하지 않음 |
| 카테고리 폴백 788개 | 사용 | AIHub 카테고리 중앙값 | TourAPI 또는 부산시 공식 명소·쇼핑·맛집 데이터의 유효 장소 |
| 동일 장소 병합 11쌍 | 대표만 사용 | 병합된 장소의 AIHub 체류시간·출처·운영시간을 보존 | 더 구체적인 지점·지역 표기를 대표명으로 쓰고, 나머지 이름은 `aliases`에 보관 |
| 포괄-내부 그룹 4쌍 | 각각 사용, 동시 선택 불가 | 각 장소의 체류시간 근거 유지 | 독립 입장·운영시간·체험이 있을 수 있어 삭제하지 않고 `siteGroupId`로 묶음 |

### 1.3 이동수단과 지도 UX

- 추천 전 이동수단은 고정하지 않는다. 지도 후보는 `도보·대중교통·차량` 중 **하나라도** `이동 + 최소 체류 + 수단별 안전 여유`를 만족하면 표시한다.
- 후보 수집 단계의 우선 확인 순서는 `도보 -> 대중교통 -> 차량`이다. 차량을 기본값으로 가정하지 않으며, 차량만 가능한 후보도 지도에서 확인할 수 있다.
- 장소를 탭하면 해당 장소로 가는 구간의 이동수단을 고르고, 이후 구간은 거리 기반 자동 수단으로 계산한다. 장바구니·코스 확정에서는 구간별 선택값으로 다시 검증한다.
- 후보 화면은 상단 지도와 하단 플로팅 시트로 구성한다. 지도에는 현재 위치·약속 장소·후보 마커와 이름을, 시트에는 스크롤 가능한 장소 목록과 상세 설명을 표시한다.
- 카테고리 필터는 구현됐다. 마커 클러스터링은 아직 구현하지 않았으며, 후보 수가 늘어난 뒤 별도 UX 작업으로 진행한다. 지도에 보이는 후보는 모두 강한 자동 추천이 아니라, 시간 조건을 통과한 선택 가능 후보다.
- 카카오 검증은 장소 본체와 일치한 `verified` 결과만 상세 링크로 제공한다. 주차장·물품보관함 등 보조시설 결과는 `weak` 위치 단서로만 남기고 해당 링크를 제공하지 않는다. 카카오맵은 메뉴·가격·사진·후기 탐색용 보조 수단이며 운영시간 판정 근거는 아니다.
- 사용자가 첫 장소를 담으면 `현재 위치 -> 담은 장소들 -> 약속 장소` 기준으로 즉시 시간을 다시 계산한다. 기존 후보는 계속 보이되, 추가할 수 없는 장소는 `시간 초과` 상태와 부족 사유를 표시하고 장바구니 추가를 막는다.
- 코스 장소 수는 1~2곳으로 고정하지 않는다. 시간 조건을 통과하는 동안 3곳 이상도 담을 수 있다.
- 같은 `siteGroupId`의 포괄 장소와 내부 시설은 각각 후보로 보일 수 있으나, 사용자가 둘을 같은 장바구니에 담거나 엔진이 둘을 한 미니코스로 조합하지 않는다.
- 초기 방문 순서는 앱이 이동시간이 가장 짧은 순서로 자동 정렬한다. 사용자는 장바구니에서 1·2·3번 순서를 직접 바꿀 수 있고, 변경 즉시 시간 적합성을 다시 계산한다.
- 같은 조건에서 같은 장소가 재표시되는 것은 허용한다. 기본 점수 차이가 작은 후보만 약하게 순환할 수 있으며, 지도 탐색·필터·장바구니 상태로 선택지를 탐색하게 한다.

### 1.4 시간 예산 규칙

```text
buffer = 이동수단별 고정 안전 여유시간
budget = 남은시간 - buffer
```

| 이동수단 | 안전 여유시간 | 추천 예산 |
|---:|---:|---:|
| 도보 | 8분 | 남은시간 - 8분 |
| 대중교통 | 20분 | 남은시간 - 20분 |
| 차량 | 10분 | 남은시간 - 10분 |

이 예산 안에서 `현재 위치 -> 담은 장소들 -> 약속 장소`(약속이 없으면 출발지 복귀)의 이동시간과 체류시간을 계산한다. 최소 체류시간과 이동 대비 체류 규칙은 [2-1. 시간 안전·체류 정책](#2-1-시간-안전체류-정책)을 단일 기준으로 사용한다.

### 1.5 운영시간 확인과 카카오맵 상세 연결

- **현행 데이터**: TourAPI 원본 ID가 있는 장소는 `detailIntro2`에서 운영시간 문자열을 파싱해 게이트를 적용한다. 부산시 공식 데이터 단독 장소는 제공된 운영시간 문구를 안내하며 자동 영업 판정은 하지 않는다.
- **2단계 운영시간 검증(확정)**: 후보 목록에서는 도보·대중교통·차량 중 각 후보의 진입 수단 조합으로 계산한 직선거리 근사 이동시간에 `10분` 보수 여유를 더해 운영시간을 1차 판단한다. 장바구니에서 코스를 확정할 때는 사용자가 고른 진입 구간과 자동 구간만 TMAP(도보·차량) 또는 ODsay(대중교통) 정밀 경로로 다시 계산하고, 실제 도착부터 체류 종료까지 운영시간 안인지 재검증한다. 정밀 경로를 받지 못한 코스는 확정하지 않는다.
- **개별 시설 정책(확정)**: 카페·식당·백화점·쇼핑몰·전시관·체험시설·케이블카 등 핵심 활동이 시설 이용인 장소는 실제 운영시간이 확인될 때만 자동 추천한다.
- **권역형 장소 정책(정제 진행 중)**: 시장·거리·골목·문화마을은 모든 점포의 영업시간을 임의로 추정하지 않는다. 공식 관광·구청·상인회·대표 안내시설의 근거로 **추천 가능 시간대**를 별도 관리한다. 이는 전체 점포의 영업시간이 아니라, 사용자가 기대한 시장·쇼핑·관람 경험을 할 수 있는 보수적 시간대다.
- **TourAPI 재검증 현황(2026-08-13)**: 우선순위 권역형 후보 40건의 `detailIntro2` 원문을 재조회했다. `시간범위확인` 14건은 `src/data/area_availability_policy.json`에 공식 시간대 정책으로 등록해 통과 여부를 판단한다. `시간범위+조건부` 4건은 점포별 상이 문구를 함께 보여 별도 검토하고, `조건부안내` 또는 `시간범위미확인` 22건은 공식 시간대가 확정될 때까지 자동 추천에서 제외한다.
- **원본 ID 없는 권역형 장소(2026-08-13)**: 광안종합시장 등 16건은 TourAPI `searchKeyword2`로 재탐색했지만, 이름 완전 일치와 250m 이내 좌표를 동시에 만족한 후보가 없었다. 이 목록은 `data/processed/review/권역형장소_TourAPI키워드재탐색_검토.json`에서 유지하며, 유사명 결과를 체류시간·운영시간 근거로 상속하지 않는다.
- **전포공구길(2026-08-15)**: 실제 골목 전체를 대표하는 장소를 AIHub 개별 상점 `오브젝트 에디트`의 체류시간으로 매칭했던 사례다. `상업지구 / 거리·골목` 포괄 장소·카테고리 체류시간으로 재분류했으며, TourAPI의 `매장별 상이` 운영시간은 권역 추천 가능 시간대로 사용하지 않는다.
- **추천 가능 성격 프로필(2026-08-15)**: 오분류가 확인된 장소에는 `facility`(운영시간 필수), `area`(공식 권역 시간대 필수), `outdoor`(검토된 야외 접근형), `hold`(자동 추천 보류)를 명시한다. 카테고리만으로 운영시간을 우회하지 않도록 이 프로필을 우선 적용한다.
- **야외 상시형 정책(확정)**: 해변·공원·산책로 중 별도 검토로 상시 접근이 확인된 장소만 운영시간 없이 추천할 수 있다. 자연관광지라는 카테고리만으로 시설형 장소를 상시 허용하지 않는다. 아직 프로필이 없는 기존 자연관광지 전체는 후속 전수 정제 대상이다.
- **근거 미확인 처리(확정)**: 상권 의존형 권역과 시설형 장소는 근거가 없으면 장소를 삭제하지 않되 자동 추천 대상에서 보류한다. 카카오맵 링크는 메뉴·사진·후기 탐색용 보조 수단이며, 운영시간 확인 책임을 사용자에게 넘기는 근거로 쓰지 않는다.
- **구현 작업**: 카카오 검색에서 확인된 장소에는 Kakao Place ID와 `place_url`을 저장한다. `not_found`는 보관만 하고 추천 엔진에서 제외한다.

---

## 2. 현행 파이프라인 (7단계)

```
입력: 현재위치 · 약속(장소·시각)|없음 · 남은시간 · 요일/시간대
  │
  ▼
[0] 예산       이동수단별 안전 여유시간(도보 8 / 대중교통 20 / 차량 10)을 각각 적용
               target = 약속장소 (없으면 현재위치=왕복)
  │
  ▼
[1] 후보 수집   **현행** 앱 번들 `busan_poi_catalog.json`의 정제 후보를 출발지·약속지 생활권과 두 지점을 잇는 경로 축에서 함께 탐색
               약속이 있으면 출발지·약속지 생활권은 최대 2.5km, 경로 축은 1.4~2.0km 반경의 연속 표본으로 제한한다.
               입력 반경 8km는 전체 부산을 허용하는 원이 아니라, 위 생활권·경로 축 반경의 상한값이다.
               약속이 없을 때만 출발지를 중심으로 입력 반경을 사용한다. 수단별 가능 여부는 다음 단계에서 판정한다.
               → matched 123개: 독립 장소는 이름+좌표 AI-Hub 매칭, 포괄 장소는 지역 맥락 체류시간
               → unmatched 788개: categoryDwell 카테고리 중앙값 폴백
               → 동일 장소 이름 차이 11쌍은 대표 장소로 병합하고, 포괄-내부 4쌍은 같은 장소 그룹의 동시 선택만 막는다.
  │
  ▼
[2] 1차 컷     `도보·대중교통·차량` 각각으로 `출발 -> 후보 -> 약속(또는 출발지 복귀)` 이동시간과 최소 체류시간을 계산한다.
               세 수단 중 하나라도 해당 수단의 예산 안이면 통과하며, 후보가 처음 통과한 수단은 초기 순위·운영시간 1차 판단에만 사용한다.
               직행 대비 우회율은 후보 제외 기준으로 사용하지 않는다.
               진단: 원시 후보 수 → 시간·최소 체류 통과 후보 수를 기록
  │
  ▼
[3] 게이트     후보 목록에서는 1차 통과 수단의 직선거리 근사 이동시간 + 10분 보수 여유로 운영시간을 1차 판단
               시장·거리·골목·문화마을은 `area_availability_policy.json`의 공식 추천 가능 시간대로 먼저 판단
               개별 시설은 TourAPI `detailIntro2` 또는 카탈로그의 공식 운영시간이 실제 시각 범위로 파싱될 때만 통과한다.
               근거 미확인 시설형·상권 의존형 장소는 자동 추천에서 보류한다.
               진단: 운영시간 상세 확인 수 → 게이트 통과 후보 수를 기록
  │
  ▼
[4] 조립       (캐시 또는 haversine 근사, 경로 API 0건)
               단일: 통과 전부
               미니코스 2곳: 단일 장소 후 여유시간이 충분할 때, 선택 수단 기준 장소 간 이동 부담·시설형 추가 이동 대비 체류를 함께 판단. 같은 `siteGroupId` 조합은 제외
  │
  ▼
[5] 랭킹 v1 ⭐  score = 0.30·체류여유 + 0.20·이동 대비 체류 + 0.20·시간대적합
                         + 0.15·AIHub 매칭 신뢰도 + 0.15·장소 간 밀집도 + 보너스/감점
               경유로 추가되는 이동은 `추가 이동 / 자투리 예산` 비중으로만 감점한다.
               → 그리디 선택: 이미 뽑힌 카테고리 −0.15/회 (다양성)
               → 상위 24개 대기열
  │
  ▼
[6] 지연 정밀화  추천 목록: 도보/차량은 TMAP 경로 API 0건
               대중교통은 근사 랭킹 → 상위 후보 ODsay 정밀화 → 재랭킹
               도보권 짧은 구간은 ODsay 호출 없이 walk_short 처리
               장바구니 확정: 도보/차량은 선택 코스 구간만 TMAP 정밀 계산 후 운영시간을 재검증한다.
               대중교통은 선택 코스 구간만 ODsay 기반 소요시간·환승 요약으로 재검증한다. 실시간 버스·지하철 정보는 카카오맵 길안내에 위임한다.
  │
  ▼
출력: 코스 후보 10개 + 대기열을 내부 후보 풀로 사용
      Results 화면에서는 중복 장소를 제거해 장소 후보로 표시
      자동진단은 상단 10개 장소의 수·카테고리·전략 분포를 평가하고, 0건이면 어느 단계에서 후보가 사라졌는지 표시
      사용자가 장소를 담을 때마다 `origin -> 담은 장소들 -> target` 기준 이동/체류/여유 시간을 재계산
      코스 확정 시에만 Detail/Execution에서 Course 형태로 사용
```

현재 사용자 화면의 기본 단위는 `완성 코스`가 아니라 `장소 후보`다. 엔진은 여전히 후보 품질을 만들기 위해 코스 후보를 생성하지만, 사용자는 장소를 장바구니처럼 담고 빼면서 직접 코스를 완성한다.

---

## 2-1. 시간 안전·체류 정책 (2026-08-08 확정)

### 안전 여유시간

약속 지연 위험이 수단마다 다르므로, 입력한 남은 시간에서 아래 시간을 먼저 확보한다. 이전의 `남은 시간 12%` 계산은 사용하지 않는다.

| 이동수단 | 안전 여유시간 | 이유 |
|---|---:|---|
| 도보 | 8분 | TMAP 실제 경로 재검증을 전제로 신호·보행 속도 차이만 흡수 |
| 대중교통 | 20분 | TAGO 실시간 버스 도착 연동 전의 접근 도보·환승·배차 변동 대응 |
| 차량 | 10분 | 교통 상황은 있으나 경로 재탐색이 쉬움 |

### 최소 체류시간

- 기본값은 장소당 `30분`이다.
- 자연관광지와 거리·골목·광장·시장 성격의 상업지구만 접근 이동시간에 따라 예외를 둔다.
  - 접근 `10분 이하`: `15분`
  - 접근 `11~15분`: `20분`
  - 접근 `16분 이상`: `30분`
- 카페·식당·문화시설·레저/스포츠와 개별상점·아울렛·쇼핑복합공간은 시설형·유료 성격으로 본다.

### 이동 대비 체류

약속지까지 원래 가야 하는 시간 자체는 불이익으로 계산하지 않는다. 대신 `경유 장소를 넣어 추가된 이동시간`과 체류시간을 비교한다.

- 시설형·유료 성격: 배정 체류시간이 추가 이동시간보다 짧으면 추천 및 장바구니 추가에서 제외한다.
- 자연·거리·광장 등 가볍게 구경 가능한 장소: 시간 예산과 최소 체류시간을 충족하면 표시한다. 추가 이동시간이 체류시간보다 길면 **제외하지 않고 낮은 순위**로 둔다.
- 장바구니에서 장소를 추가할 때도 같은 정책을 즉시 재계산한다.

운영시간 또는 권역 추천 가능 시간대가 확인되지 않은 시설형·상권 의존형 장소는 자동 추천에서 보류한다. 카카오맵 상세 링크는 사용자가 메뉴·사진·후기를 추가 탐색하는 보조 기능이다.

### 사용자에게 보이는 시간 상태

`가능 여부`와 `빠듯함`은 분리한다. 수단별 안전 여유시간(도보 8분, 대중교통 20분, 차량 10분)은 약속 지연을 막기 위한 **하드 조건**이며, 이를 충족하지 못하면 항상 불가능이다. 별도의 10분은 사용자가 판단할 수 있도록 보여 주는 **추가 여유 기준**이다.

대중교통의 20분은 임시 고정값이다. TAGO 버스 실시간 도착과 부산도시철도 시간표를 연결한 뒤에는 노선 구성에 따라 `실시간 대기시간 + 환승·지연 안전 여유`로 교체한다. 실시간 정보가 없거나 조회에 실패한 경우에는 20분을 폴백으로 유지한다.

| 상태 | 조건 | 사용자 의미 |
|---|---|---|
| 여유 | 권장 체류시간을 모두 확보하고, 안전 여유를 제외한 추가 시간이 10분 이상 | 계획대로 둘러볼 수 있음 |
| 짧게 가능 | 최소 체류시간은 확보했지만 권장 체류시간보다 짧고, 추가 시간은 10분 이상 | 짧게 들를 수 있음 |
| 빠듯 | 최소 체류시간은 확보했지만 안전 여유 외 추가 시간이 10분 미만 | 가능하지만 일정 변경 위험이 있음 |
| 불가능 | 최소 체류시간 또는 수단별 안전 여유시간을 충족하지 못함 | 장바구니에 담을 수 없음 |

10분을 모든 장소의 체류시간 단위로 쓰지는 않는다. 카페·식당·시설처럼 목적 활동이 있는 장소의 최소 체류는 30분을 유지하고, 거리·골목·광장 같은 가벼운 구경 장소만 접근 시간에 따라 15~20분을 허용한다.

---

## 2-2. 시행착오 기록: 지도 후보와 장바구니의 시간 판정 불일치 (2026-08-16)

이 기록은 단순 변경 이력이 아니다. 추천 규칙을 바꿀 때 **사용자에게 보이는 후보, 장소 상세, 장바구니**가 같은 판단을 하는지 확인하기 위한 사고 순서다.

### 1. 사용자에게 보인 증상

- 지도에는 음식점·카페 마커가 보였지만, 마커를 눌러 도보·대중교통·차량을 모두 확인하면 어느 수단으로도 장바구니에 담을 수 없는 경우가 있었다.
- 사용자는 "담을 수 없다면 왜 처음부터 추천했는가"라고 받아들인다. 이는 단순히 시간이 빠듯한 문제가 아니라 추천 결과의 신뢰를 훼손하는 오류다.

### 2. 처음의 잘못된 가정

사전 이동수단 선택을 없애고 "세 수단 중 하나라도 시간 안에 가능하면 지도에 보인다"는 방향 자체는 맞았다. 다만 후보를 거르는 계산과 상세 화면의 계산이 서로 달랐다.

| 단계 | 당시 계산 | 문제 |
|---|---|---|
| 지도 후보 | `출발 → 장소 → 약속` 전 구간을 같은 수단 하나로 계산 | 차량으로 전 구간이 가능하면 후보가 통과할 수 있었다. |
| 장소 상세·장바구니 | `출발 → 장소`는 사용자가 고른 수단, `장소 → 약속`은 구간 거리 기반 자동 수단으로 계산 | 차량으로 장소까지 간 뒤 약속지까지는 자동으로 도보·대중교통이 선택될 수 있어, 후보 단계의 차량 전 구간 계산과 결과가 달라졌다. |

즉, 지도에서는 `차량 → 차량`으로 통과했지만 실제 선택 화면에서는 `차량 → 자동 수단`으로 다시 계산되어 전부 불가능해지는 모순이 생겼다. 안전 여유시간도 구간 수단별로 달라 이 차이를 더 키웠다.

### 3. 확인한 방법

서면 출발, 약속 장소 없음(왕복), 일요일 14시, 60분 조건으로 후보와 상세 계산을 비교했다.

- 수정 전에는 지도 후보의 통과 여부와 상세의 세 수단 상태가 일치하지 않는 경우가 있었다.
- 수정 후 시간 조건을 통과한 24곳 중 운영시간 게이트를 통과해 표시된 17곳은 모두 상세와 같은 계산 기준에서 최소 한 수단이 가능했다.
- 이 검증은 직선거리 근사와 운영시간 데이터 기준이다. 장바구니 확정 시에는 실제 경로 API로 다시 계산하므로, 실제 길찾기 시간을 완전히 보장한다는 뜻은 아니다.

### 4. 채택한 단일 규칙

지도 후보, 마커 선택 상태, 장바구니 추가는 아래 규칙을 공유한다.

1. 각 후보에 대해 도보·대중교통·차량을 하나씩 `출발 → 장소`의 **진입 수단**으로 시험한다.
2. `장소 → 약속`은 사용자가 아직 고정하지 않은 구간이므로 자동 수단으로 계산한다. 도보 약 14분 이하는 도보, 초과하면 대중교통을 기본값으로 하며 차량은 자동 선택하지 않는다.
3. 이 조합 중 하나라도 최소 체류시간과 안전 여유시간을 만족할 때만 초기 지도 후보로 표시한다.
4. 사용자가 장소를 담은 뒤에는 선택한 진입 수단과 이미 담긴 순서를 기준으로 전체 코스를 다시 계산한다.
5. 코스를 확정할 때만 실제 경로 API로 구간별 소요시간과 운영시간을 재검증한다.

초기 후보에서 수단 순서는 도보, 대중교통, 차량이다. 이는 차량 보유를 기본값으로 가정하지 않기 위한 정렬 기준일 뿐, 차량만 가능한 장소도 시간 조건을 만족하면 표시한다.

실제 코스를 만든 뒤 길찾기 화면은 각 구간의 이동수단을 명확히 표시해야 한다. 예를 들어 `도보 이동 → 대중교통 이동 → 도보 이동`처럼 구간별 수단·예상 소요시간·다음 목적지를 보여 주며, 자동 선택된 마지막 구간도 숨기지 않는다.

### 5. 제외한 해결 방식

- "차량 후보만 예외 처리" 또는 "약속 장소가 없을 때만 별도 계산"처럼 조건을 덧붙이는 방식은 채택하지 않았다.
- 이런 방식은 후보 화면과 장바구니 화면에 서로 다른 분기 규칙을 늘리고, 다음 UI 변경에서 같은 문제가 다시 발생한다.
- 이동수단 선택 방식은 화면별 예외가 아니라 추천 도메인의 공통 규칙이므로, 공용 계산 함수로 유지한다.

### 6. 이후 변경 시 확인할 질문

1. 이 변경이 후보 표시, 장소 상세, 장바구니, 코스 확정 중 어느 단계의 시간 계산을 바꾸는가?
2. 바뀌는 단계가 있다면 나머지 단계도 같은 공용 규칙을 호출하는가?
3. 지도에 보이는 장소가 현재 선택 가능한 수단 중 하나로 실제 담기는가?
4. 근사 계산에서 통과한 뒤 실제 경로 재검증에서 탈락했을 때, 사용자에게 사유를 보여주는가?

---

## 3. 랭킹 v1 상세 (결정적·설명가능)

```
기본 점수 = 0.30 · min(1, 체류 가능 시간 / 60)
          + 0.20 · 체류시간 / (이동시간 + 체류시간)
          + 0.20 · 시간대 적합도
          + 0.15 · AIHub 매칭 신뢰도
          + 0.15 · 장소 간 밀집도
          + 전략·짧은 시간·지하철 보너스
          − 카테고리 폴백·운영시간 신뢰도·지도 검증·추가 이동 비중·대중교통 폴백 등 감점

그리디 선택 시: − 0.15 × 이미 뽑힌 동일 카테고리 수
점수 차이 0.035 이내 후보만 약하게 순환
```

`시간활용 = (이동시간 + 체류시간) / budget`은 현재 계산만 하며 최종 점수에는 반영하지 않는다. 시간 활용 가중치 조정은 별도 추천 로직 고도화 작업으로 남긴다.

**시간대 적합표** (미기재 카테고리 = 0.6):

| | 카페 | 식당 | 자연 | 문화 | 상업 | 레저 | 축제 |
|---|---|---|---|---|---|---|---|
| 아침 | **1.0** | 0.4 | 0.9 | 0.6 | 0.6 | 0.7 | 0.6 |
| 점심 | 0.7 | **1.0** | 0.6 | 0.6 | 0.6 | 0.6 | 0.6 |
| 오후 | 0.8 | 0.4 | **1.0** | **1.0** | 0.9 | 0.9 | 0.9 |
| 저녁 | 0.8 | **1.0** | 0.6 | 0.6 | 0.8 | 0.6 | 0.9 |
| 야간 | 0.8 | 0.7 | 0.6 | 0.6 | 0.8 | 0.6 | 0.8 |

**설명가능성**: 각 코스에 `why = "체류 85% · 오후 적합 100%"` 부착 → 결과 카드에 표시. "왜 이게 1위냐"에 점수 내역으로 답한다.

**검증 기록**: 2026-07-05의 장소 예시는 이후 카탈로그 정제·운영시간·이동수단 정책이 바뀌었으므로 현재 추천 결과의 기준으로 사용하지 않는다. 현재 후보 품질은 `docs/reports/recommendation_audit*.html`과 자동진단 기준으로 확인한다.

---

## 4. 남은 약점 (우선순위 재정렬)

| # | 약점 | 상태 | 비고 |
|---|---|---|---|
| A1 | **장소 매력 근거 부족** (후기 만족도 미누적) | 🟠 수집 기반 완료 | 코스 종료 후 장소별 만족도·실제 체류·재방문 의향을 기기 로컬에 저장한다. 표본 수·평점 보정 규칙을 정한 뒤에만 2순위 점수에 반영 |
| B1 | 수단·시간별 후보 반경 | ✅ 해결 | 지도 첫 화면은 8km 후보 풀을 사용하고, 수단별 시간 적합성으로 통과 여부를 판정 |
| B5 | **추천 기준 이동수단 혼합** | ✅ 해결 | 사전 단일 수단 선택을 제거했다. 세 수단 중 하나라도 가능한 장소를 보여주고, 장소 선택 뒤 진입 구간 수단을 정한 뒤 장바구니·확정 단계에서 구간별 재검증 |
| B4 | 가까운 약속 장소에서 후보가 전부 탈락 | ✅ 해결 | 출발지·약속지·중간 경로 후보를 같은 시간 예산 조건으로 판정한다. 직행 대비 우회율 하드 컷은 제거하고, 추가 이동이 자투리 예산에서 차지하는 비중만 순위에 반영한다. |
| B2 | 운영시간 게이트 우선순위 | ✅ 해결 | 게이트 순서를 경유 효율·시간대 적합도·체류 여유·데이터 신뢰도·카테고리 중복 감점으로 변경. 호출 한도는 수단·남은시간별 16~28개로 제한 |
| B6 | 카카오맵 상세 링크 오매칭 | ✅ 해결 | 카카오 키워드 검색이 장소 본체 대신 주차장·물품보관함을 반환한 37건은 `weak`으로 낮추고 잘못된 상세 URL을 제거했다. `verified` 장소만 외부 상세 링크를 쓴다. |
| B7 | 지도 마커 중심 후보 탐색 | ✅ 해결 | 추천 화면을 상단 지도+하단 스크롤 후보 시트로 전환했다. 마커의 번호·이름과 후보 카드가 연결되며, 실기기 지도 로드·마커 탭 동작까지 확인 완료 |
| A5 | 취향 필터가 랭킹 미반영 (사후 필터일 뿐) | 🟡 | 칩 선택 → 해당 카테고리 가점으로 승격 |
| C1 | 3곳 이상 장바구니 미지원 | ✅ 해결 | 시간 안에 가능한 장소는 3곳 이상 담을 수 있다. 장소를 추가할 때 캐시/거리 근사 이동시간으로 방문 순서를 자동 정렬하고, 장바구니에서 위·아래 버튼으로 수동 변경 및 `최적 순서로 정렬`을 지원한다. 순서 변경 즉시 이동·체류·여유 시간을 다시 계산한다. 별도 경로 API 호출은 없다 |
| A6 | 후보 목록이 엔진 랭킹을 덮어씀 | ✅ 해결 | 코스의 기본 점수·추천 근거·전략을 후보별 최고 순위로 연결한다. 필터를 사용하지 않은 목록에서는 같은 카테고리·전략이 연속되지 않도록 상위 6개 범위에서만 완만하게 재배치한다. 지도 후보 전체와 카테고리 필터의 동작은 유지한다 |
| C2 | 예산 필터 미구현 | 🟡 | 데이터 소스 미정 |
| — | 체류 폴백 뭉툭(cat3 세분화) · pending의 why가 haversine 기준 근사 | 🟡 | 백로그 |

✅ 해결됨: A2 이동낭비 보상(→체류비율) · A3 다양성(→그리디) · A4 시간대(→적합표) · 혼잡 비현실 · 3개 강제(→배치식)

---

## 5. 다음 개선 로드맵

1. **시간 활용 점수 반영 여부 결정**: 현재 계산만 하는 시간 활용값을 랭킹에 넣을지, 체류 여유 점수로 충분한지 자동진단 결과를 기준으로 판단한다.
2. **후기 기반 장소 매력 반영(A1)**: DB 도입 후 최소 표본 수·평점 보정 규칙을 정한 뒤 순위의 2순위 요소로 반영한다.
3. **지도 마커 클러스터링**: 후보 데이터가 늘어 마커가 겹치는 시점에만 클러스터링을 추가한다.

---

## 6. 원칙 (불변)

- **ML 없음** — 가중치는 손으로 정하고, 근거를 문서에 남긴다 (데이터 해상도·설명가능성, `AI허브_체류시간데이터.md` 참조)
- **모든 코스는 why를 가진다** — 점수 내역으로 설명 못 하는 랭킹 변경 금지
- **TMAP 호출 상한 의식** — 추천 목록 단계는 도보·차량 TMAP 0건을 유지하고, 선택 코스의 상세 확인에서만 정밀 경로를 계산한다.
- **이 문서와 코드는 항상 일치** — 추천 후보·필터·조합·랭킹·시간 게이트를 수정하는 모든 작업은 같은 변경 단위에서 본 문서와 자동검증 수치를 함께 갱신한다.
- **지도 후보와 자동 추천을 구분** — 지도에 표시하는 시간 적합 후보를 모두 강한 추천으로 표현하지 않는다
- **정책은 단일 기준으로 단순화** — 기존 규칙을 보존하기 위한 예외·누적 분기를 추가하지 않는다. 추천 가능 여부는 시간·최소 체류·운영시간의 하드 조건으로, 이동 효율은 전체 시간 대비 비중의 랭킹 조건으로 분리한다.
