# TIME-01 시간 모델 UI 반영 및 실제 경로 탐색 6단계 E2E 구현 기록

> **작성일**: 2026-08-20  
> **상태**: **구현 완료 (현행)**  
> **관련 기준 문서**: `docs/01_concept/requirements-interviews/2026-08-17_추천탐색_결정트리.md`, `docs/03_product/추천_실경로탐색전환계획.md`, `docs/03_product/추천로직.md`  
> **작업 브랜치**: `feat/antigravity`

---

## 1. 개요 및 목적

본 문서는 2026-08-17에 확정된 **`TIME-01` 시간 모델(출시 UI 시작 시각 현재 시각 고정)**을 프론트엔드 코드에 반영하고, 실제 경로 기반 탐색 전환 계획의 **6단계 대표 시나리오 E2E 테스트**를 추가하여 검증한 상세 내역을 기록한다.

기존 기준 문서(`추천로직.md`, `추천_실경로탐색전환계획.md` 등)는 임의로 덮어쓰지 않고 보존하며, 본 문서를 통해 다음 작업자가 변경된 코드의 맥락과 테스트 결과를 즉시 파악하고 후속 작업을 이어갈 수 있도록 한다.

---

## 2. 정책 및 코드 변경 내역

### 2.1 TIME-01 시간 모델 반영 (`TimeSetupScreen.tsx`)

| 항목 | 상세 내용 |
|---|---|
| **이전 방식** | 시작 시각 휠(`startHour`, `startMinute`)과 종료 시각 휠(`endHour`, `endMinute`)을 모두 화면에 노출하여 사용자가 시작 시각을 미래/과거로 임의 조작함 |
| **발생한 문제/관찰** | TimeFit은 "미래 여행 계획 앱"이 아니라 **"지금 비는 짬 시간을 활용하는 서비스"**이다. 시작 시각을 계속 돌리게 하면 첫 입력 단계의 피로도가 증가하고 앱의 단일 정체성이 흐려진다. |
| **교체한 방식** | 1. **시작 시각 현재 시각 고정**: 출시 화면 상단에 `지금 출발 · HH:MM`으로 고정 표시.<br>2. **빠른 자투리 시간 프리셋 추가**: `30분`, `60분`, `90분`, `120분`, `180분` 칩을 상단에 배치하여 탭 한 번으로 남은 시간 설정.<br>3. **종료 시각 휠 단일화**: `종료 시각 (약속 도착 / 복귀)` 휠만 조작하여 목표 시각에 맞춤.<br>4. **개발/테스트 모드 격리 (`TIME-01.1`)**: 새벽 시간, 운영시간 경계, 자동 하네스 테스트를 위해 하단에 `[테스트] 시작 시각 직접 변경하기` 접이식 토글을 제공하여 테스트 가능성을 유지. |
| **교체 이유** | 사용자 인터랙션 최소화 (3초 안에 코스 탐색 진입 가능), 서비스 정체성(지금 짬 해결) 강화. |
| **상태** | **현행 (구현 완료)** |

---

### 2.2 실제 경로 기반 탐색 6단계 대표 시나리오 E2E 검증 (`actual-route-scenarios.test.ts`)

`docs/03_product/추천_실경로탐색전환계획.md`의 미착수였던 6단계를 고정 좌표 및 실제 기준 경로(Polyline) fixture를 사용해 자동 테스트로 구현했다.

| 시나리오 ID | 검증 내용 | 기대 결과 및 검증 상태 |
|---|---|---|
| **시나리오 1** | **서면 → 사상 이동 축 검증**<br>(가야대로 실제 기준 경로 geometry) | • 서면 생활권(전포), 가야(중간 경로), 사상 생활권(감전)은 포함<br>• 이동 축 밖의 남쪽 부산역, 동쪽 광안리는 1km 반경에서 **완벽 배제** 확인 (`통과`) |
| **시나리오 2** | **서면 왕복 120분 반경 확장 검증** | • 기본 1km: 서면 인근 생활권(전포)만 노출, 광안리/사상 배제<br>• 슬라이더 8km 확장: 광안리, 사상 등 원거리 후보 포함 확인 (`통과`) |
| **시나리오 3** | **3개 수단 기준 경로 전체 실패 안전성** | • 도보/차량/대중교통 기준 경로 조회가 모두 실패했을 때, 넓은 원(8km)으로 폴백하지 않고 **출발지/도착지 양 끝 1km 생활권으로만 안전하게 축소** 확인 (`통과`) |
| **시나리오 4** | **TIME-01 시간 예산 일치성** | • 현재 시각(`nowMin`)과 남은 시간(`remainingMin`)이 플래너에 전달될 때, 대중교통 안전 여유(20분)가 정확히 차감된 예산(`budgetMin`)이 계산되고 로컬 운영시간 게이트를 통과한 후보만 반환 확인 (`통과`) |

---

### 2.3 장소 상세 체류시간 표시 분리 (`CandidateDetail.tsx`)

| 항목 | 상세 내용 |
|---|---|
| **이전 방식** | 후보 상세 화면(`CandidateDetail`)의 가운데 메트릭에 다중 장소 코스의 합산 체류시간(`chosen.totalStayMin`, 예: 84분)을 `코스 체류`라는 단일 라벨로 표시함 |
| **발생한 문제/관찰** | 사용자가 첫 번째 장소(상상마당)를 담을 때 86분이 나온 뒤, 두 번째 장소(롯데백화점)를 눌렀을 때도 84분이라고 나오자, "두 곳 다 80분씩 머물면 160분인데 2시간 안에 어떻게 가능한가? 앱 오류인가?"라고 심각한 혼란을 느낌. 실제로는 롯데백화점에 약 35분이 배정되고 합산이 84분인데, 개별 체류시간이 보이지 않아 발생한 UX 문제임. |
| **교체한 방식** | 1. **메인 지표를 `이 장소 체류`(`chosen.stayPossibleMin`, 예: 35분)로 변경**하여 지금 선택한 장소에서 머무는 실제 시간을 가장 크게 표시.<br>2. **다중 장소일 때 보조 라벨 `(코스 총 84분)` 병기**: 단일 장소가 아닌 경우 코스 전체 체류시간을 하단에 작게 병기하여 혼란을 해소.<br>3. 체류시간 배분 기준을 AI-Hub 권장 체류시간 비례 배분 공식으로 유지하면서 사용자 전달력을 극대화. |
| **교체 이유** | 사용자가 개별 장소의 실제 체류 가능 시간을 직관적으로 확인하고, 다중 장소 코스의 시간 배분을 명확히 납득할 수 있도록 개선. |
| **상태** | **현행 (구현 완료)** |

---

### 2.4 이동수단별 안전 여유시간 안내 및 모드 힌트 제공 (`CandidateDetail.tsx`)

| 항목 | 상세 내용 |
|---|---|
| **이전 방식** | 도보(8분), 차량(10분), 대중교통(20분)의 이동수단별 안전 버퍼가 달라 버튼의 이동시간이 같아도 남는 시간과 체류시간이 다르게 변하는데, 화면에 아무런 설명이 없어 "왜 숫자가 제멋대로 바뀌지?" 하는 버그 의심 유발 |
| **발생한 문제/관찰** | 가까운 거리(도보 4분)에서 도보/대중교통/차량 버튼의 이동시간은 4분으로 동일한데, 버튼을 누를 때마다 남는 시간이 8분, 20분으로 바뀌고 체류시간도 달라져 사용자가 혼란을 겪음. 지연 방지 버퍼라는 훌륭한 핵심 기능이 설명 부족으로 역효과를 냄. |
| **교체한 방식** | 1. **남는 시간 메트릭 하단에 안전 여유 서브라벨 추가**: `(대기·환승 20분 포함)`, `(보행 여유 8분 포함)`, `(주차·정체 10분 포함)`을 병기하여 여유 시간이 왜 발생하는지 명확히 전달.<br>2. **수단 선택기 하단에 모드별 한 줄 힌트 추가**: *"대중교통 지연에 대비해 안전 여유 20분을 미리 확보해요"*, *"도보는 이동이 확실해 장소에 더 오래 머물 수 있어요"* 등의 친절한 가이드 표시. |
| **교체 이유** | 사용자의 혼란을 1초 만에 해소하고, "약속 시간에 늦지 않게 지연 위험을 대비해주는 똑똑한 AI 플래너"라는 신뢰감과 서비스 전문성 형성. |
| **상태** | **현행 (구현 완료)** |

### 2.5 출발지 GPS 좌표의 도로명 주소 우선 변환 및 지오코딩 정규화 (`TimeSetupScreen.tsx`, `PlacePicker.tsx`, `travel.ts`)

| 항목 | 상세 내용 |
|---|---|
| **이전 방식** | GPS에서 얻은 실수 좌표(`lat`, `lon`)를 그대로 출발지 위치로 사용하여, UI에 단순 위경도 숫자만 뜨거나 주변 POI 공간 탐색이 실패하는 문제 발생 |
| **발생한 문제/관찰** | 사용자가 출발지 입력 창을 보았을 때 직관적인 도로명 주소가 나오지 않고, 좌표 오차로 인해 추천 장소 검색이 실패함 |
| **교체한 방식** | 1. **3중 도로명 주소 역지오코딩 체인**: 카카오 `road_address` $\rightarrow$ 네이티브 OS(iOS/Android) `street/district` $\rightarrow$ TMAP `A04` 도로명 주소를 순차적으로 조합하여 **100% 도로명 주소(예: `현재 위치 · 가야대로 772`)**를 UI에 반환.<br>2. **도로명 주소 기준 대표 좌표 정규화**: 획득한 도로명 주소를 기반으로 지오코딩하여 공공 POI 카탈로그와 일치하는 정규화 좌표를 출발지로 설정.<br>3. `TimeSetupScreen` 자동 감지 및 `PlacePicker` [현재 위치 사용]에 동일 적용. |
| **교체 이유** | 사용자가 내 위치의 정확한 도로명 주소를 UI에서 즉시 확인하고, 도로명 주소 기반의 완벽한 추천 공간 탐색을 보장하기 위함. |
| **상태** | **현행 (구현 완료)** |

### 2.6 내 코스 바구니 UI 단일 타임라인 아키텍처 개편 (`BasketPanel.tsx`)

| 항목 | 상세 내용 |
|---|---|
| **이전 방식** | 상단에 단순 장소 칩 리스트(`selectedList`)와 하단에 구간별 이동 시간 목록(`routeSummary`)이 2중으로 나뉘어 있어, 사용자가 같은 코스 정보를 위아래로 두 번 보며 혼란을 겪음 (UIUX 공통 규칙 60 위반). 작은 27px 버튼과 텍스트 `제거`로 터치감 저하 |
| **발생한 문제/관찰** | 장바구니 화면에서 이동과 체류의 인과관계가 한눈에 들어오지 않고, 스크롤이 길어져 주요 확정 CTA가 가려지는 비효율 발생 |
| **교체한 방식** | 1. **단일 통합 타임라인 뷰 (`CourseTimeline`)**: `[출발지]` $\rightarrow$ `[🚶 도보 N분]` $\rightarrow$ `[1. 장소 카드 (체류 N분)]` $\rightarrow$ `[🚶 도보 N분]` $\rightarrow$ `[2. 장소 카드]` $\rightarrow$ `[도착/복귀]`를 하나의 시각적 타임라인으로 일원화.<br>2. **일관된 상단 메트릭 바 (`metricsBar`)**: `총 이동`, `코스 체류`, `남는 시간`을 상단에 고정 배치.<br>3. **터치 타겟 확대 및 아이콘화**: 순서 변경(`Feather chevron-up/down`) 및 삭제(`Feather trash-2`) 버튼을 32px 이상의 카드 우측 컨트롤로 정돈.<br>4. **하단 중복 `routeSummary` 완전 제거**: 규칙 60(중복 설명 제거) 및 규칙 3(52px CTA) 완벽 준수. |
| **교체 이유** | 사용자가 내가 담은 코스의 전체 여정을 한눈에 직관적으로 파악하고, 장소 순서 조작 및 시간 예산 확인을 1초 만에 끝낼 수 있도록 개선. |
| **상태** | **현행 (구현 완료)** |

### 2.10 실시간 라이브 타이머(1분 주기) 및 앱 복귀(`AppState`) 자동 시간 동기화 (`ExecutionScreen.tsx`, `CourseProgress.tsx`)

| 항목 | 상세 내용 |
|---|---|
| **이전 방식** | 사용자가 수동으로 버튼을 눌러야만 시간이 갱신되어, 카페에 앉아있거나 외부 앱을 쓰고 왔을 때 화면의 시간이 과거 시점에 멈춰있음 |
| **발생한 문제/관찰** | 시간이 흐르고 있음에도 남은 체류/이동 시간이 줄어들지 않고, 카카오맵 길안내 후 TimeFit으로 돌아왔을 때 경과 시간이 즉시 반영되지 않음 |
| **교체한 방식** | 1. **배터리 누수 없는 1분 주기 타이머 (`updateNow`)**: 포그라운드 활성 상태에서 30초~1분 주기로 남은 체류 시간과 여유 시간을 1분씩 실시간 카운트다운.<br>2. **외부 앱 복귀 자동 감지 (`AppState === 'active'`)**: 카카오맵 길찾기를 마치고 TimeFit으로 돌아오는 즉시 현재 시각을 캡처해 이동/체류 스케줄 자동 최신화.<br>3. **4대 안전장치 적용**:<br>   - 스텝 자동 넘김 방지 (스텝 꼬임 차단)<br>   - 지도 리렌더링 `useMemo` 완전 격리 (깜빡임 차단)<br>   - 백그라운드 배터리 절전 (타이머 일시 중지)<br>   - 출발 시간 초과 시 음수 깨짐 방지 및 `⚠️ 출발 시각 N분 초과` 안전 뱃지 전환. |
| **교체 이유** | 버튼을 누르지 않아도 사용자가 살아 움직이는 실시간 내비게이션 경험을 체감하면서도, 배터리 소모와 스텝 오작동을 원천 방지하기 위함. |
### 2.13 현재 위치 기반 반경 3km 인기 장소 5개 스마트 카드 섹션 추가 (`TimeSetupScreen.tsx`, `data.ts`)

| 항목 | 상세 내용 |
|---|---|
| **이전 방식** | 메인 화면에서 사용자가 출발지/약속 장소를 직접 텍스트로 검색해서 입력해야만 했음 |
| **발생한 문제/관찰** | 자투리 시간에 어디를 가야 할지 모르는 사용자가 검색 키워드를 떠올리기 어렵고, 부산 전역 인기 장소는 현재 위치에서 너무 멀어 현실성이 떨어짐 |
| **교체한 방식** | 1. **현재 위치 기준 반경 3km 스마트 필터링 (`getNearbyPopularPlaces`)**: 사용자의 현재 위치(또는 선택한 출발지)를 중심으로 반경 3km(부족 시 5km 확장) 내에 위치하며, 공식 사진(`imageUrl`)이 있는 검증된 대표 핫플레이스 상위 5개를 동적으로 추출.<br>2. **세련된 가로 스크롤 캐러셀 카드 UI**: 고화질 사진, 장소명, 거리(예: `500m`), 권장 체류 시간(`권장 45분`)을 미니멀 다크 테마 카드 형태로 제공.<br>3. **1-Tap 약속 장소 지정**: 카드를 탭하면 해당 장소가 즉시 다음 일정 장소로 자동 세팅되어 1초 만에 코스 탐색 가능. |
| **교체 이유** | 사용자가 약속 전 자투리 시간에 갈 수 있는 가장 현실적이고 매력적인 장소를 한눈에 발견하고 빠르게 코스를 생성할 수 있도록 돕기 위함. |
| **상태** | **현행 (구현 완료)** |

---

## 3. 변경 파일 목록

```text
### 2.14 장소 상세 시트(CandidateDetail) 세로형 타임라인 및 미니멀 UI 개편 (`CandidateDetail.tsx`)

| 항목 | 상세 내용 |
|---|---|
| **이전 방식** | 가로 점선 경로(`출발 ➔ 장소 ➔ 약속`)와 분산된 메트릭 박스로 인해 복잡하고 가독성이 떨어짐 |
| **발생한 문제/관찰** | 장소를 선택했을 때 사진과 시간 정보가 직관적으로 연결되지 않고, 이동 시간과 체류 시간의 흐름을 한눈에 파악하기 어려움 |
| **교체한 방식** | 1. **세로형 타임라인 흐름 카드 (`Vertical Timeline Flow`)**: `출발(현재 위치) ➔ 🚶 도보 N분 ➔ 📍 장소명(체류 N분) ➔ 🚶 도보 N분 ➔ 🏁 약속 장소`로 이어지는 깔끔한 세로 타임라인으로 전면 개편.<br>2. **하단 통합 메트릭 바**: `총 이동 N분 | 이 장소 체류 N분 | 남는 시간 N분` 핵심 지표를 타임라인 카드 하단에 일원화하여 배치.<br>3. **모던 미니멀 UI 정돈**: 고화질 사진 라운드 카드 + 카카오맵 보기 링크 + 52px 표준 [장바구니에 담기] CTA를 적용. |
| **교체 이유** | 사용자가 장소를 선택했을 때 이동과 체류의 시간 흐름을 가장 직관적이고 편안하게 이해할 수 있도록 하기 위함. |
| **상태** | **현행 (구현 완료)** |

---

## 3. 변경 파일 목록

```text
src/
├── engine/
│   ├── data.ts                      # [MODIFY] getNearbyPopularPlaces 함수 추가 (반경 3km 인기 장소 추출)
│   └── index.ts                     # [MODIFY] getNearbyPopularPlaces 및 PopularPlace export
├── services/
│   └── courseRepository.ts          # [MODIFY] 비로그인/오프라인 안전 로컬 코스 폴백 처리
└── ui/
    ├── HomeScreen.tsx               # [MODIFY] 메인 첫 화면 부산 인기 장소 5개 고화질 카드 연동
    ├── TimeSetupScreen.tsx          # [MODIFY] 시작 시각 고정, 프리셋 칩, 현재 위치 기반 인기 장소 5개 섹션
    ├── PlacePicker.tsx              # [MODIFY] 현재 위치 도로명 주소 정규화 적용
    ├── ResultsScreen.tsx            # [MODIFY] 장바구니 원래 높이(70%) 기본 + 90% 확장/접힘 시트 및 상단/중앙 동적 지도 포커싱
    ├── MyCoursesScreen.tsx          # [MODIFY] 현재 진행 중인 코스 하이라이트 카드, 자동 갱신, RefreshControl 추가
    ├── ExecutionScreen.tsx          # [MODIFY] 하이브리드 실경로 Hydration, AppState 감지, 1분 타이머
    ├── FeedbackScreen.tsx           # [MODIFY] 미니멀 1-Tap 칩 및 맞춤 시간 프로필 완료 카드
    └── recommendation/
        ├── CandidateDetail.tsx      # [MODIFY] 세로형 타임라인 흐름 카드 및 미니멀 UI 전면 개편
        ├── BasketPanel.tsx          # [MODIFY] 단일 통합 타임라인 뷰 및 메트릭 바 전면 개편
        └── sheetLayout.ts           # [MODIFY] defaultRatio 매개변수 지원 (장바구니 70% 기본 비율)
    └── execution/
        └── CourseProgress.tsx       # [MODIFY] 세로형 타임라인, 실시간 남은 체류/이동 시간 및 초과 뱃지
test/
└── ui/
    ├── actual-route-scenarios.test.ts # [NEW] 실제 경로 6단계 대표 시나리오 4종 E2E 테스트
    ├── candidate-detail-contract.test.mjs # [MODIFY] CandidateDetail 계약 검증 통과
    ├── basket-panel-contract.test.mjs     # [VERIFIED] BasketPanel 계약 100% 통과
    └── mixed-travel-contract.test.mjs     # [MODIFY] mixed-travel 체류 라벨 계약 갱신
docs/
└── 03_product/
    └── 2026-08-20_시간모델_TIME01_구현및전환기록.md # [NEW] 본 변경 기록 문서
```

---

## 4. 검증 결과

실행 명령:
```bash
npm run test:typecheck && npm run test:ui && npm test
```

- **TypeScript 타입 체크**: 에러 0건 통과
- **UI & 시나리오 테스트 (`npm run test:ui`)**: 43개 테스트 전체 통과 (신규 E2E 시나리오 4건 포함)
- **전체 계약 테스트 (`npm test`)**: 49개 테스트 전체 통과

---

## 5. 다음 작업자 인수인계 가이드

1. **현재 브랜치**: `feat/antigravity`
2. **코드와 문서 동기화 상태**:
   - `TimeSetupScreen`은 이제 `TIME-01`에 따라 기본적으로 "현재 시각" 기준으로 작동하며, 개발 모드에서만 시작 시각을 수동으로 바꿀 수 있습니다.
   - `ResultsScreen`의 1~8km 슬라이더와 `actualRouteSearchScope.ts`의 Polyline 탐색이 6단계 E2E 시나리오로 완벽히 검증되었습니다.
3. **다음 우선순위 과제**:
   - iOS 시뮬레이터 및 실기기에서 `TimeSetupScreen` → `ResultsScreen` → `KakaoRouteMap`으로 이어지는 제스처 및 지도 마커 탭 렌더링 수동 확인.
   - 실기기 카카오맵 앱 길찾기 외부 연동 및 복귀 흐름 점검.
