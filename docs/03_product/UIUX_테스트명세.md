# TimeFit UIUX 테스트 명세

> 목적: 사용자 검토 과정에서 발견한 UIUX 문제를 재발하지 않도록 요구사항 ID, 화면 상태, 자동화 조건으로 관리한다. 이 문서는 이후 Maestro 또는 Detox E2E 테스트의 시나리오 원본이다.

## 사용 규칙

- 새 UIUX 피드백은 기존 ID와 `동일`, `보완`, `충돌` 여부를 먼저 판단한다.
- 충돌이면 기존 요구사항을 임의로 덮어쓰지 않고 사용자 결정 뒤 상태를 `대체됨`으로 바꾼다.
- `자동화 전제`의 `testID`와 고정 위치·시각·API 응답 주입이 준비되기 전에는 수동 시나리오로 확인한다.
- 자동화가 추가되면 이 문서의 상태와 [테스트 현황](../테스트.md)을 함께 갱신한다.

## 공통 화면

| ID | 문제와 확정 규칙 | 상태 | 자동화 전제 | 수동 확인 시나리오 |
| --- | --- | --- | --- | --- |
| UXV-01 | 상태 표시 영역과 지도·헤더·버튼이 겹치지 않는다. 모든 상단 요소는 safe-area inset 아래에 배치한다. | 부분 | 각 화면의 상단 컨트롤 `testID`, iPhone 소형·대형 캡처 비교 | 시간 설정, 후보 지도, 코스 확인, 내 코스, 기록, 내 정보에서 노치·Dynamic Island와 겹침 확인 |
| UXV-02 | 페이지 이동은 불필요한 스택 누적 없이 목적에 맞게 되돌아간다. Results의 장소 상세는 닫으면 같은 Results 상태로 돌아가고, 코스 화면은 시작 전후 새 화면을 쌓지 않는다. active 코스에서 나가도 Home 이어가기로 같은 단계에 복귀한다. | 구현 전환 필요 | navigation state·뒤로가기·탭 `testID`, A0/A1 상세 pop과 review→active stack depth 검사 | 입력값·목록·scroll·대표 코스·active 단계가 각 복귀 경로에서 유지되는지 확인 |
| UXV-03 | 텍스트 설명을 늘리지 않고 아이콘·상태 변화·버튼 위치로 다음 행동을 알 수 있게 한다. | 수동확인 | 화면 스냅샷 기준선 | 처음 보는 사용자가 도움말 없이 대표 코스 확인·코스 시작 가능 여부를 확인 |
| UXV-04 | 위치 권한은 첫 실행·로그인 직후가 아니라 `내 위치 사용`을 누른 뒤에만 요청한다. 거절·GPS 실패 시 지도 직접 선택으로 동일한 시간 설정 흐름을 완료할 수 있다. | 미착수 | 위치 권한 어댑터 허용/거절/GPS 실패 fixture, 각 CTA `testID` | 앱 첫 진입 시 권한 팝업이 없는지, 현재 위치 선택 뒤에만 노출되는지, 거절 뒤 지도 선택과 계산이 가능한지 확인 |
| UXV-05 | 도착지/복귀는 현재 위치 복귀가 기본값이며 지도 직접 선택으로 약속·집·역을 바꿀 수 있다. 도착 시각은 시·분 단위로 설정하되 출시 UI에서 현재 시각부터 120분을 넘길 수 없다. | 구현 전환 필요 | 고정 현재 시각, 시간 선택기·지도 선택기 `testID` | 왕복·도착지 지정·1/120/121분 경계에서 입력값과 결과 헤더가 일치하는지 확인 |
| UXV-06 | 시간 설정의 경로 입력은 `경로 설정하기` 하나로 진입한다. 경로 설정 화면에서 출발지·도착지를 함께 보고 각 필드를 탭해 같은 위치 선택 화면의 현위치·앱 내부 지도·장소/주소 검색으로 바꾼다. 권한 허용 출발지는 GPS 좌표와 provider 주소(없으면 provider 행정구역)를 자동 적용하고, 도착지를 비우면 출발지 복귀다. 장소명은 관련 POI 제안, 주소는 주소 제안을 우선 보이며 두 검색 API를 동시에 호출하지 않는다. 입력 변경 뒤에는 이전 선택으로 확정할 수 없고, 사용자가 한 행 또는 지도 핀을 명시 확정해야 한다. | 구현 보완 필요 | GPS 주소/지역 label cache, 장소/주소 분류·우선/반대 fallback·2글자/400ms debounce·10분 TTL·동일 요청 합치기 fixture, 권한 허용/거부·지도 이동 0회/핀 address 1+조건부 region 1·입력 변경/선택/확정 `testID` | 권한 허용/거부 출발지, 빈 도착지 복귀, 도착지 지정, `사상역`·주소 입력, 기기 위치 중심 지도·물방울 핀·해변 좌표 확정을 확인. |
| UXV-48 | 모든 앱 소유 버튼·탭 카드·칩은 press-in 즉시 scale/opacity 눌림 상태를 보이고 release/cancel 때 짧게 복원한다. 일반 CTA·카드·칩·뒤로가기·더보기·tray 삭제의 햅틱은 0회다. disabled·busy는 애니메이션과 실행을 막고, Reduce Motion에서는 scale 없이 opacity/색 변화만 사용한다. | 구현 전 | 공용 animated pressable의 press-in/out·cancel·disabled·busy·Reduce Motion fixture, haptic adapter 0회와 navigation/API 호출 횟수 | 주요 CTA, 결과 카드, 더보기, 뒤로가기, tray 삭제 버튼을 길게 누른 뒤 떼기·밖으로 취소·연타해 시각적 눌림·햅틱 없음·단일 실행 확인 |
| UXV-49 | 도착/개발 테스트 시각 wheel은 오전/오후·시·분을 행 단위로 snap하고 빠른 swipe의 momentum으로 여러 칸 이동한다. wheel의 사용자 새 행과 `도착 전 남길 시간` slider의 사용자 새 5분 값에만 selection haptic 1회다. mount·prop/programmatic 동기화·같은 값 중복·최종 snap/complete에는 추가 햅틱이 없고 일반 버튼은 계속 0회다. | 검증 완료 | wheel/slider/adapter fixture 16/16과 전체 UI/core/typecheck를 통과했다. `ExpoHaptics` 포함 새 internal iOS build의 wheel·slider 물리 촉각도 사용자가 실기기에서 확인했다. |
| UXV-50 | 계층 화면은 iOS native push/pop과 edge-swipe를, 임시 선택은 bottom modal/sheet를, 같은 화면 상태 변화는 해당 영역의 160~220ms 이내 전환을 사용한다. 탭은 스택을 쌓거나 좌우 push하지 않고 Reduce Motion에서는 큰 이동을 제거한다. | 구현 전 | native-stack options source 계약, navigation state·beforeRemove fixture, modal/in-place/Reduce Motion 표시 모델 | 시간 설정→결과→장소 상세/코스의 방향, 장소 상세 pop, 코스 review→active stack 증가 0, Results A/B 상태 변화와 탭 전환을 확인 |
| UXV-51 | Kakao 길찾기 handoff 성공 뒤 iOS 17 Live Activity가 `traveling`을 시작한다. 예상 `moveMin`+20% 유예(3~10분 clamp)에 `도착했어요`·1회 `5분 뒤 다시 알림`을 제공하고, 안전 출발시각 5분 전에는 출발 행동을 제공한다. 명시 도착과 다음/목적지/복귀 길찾기 성공 또는 앱 완료 사이만 체류로 인정한다. GPS·background location·원격 ActivityKit push는 사용하지 않는다. 비로그인/미동의는 로컬 진행만 가능하고, 일반 로그인+체류 기록 동의+완료 표본만 저장한다. | 구현 전 | 고정 clock, fake handoff/activity/notification/repository, 중복 action·권한 거절·강제 종료 fixture와 DB/엔진 공개 계약 | iOS 17 실기기에서 1곳·2곳 각 1회, Lock Screen/Dynamic Island, 도착/snooze/출발, 앱 복귀, 알림 또는 Live Activity 거절 시 추천·길찾기 유지 확인 |
| UXV-52 | 출시 시각 정리는 다크·파란 의미색을 유지한다. Home CTA/진행 영역은 그룹 간격으로 분리하고, 결과 카드는 사진·장소·활동·코스 시간을 유지한 채 세로 밀도를 줄인다. CourseConfirm 지도만 좌우 full-bleed이며 아래 순서는 연속 guide로 읽힌다. 행동 없는 우측 header placeholder는 보이지 않고 접근성 요소도 아니다. 로딩의 네 단계는 실제 runtime callback만 반영하고 고정 지연·추가 API·navigation을 만들지 않는다. | 구현 전 | progress callback 순서/실패/stale fixture, header spacer 접근성, 카드 원본 identity/label, route segments/marker/CTA 불변 계약 | 자동 게이트 뒤 작은/큰 iPhone에서 Home 간격, 결과 카드 수, full-bleed 지도·첫 코스 단계 가시성만 각각 한 번 확인 |
| UXV-53 | 다른 코스가 진행 중일 때 새 추천 탐색은 막지 않고, 다른 snapshot의 최종 시작에서만 `취소/기존 코스 이어가기/새 코스로 시작`을 제공한다. 같은 snapshot은 기존 단계로 바로 재개하고 교체는 완료로 기록하지 않는다. | 검증 완료 | active none/same/different, Alert 선택 전·세 action, 중복 tap, active/navigation/completion 0/1회 fixture 통과 | active A→B에서 취소, 기존 이어가기, 새 시작을 한 묶음으로 확인해 각각 A 유지, A 직전 단계, 완료 기록 없는 B 교체를 확인 |
| UXV-54 | Route Proxy anonymous session이 있어도 내정보와 계정 전용 기능은 비로그인으로 표시한다. raw session은 Proxy 재사용에 유지하고 일반 account만 이메일·로그아웃·계정 저장·개인화 UI를 사용한다. | 검증 완료 | null/anonymous/account/loading projection, Profile/saved course gate, CAPTCHA·anonymous sign-in 추가 0 fixture 통과 | 로그아웃→비로그인 추천→내정보에서 로그인 UI이며 일반 계정 오표시가 없음을 확인 |

## 후보 지도와 하단 시트

| ID | 문제와 확정 규칙 | 상태 | 자동화 전제 | 수동 확인 시나리오 |
| --- | --- | --- | --- | --- |
| UXV-10 | 지도는 전체 화면, 장소 목록은 하단 시트로 분리한다. 접힘·기본·90% 확장 상태에서 각각 지도 중심이 실제 보이는 영역의 중앙에 맞는다. | 부분 | 시트 상태 `testID`, 지도 중심 좌표를 WebView에서 보고 | 각 상태에서 현재 위치 버튼과 장소 선택 후 마커가 가시 영역 중앙에 오는지 확인 |
| UXV-11 | 시트 상단 핸들에서 시작한 세로 스와이프만 시트 높이를 바꾼다. 카드·목록에서 시작한 스와이프는 목록만 스크롤한다. | 부분 | 핸들·목록 `testID`, 좌표 기반 제스처 | 기본 시트에서 위·아래 스와이프, 카드 영역 스크롤, 확장 후 복귀를 반복 |
| UXV-12 | 마지막 장소 카드가 하단 플로팅 바나 숨겨진 시트 영역에 잘리지 않고, 기본·확장 상태에서 끝까지 스크롤된다. | 부분 | 마지막 카드 `testID`, 목록 끝 가시성 검사 | 후보가 많은 고정 입력에서 마지막 카드가 완전히 보일 때까지 스크롤 |
| UXV-13 | 지도 마커를 누르면 같은 장소 카드에 포커스만 한다. 카드 선택은 해당 장소 상세를 기본 시트에서 연다. | 부분 | 마커·카드 `testID`, 선택한 contentId 노출 | 마커 선택과 카드 선택을 번갈아 하며 지도·시트 포커스 일치 확인 |
| UXV-14 | 사진 마커·기본 마커·현재 위치 마커는 구분되고, 선택한 마커와 라벨은 다른 요소보다 위에 표시된다. | 부분 | 지도 마커 상태 이벤트, 스냅샷 | 사진 있음·없음·현재 위치·선택 상태를 각각 확인 |
| UXV-15 | 후보 필터는 분위기·활동 두 묶음이 아니라 단일 가로 스크롤 카테고리 칩이다. `카페·가벼운 휴식`, `자연관광`, `문화`, `쇼핑·거리`, `레저`처럼 런타임 카탈로그와 일치하는 이름을 쓰며, 일반 식당 `음식점` 칩은 노출하지 않는다. | 미검증 | 카테고리 칩·목록 수 `testID`, 활성 카탈로그 fixture | 좁은 화면에서 좌우 스크롤, 전체 복귀, 각 칩의 후보·빈 칩 미노출을 확인 |
| UXV-16 | `장소 후보 n`은 기본·확장 시트에서 공통으로 보인다. 접힘 상태와 장소 상세에서는 중복 표기하지 않는다. | 부분 | 시트 상태·후보 수 `testID` | 기본·확장·접힘·상세 상태를 바꾸며 표시 여부와 수량 일치 확인 |
| UXV-17 | 뒤로가기·남은 시간·코스 확인은 기본·접힘 시트에서 지도 상단에 고정한다. 90% 확장 시트는 이 컨트롤보다 위 레이어에 표시되며 시트 내부에도 같은 컨트롤을 중복하지 않는다. | 부분 | 상단 컨트롤·시트 상태 `testID` | 기본·접힘·확장·상세 상태에서 레이어 순서와 중복 여부 확인 |
| UXV-18 | 첫 결과는 장소 마커의 양보다 `종료 시각까지 남은 시간`과 `도착 전 여유`를 먼저 보여 준다. 여유 슬라이더 기본값은 10분이고 0분은 선택할 수 없다. | 미착수 | 종료 시각·여유 슬라이더·예산 지표 `testID` | 10분/최소값/큰 여유값을 바꾸며 추천·짧게 가능 상태가 즉시 변하는지 확인 |
| UXV-19 | 후보가 없을 때 일반 카페를 TimeFit 추천 카드에 섞지 않고, 카카오맵 주변 탐색·도착지 주변 보기·범위 확장 행동을 분리한다. 실제 날씨 입력이 없으면 날씨를 원인으로 말하지 않는다. | 미착수 | 빈 결과 fixture, 외부 링크 mock | 후보 0개·운영시간 미확인만 존재·경로 API 실패 상태에서 안내와 행동을 확인 |
| UXV-20 | 출시 첫 결과는 운영시간 확인·실제 경로 검증을 통과한 상단 대표 **1곳**과 서로 다른 1곳 대안 최대 3개를 보여 준다. 미검증 single 후보가 남으면 `다른 장소 더 보기`를 표시하고, 명시 tap마다 새 검증 장소 최대 3개를 기존 목록 뒤에 누적한다. 대표·기존 순서와 상세 왕복 상태를 유지하며 전체 결과를 3개로 다시 자르지 않는다. 다장소 continuation은 노출하지 않는다. | 구현 전환 필요 | initial/page 각각 provider attempt 8회 이하, page당 추가 최대 3, 전체 place ID 중복 0, 연타 호출 1회, 2·3곳 route 0, 종료 상태·카드 상세 복원과 새 internal build 확인 필요 |
| UXV-21 | 지도에서 더 보기의 확인 후보는 추천/빠듯/불가능 상태를, 운영시간 미확인 후보는 `운영시간 확인 필요` 상태를 유지하며 대표 카드에는 섞이지 않는다. | 미착수 | 확인/조건부/불가 지도 fixture | 조건부 후보의 대표 제외와 상태별 CTA를 확인 |

## 장소 상세 — 기존 시트 이력과 전체 지도형 전환

| ID | 문제와 확정 규칙 | 상태 | 자동화 전제 | 수동 확인 시나리오 |
| --- | --- | --- | --- | --- |
| UXV-26 | 장소 카드를 누르면 기존 시트 높이 안에서 상세를 확장한다. | 대체됨 | `UXV-55`가 전체 지도형 상세로 교체 | 과거 시트 회귀 이력만 보존하고 새 구현 기준으로 사용하지 않음 |
| UXV-27 | 상세는 기존 시트 안에서 아래 전환으로 열고 닫는다. | 대체됨 | `UXV-55`가 계층형 전체 지도와 Results 복귀로 교체 | 과거 전환 이력만 보존하고 새 구현 기준으로 사용하지 않음 |
| UXV-28 | 장소명·사진·시간 정보의 간격은 충분하고 사진은 둥근 모서리의 고정 비율 영역에 표시한다. 사진이 없으면 대체 화면을 쓴다. | 부분 | 사진 있음·없음 고정 데이터, 시각 회귀 캡처 | 세로·가로 비율이 다른 사진과 사진 없는 장소를 확인 |
| UXV-29 | 출시 코스 상세에는 도보·대중교통 구간, 장소 1곳, 남는 시간과 도착 전 여유가 검증 snapshot 순서로 보인다. 체류 분 전체 비노출은 UXV-44로 부분 교체됐으며, 최소·최대·범위·조절기 비노출과 사용자의 수단·체류 선택 금지는 유지한다. | 구현 전환 필요 | 1곳 고정 경로 fixture, 구간 요약·남는 시간·CTA `testID` | UXV-44의 세로 순서·시간 합계와 함께 확인 |
| UXV-38 | 모든 상세 장소는 카카오맵 확인 링크를 제공한다. 검증 URL/Place ID 우선, 없으면 카탈로그 주소 검색, 주소가 없거나 지나치게 넓을 때만 좌표 지도 보기로 연다. 장소명과 좌표를 한 검색어로 섞지 않으며 실패 시 오류를 알린다. | 구현 보완 필요 | Linking 어댑터 mock, URL 검사 | 검증 장소·상세 주소·주소 없음/넓음 장소에서 링크 목적지와 오류 처리 확인 |
| UXV-39 | 운영시간 미확인 후보는 더보기에만 `운영시간 확인 필요`와 카카오맵 확인 링크를 보이고, 대표 추천·자동 확정으로 표현하지 않는다. 사용자 확인 뒤에도 `운영시간 확인 필요(사용자 확인)` 상태를 유지한 조건부 수동 포함 코스에만 넣을 수 있다. | 미착수 | 운영시간 있음/없음 fixture, 상태 라벨·CTA·저장 스냅샷 `testID` | 확인 전 차단·확인 뒤 조건부 상태 유지·재계산과 대표 제외를 확인 |

## 자동화 순서

1. 고정 시각·위치·후보 데이터를 주입하는 테스트 모드와 핵심 `testID`를 만든다.
2. UXV-04, UXV-05, UXV-20, UXV-21, UXV-29, UXV-39, UXV-44를 먼저 E2E로 자동화한다. 위치 입력과 시간 설정, 출시 one-stop 대표·대안, 조건부 후보 확인은 새 핵심 코스 작성 흐름이다. 2·3곳 자동 코스는 배포 후 보류다.
3. UXV-10, UXV-11, UXV-13은 WebView 지도 이벤트를 테스트용으로 노출한 뒤 자동화한다.
4. UXV-01, UXV-14, UXV-21, UXV-26은 iPhone 기종별 스크린샷 회귀 테스트를 추가한다.

## 대표 1~3곳 코스 전환 (2026-08-23)

이전 후보 지도·하단 시트 세부 규칙은 다중 장소 조립 UI의 이력이다. 현행 결과 화면은 상단 대표 코스 1개와 같은 세션의 검증 대안 목록을 함께 제안하고, 지도는 확인 후보·조건부 후보를 분리한 보조 탐색으로 쓴다.

| ID | 문제와 확정 규칙 | 상태 | 자동화 전제 | 수동 확인 시나리오 |
| --- | --- | --- | --- | --- |
| UXV-30 | 메인은 **`다음 일정 전, 부산을 한 곳 더 만나볼까요?`**와 `자투리 시간 설정하기` 하나를 주 행동으로 보인다. 로그인 여부와 무관하게 같은 앱 프로세스에 진행 중인 V1 코스가 있을 때만 이어가기 진입점을 표시하고, 누르면 새 추천·첫 단계가 아니라 마지막 확정 단계로 이동한다. 카카오맵 전환·background/foreground·일반 navigation 이탈은 활성 상태를 지우지 않고, 완료·명시 취소만 제거한다. 강제 종료 뒤 복구는 U-LIVE-ACTIVITY-01 전까지 보장하지 않는다. 추천 캐러셀·관광지 피드는 보이지 않는다. | 검증 완료 | 로그인 공통·V1/legacy 우선순위·시작/단계/실패/완료·API/DB/storage 0 fixture 통과 | `QA-PROGRESS-RESUME-01` 비로그인 실기기에서 V1 시작→카카오맵 복귀→메인 카드→같은 단계 이어가기→완료 뒤 카드 제거를 통과했다. 다른 코스 시작 시 교체 확인은 별도 후속이다. |
| UXV-31 | 위치 권한은 `내 위치 사용하기`를 누른 뒤에만 요청한다. 지도 직접 선택·검색은 권한 거절 뒤에도 같은 입력값으로 돌아온다. | 부분 | 권한 어댑터 fixture와 지도 탭 이벤트 | 허용·거절·GPS 실패에서 원점이 설정되는지 확인 |
| UXV-32 | 도착지/복귀 기본값, 분 단위 시간 선택, 5~30분 여유 슬라이더가 시간 설정에 분리되어 있다. | 부분 | 고정 현재 시각·시간 선택기·슬라이더 `testID` | 왕복/도착지 지정과 최소·최대 여유에서 결과 예산이 일치하는지 확인 |
| UXV-33 | 계산 중 화면은 실제 위치·경로 기준선·후보 계산 단계와 연결된다. 인위적 지연으로 결과를 늦추지 않는다. | 부분 | API 어댑터 지연 fixture, 단계 이벤트 | 캐시·성공·실패에서 단계와 오류 복귀를 확인 |
| UXV-34 | 첫 결과의 1~3곳 대표 우선순위와 3곳 목록 노출은 다장소 public store 복구 뒤 다시 검토한다. 이번 출시는 UXV-20·UXV-44의 실제 검증 one-stop 대표·대안을 사용한다. | 배포 후 보류 | 다장소 public store 원인 해결 뒤 1/2/3곳 fixture 재개 | 이번 출시 수동 확인 대상에서 제외 |
| UXV-35 | 결과·상세·코스 확인은 실제 경로 결과만 사용한 하나의 시간 여정으로 `이동(파랑) → 활동(주황) → … → 이동(파랑) → 도착 전 여유(초록)`를 표시한다. | 부분 | 1/2/3곳 실제 경로 fixture, 접근성 라벨·구간 값 검사 | `legs/stops` 순서·불일치 차단 UI fixture는 통과. iOS 렌더링·접근성 읽기 순서 확인이 남음 |
| UXV-36 | `기록`은 독립 탭이며, 이번 달 명시 `코스 마치기` 완료의 장소·카테고리를 집계한다. 후기는 선택이고, 선택·저장·코스 보기·길찾기 실행만으로 완료를 표시하지 않는다. 실제 체류 미확인 완료는 장소/카테고리에 포함하지만 활동 시간에는 넣지 않고 미측정으로 구분한다. | 검증 완료 | 완료/중복 완료/이전 달/빈 기록/미측정/legacy 후기 fixture와 기록 탭 `testID`, 비로그인 iOS 1곳 제한 smoke를 통과했다. | 기록 탭 자동 이동, 장소 수 +1, 카테고리 반영, `체류시간 미측정`, 탭 왕복 유지와 active 제거를 확인했다. 개별 장소명·완료 시각 목록 필요 여부는 별도 제품 결정으로 남긴다. |
| UXV-37 | 개발 빌드의 시간 설정 화면은 테스트 현재 시각을 분 단위로 바꾸고, 추천·운영시간 판단에 같은 값을 사용한다. 배포 빌드에는 이 제어가 보이지 않는다. | 부분 | `dev-test-clock` testID, 고정 시각 helper fixture | 오후·야간 시각을 각각 적용해 추천 결과·운영시간 게이트가 달라지는지 확인. 배포 빌드 미노출과 요일 fixture는 후속 E2E에서 검증 |
| UXV-40 | 대표 결과와 코스 상세는 장소명 외에 사실 기반 `지역명 + 활동 유형`을 보여 부산 발견 맥락을 전달한다. 앱 완료 이력이 없는 사용자에게 `처음 가보는`, `숨은`, `인기` 같은 근거 없는 표현을 쓰지 않는다. | 부분 | 지역·활동·완료 이력 있음/없음 fixture, 텍스트 접근성 라벨 검사 | `course-v1-discovery-context.test.ts`가 주소·활동 근거가 모두 있을 때만 표시하고, 코스 순서와 금지 문구 미노출을 검증했다. 완료 이력 기반 표현은 아직 도입하지 않았으며, iOS 실기기 텍스트·VoiceOver 검증이 남아 있다. |
| UXV-41 | 대표 후보 없음과 4개 이내 실제 경로 검증 코스 없음은 서로 다른 빈 상태로 표시한다. 두 상태 모두 조건부·일반 카페를 대표로 대체하지 않는다. | 부분 | `no_representative_candidates` / `no_verified_course_within_limit` fixture, 빈 상태 CTA `testID` | UI 계약 fixture는 통과. iOS 화면·CTA E2E가 남음 |
| UXV-42 | 구조화 운영시간이 없는 시장·거리·골목은 데이터가 `conditional_visit`로 식별하고 제외 분류를 통과한 경우에만 **실제 기기 시각** `10:00 ≤ t < 18:00`의 별도 `운영시간 확인 후 들러볼 곳`에 보인다(09:59 숨김, 10:00·17:59 노출, 18:00 숨김). 18:00 경계를 넘거나 foreground 복귀하면 이미 보인 카드·더 보기도 사라지며 개발 추천 시각은 이를 연장하지 않는다. 출발/도착 중 가까운 쪽 기준 직선거리 가까운 8개를 먼저, 로컬 `더 보기`로 다음 8개를 기존 뒤에 붙이며 이 탐색은 route/API 호출 0이다. 카드·카카오맵 확인은 영업·안전·시간 적합을 보장하지 않는다. 사용자는 카카오맵 확인 뒤 실제 확인 시각도 18:00 전일 때만 명시 CTA로 경로 계산을 시작하고, 그동안 차감한 시간을 반영한다. 이후 시각이면 route 0·재추천 문구이며, 성공해도 `운영시간 확인 필요(사용자 확인)` 상태를 유지한다. | 구현 보완 필요 | 실제 표시 시각과 추천 세션 시각을 분리한 09:59/10:00/17:59/18:00·18:00 경과/foreground 복귀, 8개 페이지·거리순·중복 없음·더 보기 route 0, 시장/거리/골목·시설/개별점포/도매·새벽/장기이용 fixture, 확인 전 0 route call·확인 뒤 경과 시간 차감/조건부 상태·주소 검색/좌표 지도 보기 fallback iOS 확인 필요 |
| UXV-43 | 최종 코스의 `코스 시작하기`는 별도 진행 화면을 push하지 않고 같은 화면을 active 상태로 바꾼다. 각 travel은 최적화 snapshot의 직전 지점 `sp`와 목적지 `ep`를 가진 Kakao app route→HTTPS→browser를 각 한 번 시도한다. 현재 다음 구간만 활성화하고 handoff 성공 때만 단계와 `routeOpened=true`를 전이한다. 실패·invalid 좌표·동일 tick 중복은 현재 단계와 재시도를 유지하며 마지막 travel 뒤 `도착 후 코스 마치기`를 제공한다. | 구현 전환 필요 | 1·2곳·왕복/도착지·최적 순서 snapshot, review→active stack 0, 현재/미래/완료 상태, app/web route 성공·실패·중복 fixture | 2곳 한 코스에서 같은 화면 전환과 최적 순서의 각 Kakao 출발·도착을 확인한다. GPS 자동 도착·background location은 없어야 하고 Live Activity는 UXV-51에서 별도 확인한다. |
| UXV-44 | 대표·대안 카드는 사진·장소명·활동 카테고리와 도착 여유를 제외한 `이동+선택 체류` 합계 `약 N분 코스`만 표시한다. 전체 카드 tap은 UXV-55의 장소 상세를 열고, 명시 선택 뒤 코스 보기 CTA가 실제 snapshot의 지도·세로 순서를 연다. 대중교통 leg는 `도보→대중교통→필요한 환승→마지막 도보`를 보존하고 실제 목적지까지 이어진다. 장소 상세 진입 route 호출은 0이며, 최종 코스 view에서만 UXV-46 endpoint walk 보충을 재사용한다. | 구현 전환 필요 | 카드→상세 route 0, 선택→코스 snapshot, 두 leg actual geometry·왕복/목적지·손상/부분 geometry fixture | 장소 상세와 최종 코스 지도를 구분해, 정보 확인만으로 경로를 재검증하지 않고 선택 뒤 실제 선·세로 순서가 유지되는지 확인 |
| UXV-45 | 출시 one-stop 결과는 미검증 후보가 남을 때만 `다른 장소 더 보기`를 표시한다. tap마다 최대 3개의 새 실제 검증 카드를 기존 목록 뒤에 붙이고, 대표·기존 순서와 상세 왕복 상태를 유지하며 자동으로 다음 페이지를 호출하지 않는다. 첫 결과 0건+`more_available`도 최종 실패가 아니라 명시 더보기 상태이며, 조건부 시장의 로컬 더보기와 문구·testID·호출을 분리한다. | 자동 확인 | initial 0/1+대안·네 page state·중복·연타·상세 왕복·조건부 더보기 fixture | U-ONE-MORE-01 UI와 QA-ONE-MORE-01 밀집 fixture를 수락했다. native 버튼 위치·로딩 체감·상세 왕복은 UIUX 정리 뒤 출시 후보 수동 smoke 1회에 합친다. |

### UXV-46 — 코스 상세의 선택 후 도보 연결 보충

- **관계:** UXV-44의 `상세 진입 route 0회`를 부분 교체한다. 추천·transit 재조회 0회는 유지하고, 선택한 코스의 50m 초과 누락 endpoint walk만 1곳 최대 4회·2곳 최대 6회 호출할 수 있다.
- **기대 결과:** 검증 snapshot 선을 먼저 표시한 뒤, 하차지점→장소를 포함한 누락 시작·종점만 private Kakao walk geometry로 연결한다. 로딩은 상세·CTA를 막지 않고, 실패는 부분 선·마커·세로 순서·CTA를 유지한다.
- **금지:** 직선·endpoint 좌표 강제 삽입·실시간 GPS·transit 재조회·보충 walk 분 수를 추천 시간에 합산하는 처리는 하지 않는다.
- **검증:** 50m/51m, walk/transit, 시작/종점, 1곳 두 legs/2곳 세 legs, 왕복/목적지, 성공/부분/실패, 연타/뒤로가기 메모리 재사용, private 비영속, 1곳 최대 4회·2곳 최대 6회를 fixture로 확인한다. 순서는 `API-ROUTE-GEOMETRY-03 → U-COURSE-GEOMETRY-02 → QA-COURSE-GEOMETRY-02`다.
- **상태:** 검증 완료. `QA-COURSE-GEOMETRY-02` 자동 하네스와 실기기 transit one-stop 1/1에서 하차지점→파란 도보선→장소 marker 연결, 기존 상세·CTA·시간 snapshot 유지를 확인했다.

### UXV-47 — 최대 2곳 제한 선택·취소 복원

- **관계:** UXV-20·29·44·45를 보완했던 기존 인라인 선택 이력이다. 동일 Results/ScrollView·선택 후 B region·복원·예산은 유지하지만 카드 tap 즉시 A/B 선택은 `UXV-55`의 상세 후 명시 선택으로 교체한다.
- **기대 결과:** 장소 상세의 명시 `선택하기`에서 A 선택이 시작된다. 기존 Results tree·ScrollView·헤더·시간 요약으로 돌아와 A는 safe-area 아래 compact sticky tray에 고정되고, 기존 one-stop 카드 영역만 같은 자리에서 B 후보/검증 중 skeleton으로 교체된다. exact 성공 B는 안정된 순서로 점진 표시한다. 선택 전 하단 CTA는 없고 A 선택 뒤 `이 장소로 코스 보기`, B 선택 뒤 `선택한 2곳 코스 보기`를 safe-area 위에 고정한다. B 선택은 같은 상세의 명시 행동이며 추가 route 0, CTA에서만 선택 snapshot 코스를 연다.
- **취소:** A의 `×`와 선택 중 뒤로가기는 A 선택 직전의 카드 ID·순서·이미 연 더보기·종료 상태·스크롤을 복원한다. B의 `×`는 A·후보·continuation을 유지하고 B만 제거한다. 취소 자체 route 0, stale 응답 반영 0, 재선택 뒤 session 누적 attempt 36 이하를 만족한다.
- **역선택 재사용:** 같은 session/input에서 A 선택으로 exact 확인된 `{A,B}`는 A 취소 후 B를 기준 장소로 선택했을 때 A를 첫 검증 후보로 다시 보여 준다. 신규 adapter/provider/ledger 증가는 0이고 기존 snapshot의 실제 방문 순서·legs를 유지한다. 새 session·입력 변경·미완료/stale 결과에는 적용하지 않는다.
- **B 카드 시간:** A one-stop이 51분, exact pair가 88분이면 B 카드에는 `약 88분 코스`가 아니라 `함께 가면 약 37분 추가`를 표시한다. 계산은 양쪽의 이동+체류만 사용하고 도착 여유는 제외한다. B→A 순서에도 동일하며, 차이가 양수가 아니거나 snapshot 계약이 맞지 않으면 `선택 시 전체 약 N분`으로 표시하고 B 단독 시간으로 오인시키지 않는다. 시간 표시 계산과 B 선택은 추가 route 0회다.
- **수량·실패:** 기본 B 3개는 목표일 뿐 보장이 아니며 0~2개와 이유를 허용한다. 더보기 포함 누적 6개를 넘지 않는다. 후보 없음·시간 불충족·no-route·provider/store/한도 실패에서도 A one-stop을 유지한다.
- **검증:** 고정 fixture로 점진 1→2→3, 0/1/2개 종료, 두 순서와 동률 A 우선, 취소 중 늦은 성공·실패, 다른 A 재선택, 첫 목록 더보기와 공유 12회 소진, B 상세 추가 route 0을 확인한다. 자동 게이트 전 Simulator 조작과 실제 API 호출은 하지 않는다.
- **상태:** 엔진·예산·Results 복원 기반은 수락 상태를 유지한다. `A 즉시 tray·CTA` 부분만 철회했으며 `UXV-55/56` 구현 뒤 새 상세 선택과 최적 순서 화면을 다시 검증한다.

### UXV-55 — 전체 지도형 장소 상세와 명시 선택

- **관계:** UXV-26/27과 UXV-47의 카드 tap 즉시 선택을 교체하고 UXV-38/39의 장소 정보 근거를 재사용한다.
- **기대 결과:** A0/A1의 장소 카드 tap은 선택·pair 시작 없이 전체 지도형 상세 B를 연다. 후보 marker와 이미 허용된 실제 현재 위치를 함께 표시하고 설정 출발지가 다르면 지점을 구분한다. 상세 진입은 새 위치 권한 prompt를 만들지 않으며 위치 권한 거절·미결정/GPS 실패는 설정 출발지 fallback과 짧은 안내로 상세를 유지한다. 하단 floating sheet는 사진·장소명·category/subCategory·근거 있는 설명·운영시간 상태·주소·카카오 장소 보기·`선택하기`를 제공한다.
- **선택/복원:** 닫기는 진입 전 A0/A1 목록·scroll·continuation을 유지한다. `선택하기`는 기존 Results 인스턴스로 pop해 A0→A1 또는 A1→A2만 전이하며 중복 Results stack을 만들지 않는다. 첫 A 확정 뒤에만 pair 계산을 시작한다.
- **데이터/호출:** 런타임 카탈로그와 사전 정제 TourAPI 근거만 사용한다. 없는 설명·운영시간을 합성하지 않는다. 상세 열기·닫기·카카오 장소 보기 자체의 recommendation/Route Proxy attempt는 0회다.
- **검증:** 현재 위치=출발지/다름, 위치 허용/거절/GPS 실패, 설명·시간 있음/없음, A0 닫기/선택, A1 B 닫기/선택, 연타/stale, scroll 복원, route/API 0을 고정 fixture로 확인한다. 실기기는 지도 viewport·floating sheet·safe area·외부 카카오 장소 보기만 1회 확인한다.
- **상태:** 제품 흐름·완료 UI 시나리오 최종 승인·구현 전.


## 주변 둘러보기 시트 배치 확정 — U-NEARBY-BROWSE-01 / UX-34 보완

지도 표시 사용자 확인 후, 이중 floating 패널의 가림/틈 문제를 다음으로 교체한다: 시트는 좌우0·하단0의 연결된 바텀시트(상단만 둥근 모서리), 탭바는 기존 위치/형태 그대로 시트 위에 floating 유지. 접힘/펼침 모두 아래 배경 연결, 접힘은 제목/첫 행 가시성, 목록 끝·상세 CTA는 실제 탭바 가림 frame과 끝 여백으로 접근 보장. 기존 safeBottom+72 기준의 떠 있는 시트는 철회하며 탭바 자체 변경/숨김은 하지 않는다. 상태: **사용자 확정·구현 전**. 실제 visible sheet viewport와 map occlusion inset, 큰 글씨/마지막 행/CTA 터치 검증은 `docs/work/uiux/nearby-browse.md` 최신 절을 따른다. 데이터·추천·지도 HTML 복구 정책 불변.


## DEC-NEARBY-BROWSE-01 / UX-34 — 주변 둘러보기 (2026-09-05)

관계: 기존 내 코스 최상위 탭을 대체하는 신규 정보 탐색 UX, 추천 정책은 불변. 약속 시각과 무관하게 기준 위치 직선거리3km 이내 기존 공개 카탈로그 장소를 지도+하단 거리순 목록으로 제공한다. 사진 마커/없음·실패 기본 마커, 겹침 cluster, 선택 마커 강조·이름, 목록/마커/상세 연결을 사용한다. 지도 이동은 기준 위치/목록 순서를 바꾸지 않는다. 동일 거리 ID tie, 동일좌표 cluster도 목록에서 전수 접근 가능해야 한다. 장소 상세는 시간 검증/코스 선택이 없는 정보 표시이며 인기·방문자 데이터나 운영시간을 합성하지 않는다. 기존 카카오 장소 opener만 재사용한다.

기존 내 코스 탭 → 주변 발견과 다른 저장 중심 진입 → 주변 둘러보기로 교체 → 장소 발견 목적 분리 → **확정·구현 전**. legacy 저장 데이터 삭제는 포함하지 않는다. 적격 공개 장소만 사용하고 hold/잘못된 좌표/중복 제외, 조건부 장소는 운영시간 확인 필요로 표시한다. 추천 Results의 조건부 시장10~18 gate는 유지하되 신규 정보 탐색을 검증 코스로 취급하지 않는다. 현위치 거절/실패는 수동 기준 위치 대안, 지도 실패는 목록 유지. 자동 추천·route·저장·체류 측정 호출0. 지도 타일/사진 로딩은 별도 네트워크임을 구분한다.

검증: 반경 경계/정렬·pan 불변·사진 fallback/cluster·시트와 선택 ID·권한 실패/지도 실패·외부 쓰기0 fixture와 마지막 지도 native smoke. 실행 명령 및 범위: `docs/work/uiux/nearby-browse.md`. 상태: 구현/검증 전.


## 2026-09-05 U-LOCATION-SEARCH-01 실기기 반환 보완

사용자 실기기에서 출발/도착 분리·현위치 흐름과 검색 기능은 정상 확인했으나 재포커스 시 키보드보다 확정 버튼이 늦게 이동했다. keyboard와 footer는 동일 전환으로 동기화하고 종료 후 추가 점프를 제거한다(원인 조사·native 검증 필요). 기존 지도 하단 검색 대안 유지 규칙은 **부분 철회**: 뒤로가기가 같은 검색 draft를 복원하므로 하단 `검색으로 선택`을 제거하고 실패 안내도 뒤로가기 검색 대안에 맞춘다. 기존 노선 별도 줄 유지도 **부분 철회**: 결과 정보는 장소명·주소만, 선택됨은 상태로 유지하며 provider 데이터는 보존한다. 미선택 행 경계 부족은 결과 사이 가는 구분선으로 보완한다. 이유는 중복 행동/정보 감소와 버튼 인지성 향상이다. 상태: **사용자 확정·보완 구현 전**. 기존 검색/권한/시간/엔진 정책 불변. 최신 명령과 회귀/native 기준은 `docs/work/uiux/location-search-interaction.md` 상단을 따른다.


## 2026-09-05 위치 검색 상호작용 확정 — U-LOCATION-SEARCH-01

기존 선택 후 키보드 유지·동일 검색 선택 해제·출발/도착 draft 공유·검색 중 지도 숨김 → 사용자 확정 동작 혼란 → 자동 검색 유지, 결과 선택 시 키보드 닫기/임시 선택, 명시 확정만 부모 적용, 선택이 있을 때만 keyboard-aware CTA 표시 → 검색·선택·확정 분리 → **현행 결정·구현 전**. 동일 검색어 성공/진행 중 재검색은 중복하지 않고 선택 유지, 실패는 명시 재시도 가능하며 실제 검색어 수정은 선택 해제다. 미선택 submit은 검색/키보드 닫기만 하며 첫 결과 자동선택0이다.

출발/도착 새 편집은 빈 draft로 시작하되 기존 확정 좌표는 유지한다. 같은 target의 지도 취소/검색 복귀는 검색 draft·선택·목록 위치를 복원한다. 전체 닫기/필드 전환은 draft를 폐기하고 늦은 검색·GPS·주소 응답을 차단한다. 검색창 아래 현재위치/지도에서 선택은 항상 동일 너비 가로 배치한다. GPS도 임시 선택 뒤 명시 확정하며 권한 거절/실패에 검색/지도 대안을 유지한다. 다크/파랑·읽기 쉬운 장소명/주소·선택됨 표시·필수 아이콘만 유지한다. 추천/DB/API provider·검색 debounce/TTL·시간 안전성 정책은 불변이다.

관계: 기존 위치 입력/통합 입력 계약 보완. 검증은 검색/선택/submit/포커스/실제 편집, 필드 전환 및 같은 필드 지도 왕복, GPS stale·중복 확정0, 선택×키보드 상태·접근성 fixture와 최소 native smoke로 구분한다. 상세 실행 기준 및 검사 항목: `docs/work/uiux/location-search-interaction.md`. 과거 지도 버튼 !query 조건과 항상 노출 비활성 확정 버튼은 철회하며 이 절을 우선한다.


## 2026-09-05 통합 입력 구성 확정 — U-SETUP-UNIFIED-01

- 관계: REC-01/REC-18과 위치 입력 UX의 보완. 이전 중간 `경로 설정→경로 적용` 및 별도 도착 시간 화면 → 필드 편집마다 왕복하는 문제 → TimeSetup에 출발지·목적지 버튼과 상시 도착 시간 휠·여유 slider·하단 추천 CTA를 통합 → 입력 관계를 한눈에 확인하기 위함 → **현행 결정·구현 전**. 중간 경로 화면과 시간 휠 sheet 초안은 철회하며 검색/지도 선택기 자체는 유지한다. 공통 규칙 3-2의 과거 중간 화면/경로 적용 문구보다 이 결정이 우선한다.
- 개발 테스트 시각·출시 추천 QA 버튼은 삭제/비활성화하지 않는다. 기존 internal gate와 기능을 보존한다. 개발 영역이 없는 일반 입력 영역 기준으로 기본 글자 크기 세로 375×812, 390×844, 402×874pt에서 본문 스크롤·입력/CTA 가림 없이 배치한다. 실제 safe area/footer를 반영하고 터치 최소 44pt를 지킨다. 작은 화면/큰 글씨/오류 확장에서는 접근성을 위한 overflow를 허용한다. internal 개발 영역으로 인한 추가 높이는 일반 화면 높이 산정에서 제외하며 개발 버튼 접근을 별도로 검증한다.
- 시간 휠의 정착 값은 입력에 즉시 반영한다. 별도 시간 적용 버튼과 편집만으로 추천 호출은 없다. 120분·목적지 null=복귀·여유 기본10/5~30분·선택적 햅틱·API 예산·인증·저장 경계는 변경하지 않는다.
- 검증: production-used handler 고정 fixture로 직접 입력/위치 확정·취소/늦은 GPS/시간 경계/추천 중복/개발 gate를 확인한다. layout fixture는 기본 화면 contentHeight≤viewportHeight, 가림0; native 캡처 확인과 별도 구분한다. 완료 체크는 구현 및 검증 후에만 한다. 상세 명령: `docs/work/uiux/unified-time-route-setup.md`.

## 2026-09-05 현행 계약 복원 기록

- 사용자 승인: 기존 계약 복원. 관계는 REC-30·COURSE-03/11/15·UX-27·UXV-44/46의 정합성 보완이며 새 정책 도입이 아니다.
- 이전 지시 → 문제: 통합 세션이 U-PLACE-COURSE-FLOW-01 보완에서 과거 전체 체류 분 비노출과 one-stop 전용 connector 4개를 모든 코스에 적용해 review 정보와 2곳 마지막 도보선을 누락시켰다.
- 교체 방식: 최종 코스 **review만** 각 snapshot stop의 계획 체류를 `둘러보기 약 N분`/short `가볍게 둘러보기 약 N분`으로 표시한다. Results·선택 전 장소 정보 상세 B·active는 체류 분 비노출이며 active short 문구는 `가볍게 둘러보기`다. 같은 지도·세로 코스 트리는 유지한다.
- 교체 방식: 선택 후 CourseConfirm의 누락 endpoint walk는 **1곳 최대 4개 / 2곳 최대 6개**, 동시 최대 2개다. 50m 초과의 실제 누락만 조회하고 기존 in-flight/10분 메모리·TTL을 유지한다. 유효 캐시 재사용은 신규 시도 0이며 TTL 만료까지 무기한 0으로 해석하지 않는다. 상세 B 진입·추천/transit 재계산은 0이다.
- 이유: 2026-09-02의 상세 시간 맥락화와 기존 two-stop의 세 legs×두 endpoint 계약을 회복한다. 과거 이력의 `최대 4개`는 one-stop 범위이며 2곳에 적용하지 않는다. 전체 비노출·모든 코스 4개 제한은 **철회**, 위 기준은 **현행·복원 구현 대기**다.
- 실행 단일 기준: `docs/work/uiux/place-detail-and-optimized-course-flow.md`의 마지막 `현행 계약 복원`. 기존 상세 후 명시 선택·모드 통합·패널 실측·실행형 테스트는 보존한다. 과거 카드 즉시 선택·B 전체시간 표시·별도 진행 화면을 복원하지 않는다.

### UXV-56 — 최적 순서 코스의 단일 review/active 화면

- **관계:** UXV-43/44와 COURSE-03/07/12를 보완하고 별도 CourseConfirm→Progress 화면 push를 교체한다.
- **기대 결과:** 2곳 코스는 선택 순서가 아니라 exact 검증된 최소 이동 유효 순서로 지도 번호·세로 카드·route handoff를 일치시킨다. `코스 시작하기` 전에는 동일 화면의 review 상태, 시작 뒤에는 active 상태이며 지도·순서·snapshot identity를 유지한다. active는 현재 다음 구간의 Kakao 길찾기만 활성화하고 완료·미래 구간은 상태만 표시한다.
- **실패/복구:** handoff 실패는 현재 단계와 재시도를 유지한다. 외부 지도 복귀와 Home 이어가기는 같은 `courseRunId`·마지막 단계로 복원한다. 미래 구간 skip, UI 재정렬, 시작 전 체류 측정과 route 재호출은 금지한다.
- **검증:** `O-A-B-D` 우선/`O-B-A-D` 우선/한 순서만 유효/둘 다 실패/동률, 1곳/2곳·왕복/별도 도착지, review→active navigation stack 증가 0, 현재 버튼 하나만 활성, handoff 성공/실패/연타, Home 재진입과 완료 기록 1회를 fixture로 확인한다. 실기기는 2곳 한 코스에서 지도 번호·카드·Kakao 출발/도착 순서만 제한 확인한다.
- **상태:** 제품 흐름·완료 UI 시나리오 최종 승인·구현 전.
